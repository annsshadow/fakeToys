# x_portal_assemble_designer

## Endpoints

- `GET /jaxrs/id/{count}` — getId
- `GET /jaxrs/portal/list` — listApplication
- `GET /jaxrs/portal/list/summary` — listApplicationSummary
- `GET /jaxrs/portal/list/summary/portalcategory/{applictionCategory}` — listApplicationByCategorySummary
- `GET /jaxrs/portalcategory/list` — listApplicationCategory
- `GET /jaxrs/portal/list/portalcategory/{applicationCategory}` — listApplicationByCategory
- `POST /jaxrs/portal` — addApplication
- `PUT /jaxrs/portal/{id}` — updateApplication
- `PUT /jaxrs/portal/{id}/icon` — updateApplicationIcon
- `GET /jaxrs/portal/{id}` — getApplication
- `DELETE /jaxrs/portal/{id}` — removeApplication
- `DELETE /jaxrs/portal/{id}` — deleteApplication
- `GET /jaxrs/page/list/portal/{id}` — listPage
- `GET /jaxrs/source/list/portal/{id}` — listSource
- `GET /jaxrs/script/list/portal/{id}` — listScript
- `GET /jaxrs/menu/list/portal/{id}` — listMenu
- `GET /jaxrs/widget/list/portal/{id}` — listWidget
- `GET /jaxrs/file/list/application/{id}` — listFile
- `GET /jaxrs/page/{id}` — getPage
- `POST /jaxrs/page` — addPage
- `DELETE /jaxrs/page/{id}` — removePage
- `DELETE /jaxrs/page/{id}` — deletePage
- `PUT /jaxrs/page/{id}` — updatePage
- `GET /jaxrs/templatepage/{id}` — getPageTemplate
- `DELETE /jaxrs/templatepage/{id}` — deletePageTemplate
- `POST /jaxrs/templatepage` — addPageTemplate
- `GET /jaxrs/templatepage/list` — listPageTemplate
- `GET /jaxrs/templatepage/list/category` — listPageTemplateCategory
- `PUT /jaxrs/templatepage/list/category` — listPageTemplateByCategory
- `GET /jaxrs/source/{id}` — getSource
- `POST /jaxrs/source` — addSource
- `DELETE /jaxrs/source/{id}` — removeSource
- `PUT /jaxrs/source/{id}` — updataSource
- `GET /jaxrs/dict/list/paging/{page}/size/{size}` — listDictionaryPaging
- `GET /jaxrs/dict/list/portal/{portalId}` — listDictionary
- `GET /jaxrs/dict/{id}` — getDictionary
- `POST /jaxrs/dict` — addDictionary
- `DELETE /jaxrs/dict/{id}` — removeDictionary
- `PUT /jaxrs/dict/{id}` — updataDictionary
- `GET /jaxrs/script/{id}` — getScript
- `POST /jaxrs/script` — addScript
- `DELETE /jaxrs/script/{id}` — removeScript
- `DELETE /jaxrs/script/{id}` — deleteScript
- `PUT /jaxrs/script/{id}` — updataScript
- `GET /jaxrs/script/application/{applicationId}/name/{name}` — getScriptByName
- `GET /jaxrs/menu/{id}` — getMenu
- `POST /jaxrs/menu` — addMenu
- `DELETE /jaxrs/menu/{id}` — removeMenu
- `PUT /jaxrs/menu/{id}` — updataMenu
- `GET /jaxrs/widget/{id}` — getWidget
- `POST /jaxrs/widget` — addWidget
- `DELETE /jaxrs/widget/{id}` — removeWidget
- `DELETE /jaxrs/widget/{id}` — deleteWidget
- `PUT /jaxrs/widget/{id}` — updateWidget
- `GET /jaxrs/file/{id}` — getFile
- `POST /jaxrs/file` — addFile
- `DELETE /jaxrs/file/{id}` — removeFile
- `DELETE /jaxrs/file/{id}` — deleteFile
- `PUT /jaxrs/file/{id}` — updataFile
- `GET /jaxrs/file/application/{applicationId}/name/{name}` — getFileByName
- `GET /jaxrs/file/{id}/download` — downloadFile
- `GET /jaxrs/file/{id}/content` — readFile
- `POST /jaxrs/file/{id}/upload` — uploadFile
- `UNKNOWN x_portal_assemble_designer` — clazz
