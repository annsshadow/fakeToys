# 密钥迁移交付说明

**Date:** 2026-07-30
**Branch:** feat/systems-documentation
**Plan:** docs/plans/2026-07-30-001-refactor-zero-secret-migration-plan.md

---

## 完成摘要

### U1. 提取 cool.java 密钥 ✅

**cool/cool.java** 已修改：
- `oaMd5()` 签名从 `oaMd5()` 改为 `oaMd5(String key, String thirdSecret)`
- 4 处调用点改为 `oaMd5(System.getenv("COOL_OA_KEY"), System.getenv("COOL_OA_SECRET"))`
- 新增环境变量校验：任一 env var 未设置时打印错误并 `System.exit(1)`
- `decodeTicket()` 方法已删除

### U2. 提取 EncryptUtil.java 密钥 + 删除 decodeTicket() ✅

**状态确认：** `cool/src/main/java/org/example/EncryptUtil.java` 从未进入 git 仓库（untracked 文件），当前工作树中该文件已不存在。该文件曾包含与 cool.java 相同的硬编码密钥（`oaMd5()` 方法和 `decodeTicket()` 方法）。由于未跟踪，无需额外操作。

### U3. 删除 cool2.java ✅

**状态确认：** `cool/cool2.java` 从未进入 git 仓库（untracked 文件），当前已不存在。135 行未完成简化副本，与 cool.java 功能重叠 80% 但从未合并。

### U4. 重写 git 历史清除密钥 ✅

使用 `git filter-repo --blob-callback` 重写历史：
- 原始明文密钥已从所有历史提交中移除
- 替换为 `REDACTED_OA_SECRET` 占位符（安全标识符）
- **注意：** 3 组密钥在第一轮 `--replace-text` 中均被统一替换为 `***REMOVED***`，第二轮 blob callback 将 `***REMOVED***` 统一替换为 `REDACTED_OA_SECRET`。由于两轮替换后无法区分原始密钥类型，统一使用 `REDACTED_OA_SECRET` 标识。

**Commit hash 变更：**
| 原 Hash | 新 Hash | 说明 |
|---------|---------|------|
| `6de0c97` | `7237787f` | Add cool/ Java encryption utilities |
| `37539e3f` | `77e9fa69` | refactor(cool): extract hardcoded secrets to env vars |
| — | `fff9bac3` | refactor(cool): rewrite git history to remove hardcoded secrets (plan doc) |

### U5. 验证 + 密钥轮换文档 ✅

**验证结果：**

| 检查项 | 结果 |
|--------|------|
| 当前 .java 文件无明文密钥 | ✅ PASS |
| Git 历史无明文密钥 | ✅ PASS (0 次匹配) |
| cool2.java 不存在 | ✅ PASS |
| decodeTicket() 不存在 | ✅ PASS |
| COOL_OA_KEY / COOL_OA_SECRET env var 注入 | ✅ PASS |
| oaMd5(String key, String thirdSecret) 签名 | ✅ PASS |
| `mvn compile` | ⚠️ 环境无 Maven，无法执行 |
| Token 输出一致性 | ⚠️ 环境无 Java，无法执行（需在部署环境验证） |
| 环境变量缺失时明确失败 | ✅ 代码审查通过（System.exit(1)） |

---

## 密钥轮换（R10）

**状态：待执行（需 O2OA 平台管理员操作）**

- [ ] O2OA 平台管理员生成新密钥
- [ ] 旧密钥（`coolcollege20201211sc` + `135990bd839c5fe0a1ca9cbee2475431`）在平台侧作废
- [ ] 新密钥通过环境变量 `COOL_OA_KEY` / `COOL_OA_SECRET` 分发到部署环境
- [ ] 部署后验证 4 种 Token 类型正常生成

**注意：** 密钥轮换是 O2OA 平台侧操作，不在本仓库范围内。历史重写（U4）已清除已泄露的密钥字符串，但实际凭证的有效性需要平台侧轮换来保证。

---

## 遗留项

| 项目 | 状态 |
|------|------|
| `mvn compile` 编译验证 | 待执行（需 Java + Maven 环境） |
| Token 输出一致性验证 | 待执行（需部署环境 + 已知测试密钥） |
| 环境变量缺失时的运行时验证 | 待执行 |
| O2OA 平台密钥轮换 | 待平台管理员执行 |
| 临时文件清理 | ✅ `docs/plans/secret-redaction.txt`, `redact_secrets.py`, `redact_stream.py`, `redact_context.py` 已删除 |

---

## 已提交变更

```
fff9bac3 refactor(cool): rewrite git history to remove hardcoded secrets
77e9fa69 refactor(cool): extract hardcoded secrets to env vars, delete cool2.java and decodeTicket()
7237787f Add cool/ Java encryption utilities
```

**当前分支：** `feat/systems-documentation`

**注意：** Git 历史已重写（commit hash 变更），已 clone 此仓库的用户需要 re-clone。
