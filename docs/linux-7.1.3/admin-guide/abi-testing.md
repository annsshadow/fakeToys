## ABI 测试符号


记录那些被认为稳定、因为该接口的主要开发已完成的接口。

可以在不破坏当前接口的前提下更改接口以添加新特性，除非在其中发现了严重错误或安全问题。

用户空间程序可以开始依赖这些接口，但它们必须意识到，在这些接口被标记为稳定之前可能会发生变化。

强烈鼓励使用这些接口的程序将自己的名称添加到这些接口的描述中，以便内核开发者在发生变化时能轻松通知它们。

   :no-files:
