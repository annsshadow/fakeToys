# Kernel Configuration Features

This page lists the configurable features available in the Linux kernel.

For architecture-specific features, see the [arch/](../../arch) section.

## Building the Kernel

See the [kbuild documentation](../../kbuild) for details on kernel configuration.