## saa7134 驱动


Author Gerd Hoffmann


这是面向基于 saa7130/33/34/35 的采集/TV 板的 v4l2/oss 设备驱动。


### 状态


几乎一切都在工作。视频、声音、调谐器、收音机、mpeg ts……

与 bttv 一样，需要进行板特定的调整。查阅 CARDLIST 以获取已知 TV 卡列表，以及 saa7134-cards.c
以获取驱动的板配置信息。


### 构建


一旦你获取了内核源码，应当配置、构建、安装并启动新内核。你至少需要
```

    ./scripts/config -e PCI
    ./scripts/config -e INPUT
    ./scripts/config -m I2C
    ./scripts/config -m MEDIA_SUPPORT
    ./scripts/config -e MEDIA_PCI_SUPPORT
    ./scripts/config -e MEDIA_ANALOG_TV_SUPPORT
    ./scripts/config -e MEDIA_DIGITAL_TV_SUPPORT
    ./scripts/config -e MEDIA_RADIO_SUPPORT
    ./scripts/config -e RC_CORE
    ./scripts/config -e MEDIA_SUBDRV_AUTOSELECT
    ./scripts/config -m VIDEO_SAA7134
    ./scripts/config -e SAA7134_ALSA
    ./scripts/config -e VIDEO_SAA7134_RC
    ./scripts/config -e VIDEO_SAA7134_DVB
    ./scripts/config -e VIDEO_SAA7134_GO7007

```
```

    make && make modules_install && make install

```
一旦新内核启动，saa7134 驱动应当自动加载。

根据你的卡，你可能必须传递 `card=<nr>` 作为 insmod 选项。
如果是这样，请查阅 Documentation/admin-guide/media/saa7134-cardlist.rst
以获取有效选项。

一旦你有了卡的型号编号，你可以通过一个文件传递模块配置（通常是 `/etc/modules.conf` 或
`/etc/modules-load.d/` 中的某个文件，但实际位置取决于你的
```

    options saa7134 card=13 # Assuming that your card type is #13


```
### 变更 / 修复


请将针对 linux media git 树的 unified diff 邮件发送到 linux-media AT vger.kernel.org：

    https://git.linuxtv.org/media.git/

这通过在一个 git 树克隆上提交补丁并使用 `git send-email` 提交补丁来完成。别忘了在
lots 处描述它改变了什么 / 修复了什么问题 / 有什么用……


### 已知问题


- flyvideos 的调谐器不会被自动检测，且默认值可能不适用于你，具体取决于你拥有的版本。
  有一个 `tuner=` insmod 选项可以覆盖驱动的默认值。

### 致谢


andrew.stevens@philips.com + werner.leeb@philips.com 提供了 saa7134 硬件规格与样片板。
