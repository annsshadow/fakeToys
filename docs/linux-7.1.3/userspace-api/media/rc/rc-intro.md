######## 简


目前，大多数模拟和数字设备都配有用于遥控器的红外（IR）输入。每个制造商都有自己类型的控制器。同一制造商根据设备不同提供不同类型控制器的情形也并不罕见

遥控器接口被映射为普通的 evdev/input 接口，就像键盘或鼠标一样。因此，它使用所有已为其他输入设备定义的 ioctl

不过，遥控器比普通输入设备更灵活，因为红外接收器（和/或发射器）可以与各种各样的不同红外遥控器配合使用

为了提供灵活性，遥控器（Remote Controller）子系统允许通过 sysfs 类节<remote_controllers_sysfs_nodes> 控制 RC 特定的属性
