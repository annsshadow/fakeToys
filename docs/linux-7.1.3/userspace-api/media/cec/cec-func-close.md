
######## cec close()

## 名称


cec-close - 关闭一cec 设备

## 概要



    #include <unistd.h>


## 参数


`fd`
    `open()` 返回的文件描述符

## 描述


关闭 cec 设备。与文件描述符关联的资源被释放。设备配置保持不变

## 杩斿洖鍊。


`close()` 成功时返0。出错时返回 -1，并相应地设`errno`。可能的错误码包括：

`EBADF`
    `fd` 不是有效的已打开文件描述符
