## 内核 驱动 lm85


鍙楁敮鎸?chips:

  - National Semiconductor LM85 (B 鍜?C versions)

    Prefix: 'lm85b' 鎴?'lm85c'

    地址 scanned: I2C 0x2c, 0x2d, 0x2e

    Datasheet: http://www.national.com/pf/LM/LM85.html

  - Texas Instruments LM96000

    Prefix: 'lm9600'

    地址 scanned: I2C 0x2c, 0x2d, 0x2e

    Datasheet: https://www.ti.com/lit/ds/symlink/lm96000.pdf

  - Analog 设备 ADM1027

    Prefix: 'adm1027'

    地址 scanned: I2C 0x2c, 0x2d, 0x2e

    Datasheet: https://www.onsemi.com/PowerSolutions/product.do?id=ADM1027

  - Analog 设备 ADT7463

    Prefix: 'adt7463'

    地址 scanned: I2C 0x2c, 0x2d, 0x2e

    Datasheet: https://www.onsemi.com/PowerSolutions/product.do?id=ADT7463

  - Analog 设备 ADT7468

    Prefix: 'adt7468'

    地址 scanned: I2C 0x2c, 0x2d, 0x2e

    Datasheet: https://www.onsemi.com/PowerSolutions/product.do?id=ADT7468

  - SMSC EMC6D100, SMSC EMC6D101

    Prefix: 'emc6d100'

    地址 scanned: I2C 0x2c, 0x2d, 0x2e

    Datasheet: http://www.smsc.com/media/Downloads_Public/discontinued/6d100.pdf

  - SMSC EMC6D102

    Prefix: 'emc6d102'

    地址 scanned: I2C 0x2c, 0x2d, 0x2e

    Datasheet: http://www.smsc.com/main/catalog/emc6d102.html

  - SMSC EMC6D103

    Prefix: 'emc6d103'

    地址 scanned: I2C 0x2c, 0x2d, 0x2e

    Datasheet: http://www.smsc.com/main/catalog/emc6d103.html

  - SMSC EMC6D103S

    Prefix: 'emc6d103s'

    地址 scanned: I2C 0x2c, 0x2d, 0x2e

    Datasheet: http://www.smsc.com/main/catalog/emc6d103s.html

Authors:
       - Philip Pokorny <ppokorny@penguincomputing.com>,
       - Frodo Looijaard <frodol@dds.nl>,
       - Richard Barrington <rich_b_nz@clear.net.nz>,
       - Margit Schubert-While <margitsw@t-online.de>,
       - Justin Thiessen <jthiessen@penguincomputing.com>

### Description


驱动 implements 支持 用于 the National Semiconductor LM85 
compatible chips including the Analog 设备 ADM1027, ADT7463, ADT7468 
SMSC EMC6D10x chips family.

The LM85 uses the 2-wire 接口 compatible the SMBUS 2.0
specification. 使用 一analog 数字 converter measures three (3)
temperatures five (5) voltages. 具有 four (4) 16-counters 用于
measuring 风扇 speed. Five (5) 数字 inputs provided 用于 sampling the
VID signals 来自 the processor the VRM. Lastly, 存在 three (3) PWM
outputs 使用 control 风扇 speed.

The 电压 inputs 具有 内部 scaling resistors 因此 the 以下
电压 measured 外部 resistors:

  2.5V, 3.3V, 5V, 12V, CPU 核心 电压 (2.25V)

The temperatures measured one 内部 diode, two remote diodes.
Remote 1 generally the CPU 温度. 这些 inputs designed 
measure 一thermal diode 类似 the one 一Pentium 4 processor 一套接
423 套接478 package. 它们 measure 温度 使用 一
transistor 类似 the 2N3904.

一sophisticated control 系统 用于 the PWM outputs designed 进入 the
LM85 allows 风扇 speed adjusted automatically 基于 任何 the
three 温度 传感 每个 PWM 输出 individually adjustable 
programmable. 一configured, the LM85 adjust the PWM outputs 
响应 the measured temperatures further host intervention.
特已禁用于 manual control the PWM's.

每个 the measured inputs (电压, 温度, 风扇 speed) 具有
corresponding high/low limit  The LM85 信号 一ALARM 任何
measured exceeds 任一limit.

The LM85 samples 全部 inputs continuously. The lm85 驱动 读取
the 寄存更多 通常 一一second. Further, 配置 数据 
读取 一每个 5 minutes. 存在 twice 作为 much 配置 数据 作为
measurements, 因此 将会 seem 一worthwhile optimization.

### 特殊 特


The LM85 具有 four 风扇 speed monitoring modes. The ADM1027 具有 two.
两具有 特殊 circuitry compensate 用于 PWM interactions the
TACH 信号 来自 the fans. The ADM1027 configured measure the
speed 一two wire 风扇, the 输入 conditioning circuitry 不同
用于 3-wire 2-wire 模式. 用于 reason, the 2-wire 风扇 modes 
exposed 用户 control. The BIOS 应当 initialize them the correct
模式. ve designed 您的 own ADM1027, ll 具有 modify the
初始化_client 函数 add 一insmod 参数 set up.

smooth the 响应 fans changes 温度, the LM85 具有 一
可filter 用于 smoothing temperatures. The ADM1027 具有 the 相同
配置 选项 uses rate limit the changes 风扇 speed 改为.

The ADM1027, ADT7463 ADT7468 具有 一10-ADC 因此
measure temperatures 0.25 degC resolution. 它们 提供 一偏移
the 温度 readings automatically applied 期间
measurement. 偏移 使用 zero out 任何 错误 由于 traces
placement. The documentation says the 偏移 0.25 degC
steps, initial testing the ADM1027 曾是 1.00 degC steps. Analog
设备 具有 confirmed "bug". The ADT7463 reported work 作为
描述 the documentation. The 电流 lm85 驱动 执行 显示 the
偏移 注册.

The ADT7468 具有 一high-frequency PWM 模式, 何处 全部 PWM outputs 
driven 一22.5 kHz clock. 这是 一全局 模式, per-PWM 输出,
means 设置 任何 PWM frequency 上文 11.3 kHz switch
全部 3 PWM outputs 一22.5 kHz frequency. Conversely, 设置 任何 PWM
frequency 下文 11.3 kHz switch 全部 3 PWM outputs 一frequency
之间 10 100 Hz, 然后 tuned separately.

参见 the 厂商 datasheets 用于 更多 information. 存在 应用程序 注意
来自 National (AN-1260) 一额外 information 关于 the LM85.
The Analog 设备 datasheet very detailed describes 一procedure 用于
determining 一optimal 配置 用于 the automatic PWM control.

The SMSC EMC6D100 & EMC6D101 监视外部 voltages, temperatures, 
风扇 speeds. 它们 使用 monitoring capability alert the 系统 out
limit conditions automatically control the speeds 多个
fans 一PC embedded 系统. The EMC6D101, 可用 一24-pin SSOP
package, the EMC6D100, 可用 一28-pin SSOP package, designed
注册 compatible. The EMC6D100 offers 全部 the 特the
EMC6D101 增强额外 电压 monitoring 系统 control 特
Unfortunately 它是 可能 distinguish 之间 the package
versions 注册 level 因此 这些 额外 电压 inputs 读取
zero. EMC6D102 EMC6D103 特额外 ADC 从extending precision
电压 温度 channels.

SMSC EMC6D103S similar EMC6D103, 执行 支持 pwm#_auto_pwm_minctl
鍜?temp#_auto_temp_off.

The LM96000 supports 额外 high frequency PWM modes (22.5 kHz, 24 kHz,
25.7 kHz, 27.7 kHz 30 kHz), configured 一per-PWM basis.

### 硬件 Configurations


The LM85 jumpered 用于 3 不同 SMBus 地址. 存在
其他 硬件 配置 选项 用于 the LM85.

The lm85 驱动 detects 两LM85B LM85C revisions the 芯片. 参见 the
datasheet 用于 一complete description the differences. 其他 
identifying the 芯片, the 驱动 behaves differently regard 
这些 two chips. The LM85B recommended 用于 designs.

The ADM1027, ADT7463 ADT7468 chips 具有 一可SMBALERT 输出
使用 信号 the chipset case 一limit exceeded the
温度 传感fail. 各个 传感中断 masked 因此
瀹冧滑 won't trigger SMBALERT. The SMBALERT 杈撳嚭 鑻?configured replaces one
the 其他 函数 (PWM2 ). functionality implemented
电流 驱动.

The ADT7463 ADT7468 具有 一可THERM 输出/输入 
connected the processor PROC_HOT 输出. 可用, the autofan
control 动Tmin 特已启keep the 系统 温度
之内 spec (just!) the least 可能 风扇 noise.

### 配置 Notes


Besides 标准 interfaces 驱动 adds 以下:

- Temperatures 鍜?Zones

每个 温度 传感associated 一Zone. 存在 three
传感因此 three zones (# 1, 2 3). 每个 zone 具有 the 以下
温度 配置 points:

- temp#_auto_temp_off
 - 温度 下文 fans 应当 off spinning very low.
- temp#_auto_temp_min
 - 温度 在…上 fans 启动 spin.
- temp#_auto_temp_max
 - 温度 fans spin full speed.
- temp#_auto_temp_crit
 - 温度 全部 fans 运行 full speed.

##### PWM Control


存在 three PWM outputs. The LM85 datasheet suggests the
pwm3 输出 control 两风扇3 风扇4. 每个 PWM individually
configured assigned 一zone 用于 control  每个 PWM 
configured individually 根据 the 以下 选项.

- pwm#_auto_pwm_min
 - specifies the PWM 用于 temp#_auto_temp_off
	  温度. (PWM 来自 0 255)

- pwm#_auto_pwm_minctl
 - 标志 selects 用于 temp#_auto_temp_off 温度
	  the behaviour fans. 写入 1 let fans spinning 
	  pwm#_auto_pwm_min 写入 0 let them off.


	具有 已经 reported 存在 一bug the LM85 causes
	the 标志 associated the zones the PWMs. 
	contradicts 全部 the published documentation. 设置 pwm#_min_ctl
	case actually affects 全部 PWMs controlled zone '#'.

##### PWM Controlling Zone selection


- pwm#_auto_channels
 - controls zone 鍗?associated 涓?PWM

配置 choices:

========== =============================================
鍊?     Meaning
========== =============================================
      1    Controlled 鐢?Zone 1
      2    Controlled 鐢?Zone 2
      3    Controlled 鐢?Zone 3
     23    Controlled 鐢?higher temp 鐨?Zone 2 鎴?3
    123    Controlled 鐢?highest temp 鐨?Zone 1, 2 鎴?3
      0    PWM 始终 0%  (off)
     -1    PWM 始终 100%  (full 
     -2    Manual control (写入 'pwm#' set)
========== =============================================

The National LM85's 具有 two 厂商 特定 配置
特 Tach. 模式 Spinup Control. 用于 更多 details 这些,
参见 the LM85 datasheet 应用程序 注意 AN-1260. 这些 特
currently 受支the lm85 驱动.

The Analog 设备 ADM1027 具有 若干 厂商 特定 enhancements.
The 数字 pulses-per-rev the fans set, Tach monitoring
optimized 用于 PWM 操作, 一偏移 applied 
the temperatures compensate 用于 systemic 错误 the
measurements. 这些 特currently 受支the lm85
驱动.

此外 the ADM1027 特 the ADT7463 ADT7468 具有
Tmin control 鍜?THERM asserted counts. Automatic Tmin control acts 鍒。
adjust the Tmin maintain the measured 温度 传感一
specified 温度. 那里 isn't much documentation 特
the ADT7463 数据 sheet. 这是 受支电流 驱动.
