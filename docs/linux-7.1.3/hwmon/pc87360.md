## 内核 驱动 pc87360


受支持 chips:

  - National Semiconductor PC87360, PC87363, PC87364, PC87365 和 PC87366

    Prefixes: 'pc87360', 'pc87363', 'pc87364', 'pc87365', 'pc87366'

    地址 scanned: none, 地址 读取 来自 Super I/O 配置 space

    Datasheets: 无 longer 可用

Authors: Jean Delvare <jdelvare@suse.de>

Thanks 到 Sandeep Mehta, Tonko de Rooy 和 Daniel Ceregatti 用于 testing.

Thanks 到 Rudolf Marek 用于 helping me investigate conversion issues.


### 模块 参数


- 初始化 int
    芯片 初始化 level:

    - 0: None
    - **1**: Forcibly 启用 内部 电压 和 温度 channels,
      except 在9
    - 2: Forcibly 启用 全部 电压 和 温度 channels, except 在9
    - 3: Forcibly 启用 全部 电压 和 温度 channels, including 在9

注意 该 此 参数 具有 无 effect 用于 the PC87360, PC87363 和 PC87364
chips.

也 注意 该 用于 the PC87366, 初始化 levels 2 和 3 don't 启用
全部 温度 channels, 因为 一些 的 them share pins 与 每个 其他,
因此 它们 可't 为 使用 同时.


### Description


The National Semiconductor PC87360 Super I/O 芯片 包含 monitoring 和
PWM control circuitry 用于 two fans. The PC87363 芯片 是 similar, 和 the
PC87364 芯片 具有 monitoring 和 PWM control 用于 一个 third 风扇.

The National Semiconductor PC87365 和 PC87366 Super I/O chips 是 complete
硬件 monitoring chipsets, 不 仅 controlling 和 monitoring three fans,
但 也 monitoring eleven 电压 inputs 和 two (PC87365) 或 up 到 four
(PC87366) temperatures.

  =========== ======= ======= ======= ======= =====
  芯片        #vin    #风扇    #pwm    #temp   devid
  =========== ======= ======= ======= ======= =====
  PC87360     -       2       2       -       0xE1
  PC87363     -       2       2       -       0xE8
  PC87364     -       3       3       -       0xE4
  PC87365     11      3       3       2       0xE5
  PC87366     11      3       3       3-4     0xE9
  =========== ======= ======= ======= ======= =====

The 驱动 assumes 该 无 多于 one 芯片 是 present, 和 one 的 the
标准 Super I/O 地址 是 使用 (0x2E/0x2F 或 0x4E/0x4F)

### 风扇 Monitoring


风扇 rotation speeds 是 reported 在 RPM (revolutions 每 minute). 一个 alarm
是 triggered 若 the rotation speed 具有 dropped 下文 一个 programmable limit.
一个 不同 alarm 是 triggered 若 the 风扇 speed 是 too low 到 为 measured.

风扇 readings 是 affected 由 一个 programmable clock divider, giving the
readings 更多 range 或 accuracy. 通常, users 具有 到 learn 如何 它 works,
但 此 驱动 implements 动态 clock divider selection, 因此 您 don't
具有 到 care 无 更多.

用于 参考, 此处 是 一个 少量 值 关于 clock dividers:

    =========== =============== =============== ===========
		slowest         accuracy        highest
		measurable      around 3000     accurate
    divider     speed (RPM)     RPM (RPM)       speed (RPM)
    =========== =============== =============== ===========
	 1        1882              18           6928
	 2         941              37           4898
	 4         470              74           3464
	 8         235             150           2449
    =========== =============== =============== ===========

用于 the curious, 此处 是 如何 the 值 上文 曾是 computed:

 - slowest measurable speed: clock/(255*divider)
 - accuracy around 3000 RPM: 3000^2/clock
 - highest accurate speed: sqrt(clock*100)

The clock speed 用于 the PC87360 family 是 480 kHz. I arbitrarily chose 100
RPM 作为 the lowest acceptable accuracy.

作为 mentioned 上文, 您 don't 具有 到 care 关于 此 无 更多.

注意 该 不 全部 RPM 值 可 为 represented, even 当 the best clock
divider 是 selected. 这是 不 仅 true 用于 the measured speeds, 但
也 用于 the programmable low limits, 因此 don't 为 surprised 若 您 try 到
set, say, 风扇1_min 到 2900 和 它 finally reads 2909.


### 风扇 Control


PWM (pulse width modulation) 值 range 来自 0 到 255, 与 0 meaning
该 the 风扇 是 stopped, 和 255 meaning 该 the 风扇 goes 在 full speed.

为 extremely careful 当 changing PWM 值. Low PWM 值, even
non-zero, 可 停止 the 风扇, 其 可 cause irreversible damage 到 您的
硬件 若 温度 increases too much. 当 changing PWM 值, go
step 由 step 和 keep 一个 eye 在 temperatures.

One 用户 reported problems 与 PWM. Changing PWM 值 将会 break 风扇
speed readings. 无 explanation nor fix 可以 为 found.


### 温度 Monitoring


Temperatures 是 reported 在 degrees Celsius. 每个 温度 measured 具有
associated low, high 和 overtemperature limits, 每个 的 其 triggers 一个
alarm 当 crossed.

The 第一 two 温度 channels 是 外部. The third one (PC87366
仅) 是 内部.

The PC87366 具有 three 额外 温度 channels, 基于
thermistors (相对于 thermal diodes 用于 the 第一 three 温度
channels). 用于 technical reasons, 这些 channels 是 held 由 the VLM
(电压 level 监视器) logical 设备, 不 the TMS (温度
measurement) one. 作为 一个 consequence, 这些 temperatures 是 exported 作为
voltages, 和 converted 进入 temperatures 在 user-space.

注意 该 这些 three 额外 channels share 它们的 pins 与 the
外部 thermal diode channels, 因此 您 (physically) 可't 使用 them 全部 在
the 相同 time. 尽管 它 应当 为 可能 到 mix the two 传感器 types,
the documents 来自 National Semiconductor suggest 该 motherboard
manufacturers 应当 choose one 类型 和 stick 到 它. 因此 您 将 更多
likely 具有 任一个 channels 1 到 3 (thermal diodes) 或 3 到 6 (内部
thermal diode, 和 thermistors).


### 电压 Monitoring


Voltages 是 reported relatively 到 一个 参考 电压, 任一个 内部 或
外部. 一些 的 them (在7:Vsb, 在8:Vdd 和 在10:AVdd) 是 divided 由 two
internally, 您 将 具有 到 compensate 在 传感器.conf. Others (在0 到 在6)
是 likely 到 为 divided externally. The meaning 的 每个 的 这些 inputs 作为
well 作为 the 值 的 the resistors 使用 用于 division 是 left 到 the
motherboard manufacturers, 因此 您 将 具有 到 document yourself 和 edit
传感器.conf accordingly. National Semiconductor 具有 一个 document 与
recommended resistor 值 用于 一些 voltages, 但 此 仍然 leaves much
room 用于 每 motherboard specificities, unfortunately. Even worse,
motherboard manufacturers don't seem 到 care 关于 National Semiconductor's
recommendations.

每个 电压 measured 具有 associated low 和 high limits, 每个 的 其
triggers 一个 alarm 当 crossed.

当 可用, VID inputs 是 使用 到 提供 the nominal CPU 核心 电压.
The 驱动 将 默认 到 VRM 9.0, 但 此 可 为 changed 来自 user-space.
The chipsets 可 handle two sets 的 VID inputs (在 dual-CPU 系统), 但
the 驱动 将 仅 export one 用于 现在. 此 可 change 稍后 若 存在
一个 需要.


### 通用 Remarks


若 一个 alarm triggers, 它 将 remain triggered 直到 the 硬件 注册
是 读取 至少 一旦. 此 means 该 the cause 用于 the alarm 可 已经
具有 disappeared! 注意 该 全部 硬件 寄存器 是 读取 whenever 任何
数据 是 读取 (除非 它是 少于 2 seconds since the 最后 更新, 在
其 case cached 值 是 returned 改为). 作为 一个 consequence, 当
一个 once-only alarm triggers, 它 可 take 2 seconds 用于 它 到 显示, 和 2
更多 seconds 用于 它 到 disappear.

Monitoring 的 在9 isn't 已启用 在 lower 初始化 levels (<3) 因为 该
channel measures the 电池 电压 (Vbat). 它是 一个 known fact 该
repeatedly sampling the 电池 电压 reduces 其 lifetime. National
Semiconductor smartly designed 它们的 chipset 因此 该 在9 是 sampled 仅
一旦 every 1024 sampling cycles (即 every 34 minutes 在 the 默认
sampling rate), 因此 the effect 是 attenuated, 但 仍然 present.


### Limitations


The datasheets suggests 该 一些 值 (风扇 mins, 风扇 dividers)
shouldn't 为 changed 一旦 the monitoring 具有 started, 但 我们 ignore 该
recommendation. 我们'll reconsider 若 它 actually causes trouble.
