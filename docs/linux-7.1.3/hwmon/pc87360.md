## 内核 驱动 pc87360


鍙楁敮鎸?chips:

  - National Semiconductor PC87360, PC87363, PC87364, PC87365 鍜?PC87366

    Prefixes: 'pc87360', 'pc87363', 'pc87364', 'pc87365', 'pc87366'

    地址 scanned: none, 地址 读取 来自 Super I/O 配置 space

    Datasheets: longer 可用

Authors: Jean Delvare <jdelvare@suse.de>

Thanks Sandeep Mehta, Tonko de Rooy Daniel Ceregatti 用于 testing.

Thanks Rudolf Marek 用于 helping me investigate conversion issues.


### 模块 参数


- 初始int
    芯片 初始level:

    - 0: None
    - **1**: Forcibly 启用 内部 电压 温度 channels,
      except 鍦。
    - 2: Forcibly 启用 全部 电压 温度 channels, except 
    - 3: Forcibly 启用 全部 电压 温度 channels, including 

注意 参数 具有 effect 用于 the PC87360, PC87363 PC87364
chips.

注意 用于 the PC87366, 初始levels 2 3 don't 启用
全部 温度 channels, 因为 一them share pins 每个 其他,
因此 它们 t 使用 同时.


### Description


The National Semiconductor PC87360 Super I/O 芯片 包含 monitoring 
PWM control circuitry 用于 two fans. The PC87363 芯片 similar, the
PC87364 芯片 具有 monitoring PWM control 用于 一third 风扇.

The National Semiconductor PC87365 鍜?PC87366 Super I/O chips 鏄?complete
硬件 monitoring chipsets, controlling monitoring three fans,
monitoring eleven 电压 inputs two (PC87365) up four
(PC87366) temperatures.

  =========== ======= ======= ======= ======= =====
  芯片        #vin    #风扇    #pwm    #temp   devid
  =========== ======= ======= ======= ======= =====
  PC87360     -       2       2       -       0xE1
  PC87363     -       2       2       -       0xE8
  PC87364     -       3       3       -       0xE4
  PC87365     11      3       3       2       0xE5
  PC87366     11      3       3       3-4     0xE9
  =========== ======= ======= ======= ======= =====

The 驱动 assumes 多于 one 芯片 present, one the
标准 Super I/O 地址 使用 (0x2E/0x2F 0x4E/0x4F)

### 风扇 Monitoring


风扇 rotation speeds reported RPM (revolutions minute). 一alarm
triggered the rotation speed 具有 dropped 下文 一programmable limit.
一不同 alarm triggered the 风扇 speed too low measured.

风扇 readings affected 一programmable clock divider, giving the
readings 更多 range accuracy. 通常, users 具有 learn 如何 works,
驱动 implements 动clock divider selection, 因此 don't
具有 care 更多.

用于 参 此处 一少量 关于 clock dividers:

    =========== =============== =============== ===========
		slowest         accuracy        highest
		measurable      around 3000     accurate
    divider     speed (RPM)     RPM (RPM)       speed (RPM)
    =========== =============== =============== ===========
	 1        1882              18           6928
	 2         941              37           4898
	 4         470              74           3464
	 8         235             150           2449
    =========== =============== =============== ===========

用于 the curious, 此处 如何 the 上文 曾是 computed:

 - slowest measurable speed: clock/(255*divider)
 - accuracy around 3000 RPM: 3000^2/clock
 - highest accurate speed: sqrt(clock*100)

The clock speed 用于 the PC87360 family 480 kHz. I arbitrarily chose 100
RPM 作为 the lowest acceptable accuracy.

作为 mentioned 上文, don't 具有 care 关于 更多.

注意 全部 RPM represented, even the best clock
divider selected. 这是 true 用于 the measured speeds, 
用于 the programmable low limits, 因此 don't surprised try 
set, say, 风扇1_min 2900 finally reads 2909.


### 风扇 Control


PWM (pulse width modulation) range 来自 0 255, 0 meaning
the 风扇 stopped, 255 meaning the 风扇 goes full speed.

涓?extremely careful 褰?changing PWM 鍊? Low PWM 鍊? even
non-zero, 停止 the 风扇, cause irreversible damage 您的
硬件 温度 increases too much. changing PWM  go
step step keep 一eye temperatures.

One 用户 reported problems PWM. Changing PWM 将会 break 风扇
speed readings. explanation nor fix 可以 found.


### 温度 Monitoring


Temperatures reported degrees Celsius. 每个 温度 measured 具有
associated low, high overtemperature limits, 每个 triggers 一
alarm 褰?crossed.

The 第一 two 温度 channels 外部. The third one (PC87366
 内部.

The PC87366 具有 three 额外 温度 channels, 基于
thermistors (相对thermal diodes 用于 the 第一 three 温度
channels). 用于 technical reasons, 这些 channels held the VLM
(电压 level 监视 logical 设备, the TMS (温度
measurement) one. 作为 一consequence, 这些 temperatures exported 作为
voltages, converted 进入 temperatures user-space.

注意 这些 three 额外 channels share 它们pins the
外部 thermal diode channels, 因此 (physically) t 使用 them 全部 
the 相同 time. 尽管 应当 可能 mix the two 传感types,
the documents 来自 National Semiconductor suggest motherboard
manufacturers 应当 choose one 类型 stick  因此 更多
likely 具有 任一channels 1 3 (thermal diodes) 3 6 (内部
thermal diode, 鍜?thermistors).


### 电压 Monitoring


Voltages reported relatively 一参电压, 任一内部 
外部. 一them (:Vsb, :Vdd 0:AVdd) divided two
internally, 具有 compensate 传感conf. Others ( )
likely divided externally. The meaning 每个 这些 inputs 作为
well 作为 the the resistors 使用 用于 division left the
motherboard manufacturers, 因此 具有 document yourself edit
传感conf accordingly. National Semiconductor 具有 一document 
recommended resistor 用于 一voltages, 仍然 leaves much
room 用于 motherboard specificities, unfortunately. Even worse,
motherboard manufacturers don't seem care 关于 National Semiconductor's
recommendations.

每个 电压 measured 具有 associated low high limits, 每个 
triggers 一alarm crossed.

可用, VID inputs 使用 提供 the nominal CPU 核心 电压.
The 驱动 默认 VRM 9.0, changed 来自 user-space.
The chipsets handle two sets VID inputs (dual-CPU 系统), 
the 驱动 export one 用于 现在. change 稍后 存在
一需


### 通用 Remarks


一alarm triggers, remain triggered 直到 the 硬件 注册
读取 至少 一 means the cause 用于 the alarm 已经
具有 disappeared! 注意 全部 硬件 寄存读取 whenever 任何
数据 读取 (除非 它是 少于 2 seconds since the 最更新, 
case cached returned 改为). 作为 一consequence, 
一once-only alarm triggers, take 2 seconds 用于 显示, 2
更多 seconds 用于 disappear.

Monitoring  isn't 已启lower 初始levels (<3) 因为 
channel measures the 电池 电压 (Vbat). 它是 一known fact 
repeatedly sampling the 电池 电压 reduces lifetime. National
Semiconductor smartly designed 它们chipset 因此  sampled 
一every 1024 sampling cycles (every 34 minutes the 默认
sampling rate), 因此 the effect attenuated, 仍然 present.


### Limitations


The datasheets suggests 一(风扇 mins, 风扇 dividers)
shouldn't changed 一the monitoring 具有 started, 我们 ignore 
recommendation. 我们'll reconsider actually causes trouble.
