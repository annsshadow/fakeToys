## sm501fb


配置：

你可以向 sm501 传递以下内核命令行选项

```

	sm501fb.bpp=	SM501 Display driver:
			Specify bits-per-pixel if not specified by 'mode'

	sm501fb.mode=	SM501 Display driver:
			Specify resolution as
			"<xres>x<yres>[-<bpp>][@<refresh>]"

```
