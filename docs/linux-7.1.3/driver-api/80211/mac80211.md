## mac80211 子系统（基础

在实mac80211 驱动时，你应该阅读并理解本书这一部分所包含的信息。在某些
章节中标注了高级用法，若不需要可以跳过
本书这一部分仅涵盖工作站（station）与监视（monitor）模式的功能，实现其模式所需的额外信息在本书第二部分中涵盖
## 基本硬件处理


TBD

本章应包含如何分配并注册一hw 结构体到 mac80211 的信息
由于在注hw 结构体之前需要先分配 rates/modes，本章还应包含设rate/mode 结构体的信息
此外，这里应有关于回调与通用编程模型的一些讨论，包括将被大量引用ieee80211_ops 的定义
最后，应结合对本书其他部分的引用，对硬件能力进行讨论
   :functions:
	ieee80211_hw
	ieee80211_hw_flags
	SET_IEEE80211_DEV
	SET_IEEE80211_PERM_ADDR
	ieee80211_ops
	ieee80211_alloc_hw
	ieee80211_register_hw
	ieee80211_unregister_hw
	ieee80211_free_hw

## PHY 配置


TBD

本章应描PHY 处理，包start/stop 回调及所使用的各种结构体
   :functions:
	ieee80211_conf
	ieee80211_conf_flags

## 虚拟接口


TBD

本章应描述与驱动相关的虚拟接口基础（VLAN、MGMT 等则不在此列）。它应解add_iface/remove_iface 回调以及接口配置回调的使用
AP 模式相关的内容应在此讨论
与支持多个接口相关的内容应放在适当的章节中，不过这里应有一BIG FAT 提示并建议一开始只允许单个 STA 模式接口
   :functions: ieee80211_vif

## 接收与发送处

### 应在此处的内

TBD

这应描述 mac80211/驱动中的接收与发送路径，以及发送状态处理
### 帧格

   :doc: Frame format

### 数据包对

   :doc: Packet alignment

### 从中断中调用 mac80211


   :doc: Calling mac80211 from interrupts

### 函数/定义


   :functions:
	ieee80211_rx_status
	mac80211_rx_encoding_flags
	mac80211_rx_flags
	mac80211_tx_info_flags
	mac80211_tx_control_flags
	mac80211_rate_control_flags
	ieee80211_tx_rate
	ieee80211_tx_info
	ieee80211_tx_info_clear_status
	ieee80211_rx
	ieee80211_rx_ni
	ieee80211_rx_irqsafe
	ieee80211_tx_status_skb
	ieee80211_tx_status_ni
	ieee80211_tx_status_irqsafe
	ieee80211_rts_get
	ieee80211_rts_duration
	ieee80211_ctstoself_get
	ieee80211_ctstoself_duration
	ieee80211_generic_frame_duration
	ieee80211_wake_queue
	ieee80211_stop_queue
	ieee80211_wake_queues
	ieee80211_stop_queues
	ieee80211_queue_stopped

## 甯ц繃婊。

   :doc: Frame filtering

   :functions: ieee80211_filter_flags

## mac80211 工作队列


   :doc: mac80211 workqueue

   :functions:
	ieee80211_queue_work
	ieee80211_queue_delayed_work
