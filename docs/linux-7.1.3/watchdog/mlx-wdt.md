## Mellanox 鐪嬮棬鐙楅┍鍔。

## 面向基于 x86 的系统交换机


该驱动为各种 Mellanox 以太网与 Infiniband 交换机系统提供看门狗功能
Mellanox 看门狗设备实现在一个可编程逻辑设备中
2 HW 看门狗实现
Type 1  实际 HW 超时可以定义2 的幂毫秒数  例如 20 秒超时会向上取整32768 毫秒  最大超时周期为 32 秒（32768 毫秒）  不支持获取剩余时间（time-left
Type 2  实际 HW 超时以秒定义，且与用户定义的超时相同  最大超时是 255 秒  支持获取剩余时间（time-left）
Type 3  Type 2 相同，但最大超时周期更长  最大超时是 65535 秒
Type 1 HW 看门狗实现存在于旧系统中，所有新系统都有 Type 2 HW 看门狗两种 HW 实现也有不同的寄存器映射
Type 3 HW 看门狗实现可存在于所有带有新可编程逻辑设备Mellanox 系统上它通过 WD capability 位来区分。旧系统仍然只有一个主看门狗
Mellanox 系统可以有两个看门狗：主看门狗和辅助看门狗主看门狗和辅助看门狗设备可以在同一系统上同时启用在看门狗中可以定义若干动作：系统复位、以全速启动风扇以及递增寄存器计数器后两个动作在没有系统复位的情况下执行没有复位的动作提供给可选的辅助看门狗设备看门狗可以在 probe 期间启动，在这种情况下，在用户空间应用程序打开看门狗设备之前，它将看门狗核心（watchdog core）定ping看门狗可以以 nowayout 方式初始化，即一旦启动就无法停止
mlx-wdt 驱动支持两种 HW 看门狗实现
看门狗驱动由通用mlx_platform 驱动探测。Mlx_platform 驱动Mellanox 看门狗设备提供一适当的寄存器、标识名称（mlx-wdt-main mlx-wdt-aux）、初始超时、到期时执行的动作以及配标志。看门狗配置标志：nowayout start_at_boot，hw 看门狗版本——type1 type2驱动在初始化期间检查上一次系统复位是否由看门狗完成。如果是，它会就这一事件发出通知
HW 寄存器的访问通过一个通用regmap 接口进行。可编程逻辑设备寄存器的字节顺序为小（little-endian）