## Python 单元测试


检查 Python 模块的一致性可能比较复杂。有时，定义一组单元测试来帮助检查它们会很有用。

虽然实际的测试实现取决于具体用例，但 Python 已经提供了一种使用 `import unittest` 添加单元测试的标准方式。

使用此类需要建立一个测试套件。此外，默认格式有些笨拙。为了改进它并提供一种更统一的方式来报告错误，这里定义了一些 unittest 类和函数。


## Unittest 辅助模块


   :members:
   :show-inheritance:
   :undoc-members:
