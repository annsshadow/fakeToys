# AI 训练数据增强工具 - 需求文档

## 概述

基于现有的 `enhanceTXT.py` 脚本，构建一个完整的 AI 训练数据增强工具，支持多模型、多格式、可视化管理。

## 当前状态

- **现有代码**: `ai/enhanceTXT.py` - 基于百度 ERNIE 的数据增强脚本
- **数据格式**: JSON 数组，每条记录包含 `input`、`instruction`、`output` 字段
- **现有能力**: 40 线程并发、断点续传、同义词替换（部分实现）

## 功能需求

### 1. 多模型后端支持

**目标**: 支持多种 LLM 后端，统一调用接口

**需求**:
- 支持模型:
  - 百度 ERNIE（已有）
  - OpenAI GPT-3.5/GPT-4
  - 本地模型（Ollama/vLLM/LocalAI）
- 统一调用接口，配置文件切换模型
- 支持设置不同模型的参数（temperature、top_p 等）
- 错误重试机制（指数退避）

**配置示例**:
```yaml
models:
  ernie:
    type: baidu
    api_key: ${BAIDU_API_KEY}
    secret_key: ${BAIDU_SECRET_KEY}
    model: ernie-3.5-8k
  openai:
    type: openai
    api_key: ${OPENAI_API_KEY}
    model: gpt-3.5-turbo
  local:
    type: ollama
    base_url: http://localhost:11434
    model: qwen2:7b
```

### 2. 数据质量评分

**目标**: 自动评估生成数据的质量，过滤低质量样本

**需求**:
- 语义相似度评分（instruction 与原始 seed 的相似度）
- 回答质量评分（output 的完整性和相关性）
- 多样性评分（避免生成高度相似的问题）
- 可配置的质量阈值，低于阈值的样本自动过滤
- 输出质量报告（评分分布、过滤统计）

**评分算法**:
```python
def calculate_quality_score(original, generated, output):
    # 1. 语义相似度 (0.3权重)
    semantic_sim = sentence_transformer.similarity(original, generated)
    
    # 2. 回答相关性 (0.4权重)
    relevance = cross_encoder.predict(generated, output)
    
    # 3. 多样性 (0.3权重)
    diversity = 1 - max(existing_similarities)
    
    return 0.3 * semantic_sim + 0.4 * relevance + 0.3 * diversity
```

### 3. 智能去重

**目标**: 检测并合并近义重复问题

**需求**:
- 基于向量相似度的近义检测
- 可配置的相似度阈值（默认 0.9）
- 合并策略：保留质量最高的版本
- 输出去重报告（重复数量、合并详情）

### 4. 多格式导出

**目标**: 支持主流微调框架的数据格式

**需求**:
- 支持格式:
  - **JSONL**（当前格式）
  - **Llama-Factory** 格式
  - **Alpaca** 格式（Stanford）
  - **ShareGPT** 格式（多轮对话）
  - **ChatML** 格式
- 命令行参数指定输出格式
- 格式转换预览

**格式示例**:
```json
// Llama-Factory
{
  "instruction": "用户提问",
  "input": "",
  "output": "客服回答",
  "system": "你是安居乐寓的智能客服"
}

// ShareGPT
{
  "conversations": [
    {"from": "human", "value": "用户提问"},
    {"from": "gpt", "value": "客服回答"}
  ]
}
```

### 5. 对话上下文增强

**目标**: 支持多轮对话数据生成

**需求**:
- 从单轮 Q&A 生成多轮对话上下文
- 支持配置对话轮数（2-5轮）
- 保持对话连贯性
- 添加 `history` 字段存储历史对话

**数据结构**:
```json
{
  "instruction": "后续问题",
  "input": "",
  "output": "客服回答",
  "history": [
    {"role": "user", "content": "第一个问题"},
    {"role": "assistant", "content": "第一个回答"},
    {"role": "user", "content": "第二个问题"},
    {"role": "assistant", "content": "第二个回答"}
  ]
}
```

### 6. Web UI 管理界面

**目标**: 可视化管理数据增强流程

**需求**:
- **数据管理**:
  - 数据浏览（分页、搜索、筛选）
  - 数据编辑（单条修改）
  - 数据导出（选择格式、批量导出）
- **增强监控**:
  - 实时进度显示
  - 质量评分分布图
  - 错误日志查看
- **配置管理**:
  - 模型配置
  - 参数调整
  - API 密钥管理
- **数据可视化**:
  - 词云图
  - 长度分布图
  - 话题聚类图

**技术栈**:
- 后端: FastAPI
- 前端: React + Ant Design
- 图表: ECharts

### 7. 断点续传优化

**目标**: 优化现有的断点续传机制

**需求**:
- 自动保存进度（每 N 条）
- 进度文件包含：已完成索引、失败记录、质量评分
- 支持手动恢复
- 失败重试机制（可配置重试次数）
- 进度可视化（完成百分比、预计剩余时间）

### 8. 数据版本管理

**目标**: 管理数据集的版本历史

**需求**:
- **快照功能**:
  - 保存数据集快照
  - 添加版本标签和描述
  - 自动快照（增强完成后）
- **版本对比**:
  - Diff 对比（新增、删除、修改）
  - 统计信息（数量变化、质量变化）
- **回滚功能**:
  - 回滚到指定版本
  - 部分回滚（只回滚某些字段）
- **历史记录**:
  - 操作日志
  - 版本时间线

**存储结构**:
```
data/
├── versions/
│   ├── v1.0_20240515/
│   │   ├── data.json
│   │   ├── metadata.json
│   │   └── report.json
│   ├── v1.1_20240516/
│   │   └── ...
│   └── current -> v1.1_20240516
```

### 9. 主动学习选样

**目标**: 识别需要更多增强的问答类型

**需求**:
- 分析现有数据的覆盖情况
- 识别覆盖不足的话题/类型
- 优先增强这些 seed
- 输出选样报告（推荐增强的 seed 列表）

**分析维度**:
- 话题分布
- 问题类型分布（疑问词、句式）
- 长度分布
- 答案复杂度

### 10. 自动领域扩展

**目标**: 基于 seed 自动生成新话题分类

**需求**:
- 分析 seed 的话题结构
- 自动扩展相关话题
- 生成新话题的 seed 建议
- 人工确认后批量生成

**扩展策略**:
- 同类扩展（相似话题）
- 关联扩展（相关话题）
- 场景扩展（使用场景）

### 11. 微调效果追踪

**目标**: 验证增强数据对下游任务的提升

**需求**:
- 对接微调流程
- 对比实验（有/无增强数据）
- 效果指标追踪（loss、accuracy、perplexity）
- 生成效果报告

**追踪指标**:
```json
{
  "experiment_id": "exp_20240515",
  "dataset_version": "v1.1",
  "metrics": {
    "train_loss": [0.5, 0.3, 0.2],
    "eval_accuracy": [0.6, 0.7, 0.75],
    "perplexity": [12.5, 8.3, 6.2]
  }
}
```

### 12. 数据分布可视化

**目标**: 可视化数据集的统计信息

**需求**:
- **词云图**: 高频词汇展示
- **长度分布**: 问题/回答长度直方图
- **话题聚类**: t-SNE/UMAP 降维可视化
- **时间线**: 数据增强进度
- **质量分布**: 评分分布直方图

## 非功能需求

### 性能
- 支持 10 万级数据处理
- Web UI 响应时间 < 200ms
- 并发处理支持（多线程/多进程）

### 可用性
- 命令行界面（CLI）
- Web 界面
- 两种部署模式（本地/服务器）

### 可扩展性
- 插件化模型后端
- 可扩展的导出格式
- 配置驱动的行为

### 可靠性
- 数据备份机制
- 错误恢复
- 日志记录

## 技术约束

- **语言**: Python 3.10+
- **依赖**: 尽量使用现有依赖（nlpaug、nlpcda）
- **存储**: JSON 文件（小规模）或 SQLite（大规模）
- **部署**: Docker 容器化

## 优先级

所有功能同等重要，同时实现。

## 成功标准

1. 所有 12 个功能可正常使用
2. 支持至少 3 种模型后端
3. 支持至少 4 种导出格式
4. Web UI 功能完整
5. 数据处理性能可接受
6. 有完整的使用文档
