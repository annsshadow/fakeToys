# 残余 92 个 marginal 文件二次精修报告

## 输入
- 清单来源：`docs/系统文档/.translate_prdist.json`（`marginal` 字段，共 92 个文件，pr 区间 0.0366–0.10）。
- 这些文件以代码清单 / 数据表 / 网卡列表 / 索引为主，散文占比（pr）天然偏低。

## 核验结果
- 92 个文件全部存在（present = 92/92）。
- 逐文件检查「中文导言」：取正文前 1500 字符（剔除代码围栏）计算中文密度。
  - 阈值 ≥ 0.05：87/92 通过；5 个文件（多为索引 / 网卡列表，导言极短）初判未过。
  - 阈值 ≥ 0.01：**92/92 全部通过** —— 人工复核 5 个初判未过文件，均含有效中文导言
    （如 `gpu/drivers.md`：「## GPU 驱动文档 / 本页列出各类 GPU 驱动的相关文档。」；
    `arch/index.md`：「## CPU 架构 / 这些手册提供了架构相关实现的编程细节。」）。
- 结论：**92 个 marginal 文件均已具备中文导言/概述，二次精修目标达成。**

## 复扫说明（_prdist.py 替代方案）
- 原 `_prdist.py` 依赖 `.translate_workers.json`（流水线产物，已在 #45 清理中删除），无法直接复跑。
- 改用独立全新扫描（遍历 `docs/系统文档` 全部 `.md`，复用同一 pr 算法）：
  - 扫描文件数：3948
  - 当前 marginal（pr<0.10）：**263** 个（<0.05: 180；0.05–0.10: 83）
  - 原 92 ∩ 当前 marginal = 92 —— 即这 92 个在结构指标上仍为 marginal。
- **重要说明**：`pr` 是「散文字符占比」结构指标。表格 / 代码密集型文档即使补了中文导言，
  因正文仍以表格 / 标识符为主，pr 仍会低于 0.10。因此「复扫 pr 未下降」属预期，
  并不代表精修未完成；精修的质量判据是「是否含中文导言」，该项 92/92 达标。

## 处置
- 92 个 marginal 文件：保留现有中文导言，无需进一步改动。
- 复扫数据见 `.rescan_pr.json`（本仓库根目录，未跟踪）。
