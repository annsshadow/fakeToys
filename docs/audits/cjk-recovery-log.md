# CJK 双重编码恢复日志（plan002 U11.1）

- 行尾统一为 LF、保留 BOM（与 HEAD blob 一致；core.autocrlf=true 提交时亦归一为 LF）。
- 机制：UTF-8 原文被按 Windows cp936（ANSI）误读后存回 UTF-8；逐行用 Windows MultiByteToWideChar(936) 导出的精确映射表反向还原（覆盖 €/PUA 特殊映射，gb18030 兜底），并以“正向再损坏 == 原行”证明后才采纳；原损坏中字节已丢失的位置（字面 `?`）仅能有损恢复。

## docs/linux-7.1.3/admin-guide/bug-hunting.md

- 总行数：285（含空行；非空 217）
- 干净行：220；恢复行：13（另有有损恢复 51 行）；恢复失败行：1
- 标点替换：44 处（句号 37，逗号 7）；未解决 `?`：36 处
- 自验乱码签名行率：64/217（29.5%）→ 2/217（0.9%）
- 注意：51 行因原文件中存在字节丢失（字面 `?`），仅能部分还原

### 恢复失败行（保留原文，需人工对照上游）
- L237: `linux-kernel@vger.kernel.org銆?`

### 未解决 `?`（不满足替换规则，保留原样）
- L1: `## 缺陷追踪（Bug Hunting?`
- L50: `尽管它是一?**Oops** 或某种其他类型的栈跟踪，通常仍需要找到出错的那一行才能定位和处理缺陷。在本章中，我们用“Oops”来泛指所有需要分析的各类栈跟踪?`
- L55: `已被污染或正在加?卸载的模块会以?...)”标记，其中污染标志?`Documentation/admin-guide/tainted-kernels.rst` `
- L59: `通常 Oops 文本?`klogd` 从内核缓冲区读出，并交给 `syslogd` 写入一?syslog 文件，通常?`/var/log/messages`（取`
- L61: `有时 `klogd` 会死掉，这时你可以运?`dmesg > file` 从内核缓冲区读出数据并保存。或者你可以`
- L62: ``cat /proc/kmsg > file`，不过你必须中断传输来停止它，因?`kmsg` 是一个“永不结束的文件”?`
- L64: `(1) 从屏幕手写抄录文本，并在机器重启后键入。这很麻烦，但如果你没有为崩溃做准备，这是唯一的办法。或者，你可以用数码相机拍下屏幕——虽然不好，但总比没有强。如`
- L66: `(2) 用串口控制台引导（参?`Documentation/admin-guide/serial-console.rst` <serial_console>），`
- L67: `(3) 使用 Kdump（参?`Documentation/admin-guide/kdump/kdump.rst`），通过 `Documentation/ad`
- L71: `如果你能将缺陷的位置指向内核源文件，报告缺陷的效果最好。有两种方法可以做到这一点。通常，使?`gdb` 更简单，但内核应预先使用调试信息编译?`
- L75: `GNU 调试器（`gdb`）是确定 OOPS ?`vmlinux` 文件中精确文件与行号的最佳方式?`
- L76: `在带?`CONFIG_DEBUG_INFO` 编译的内核上，`gdb` 的使用效果最佳?`
- L82: `在带?`CONFIG_DEBUG_INFO` 编译的内核上，你只需复制`
- L96: `如果你没有启?`CONFIG_DEBUG_INFO`，你可以使用函数`
- L217: `例如，如果你?gspca ?sonixj.c 文件中发现了一个缺陷，你可以得?`
- L230: `- 最后修改过源代码的开发者（如果这是?git 树内完成的）。在上面例子中是 Tejun ?Bhaktipriya（在这个具体情况下，他们都没有真正参与该文件的`
- L235: `如果列表中末尾包含缺陷报?URI，请优先使用它们而不是电子邮件。否则，请将缺陷报告给用于该代码开发的邮件列表（linux-media ML），并抄送给驱动维护者`
- L238: `感谢你为?Linux 尽可能稳定所提供的帮助?`
- L251: `为了帮助 Linus 和其他内核开发者，`klogd` 中加入了大量用于处保护故障的支持。为了获得对地址解析?完整支持，至少应使用 `sysklogd` 软件?`
- L254: ``klogd` 执行两种类型的地址解析。第一种是静态转换，第二种是动态转换。静态转换使?System.map 文件?为了进行静态转换，`klogd` 守护进程必`
- L255: `搜索映射文件的信息，请参?klogd 手册页?`
- L257: `内核支持一些系统调用，允许程序确定加载了哪些模块以及它们在内存中的位置。通过对这些系统调用的使用?`klogd` 守护进程构建了一张符号表，可用于调试可加载内核`
- L261: `选项可供使用，它们允?klogd 向当前正在执行的守护进程发信号，表示应该刷新符号信息。有关更多信息，请参?`klogd` 手册页?`
- L262: `sysklogd 发行版中包含一个补丁，它修?`modules-2.0.0` 软件包，使其在每次加载或卸载模块时自动向 klogd`

## docs/linux-7.1.3/dev-tools/kmsan.md

- 总行数：343（含空行；非空 206）
- 干净行：258；恢复行：17（另有有损恢复 68 行）；恢复失败行：0
- 标点替换：51 处（句号 51，逗号 0）；未解决 `?`：91 处
- 自验乱码签名行率：79/206（38.3%）→ 0/206（0.0%）
- 注意：68 行因原文件中存在字节丢失（字面 `?`），仅能部分还原

### 未解决 `?`（不满足替换规则，保留原样）
- L15: `为了构建带有 KMSAN 的内核，你需要一个较新的 Clang?4.0.6+）。有关如何构?Clang 的说明，请参?`LLVM documentation`_`
- L54: `该报告表明，局部变?`uninit` ?`do_uninit_local_array()` 中以未初始化状态创建。第三条栈回溯对应该变量被创建的位置?`
- L56: `第一条栈回溯展示了未初始化值在何处被使用（?`test_uninit_kmsan_check_memory()` 中）。该工具还显示了局部变量中哪些字节未被初始`
- L58: `KMSAN 在以下情况下会报告对未初始化?`v` 的使用：`
- L60: `- 在条件判断中，例?`if (v) { ... }`?`
- L61: `- 在索引或指针解引用中，例?`array[v]` ?`*v`?`
- L62: `- 当它被复制到用户空间或硬件时，例?`copy_to_user(..., &v, ...)`?`
- L71: `可以使用函数属?`__no_kmsan_checks` 标记某个函数。这样做会使 KMSAN 忽略该函数中的未初始化值，并将其输出标记为已初始化。结果是，用户将`
- L73: `KMSAN 支持的另一个函数属性是 `__no_sanitize_memory`。将该属性应用于某个函数会使 KMSAN 不对其插桩，如果我们不希望编译器干扰某`
- L77: `作为经验法则，应避免显式使用 `__no_sanitize_memory`?`
- L88: `?Makefile 中。可以将其视为对该文件或目录中的每个函数应用 `__no_sanitize_memory`。大多数用户不需?`KMSAN_SANITIZE`
- L90: `KMSAN 检查也可以使用 `kmsan_disable_current()` ?`kmsan_enable_current()` 调用为当前任务临时禁用。每?`
- L95: `为了?KMSAN 正常工作，内核必须使?Clang 构建，迄今为?Clang 是唯一支持 KMSAN 的编译器。内核插桩过程基于用户空间的 `MemorySan`
- L97: `目前运行时库仅支?x86_64?`
- L105: `KMSAN 为内核内存的每一个字节关联一个元数据字节（也称为 shadow 字节）。如果内核内存字节中的对应位未初始化，则 shadow 字节中的相应位被置位。`
- L107: `当一个新的变量在栈上分配时，默认情况下会被编译器插入的插桩代码污染（除非它是一个立即被初始化的栈变量）。任何没有使?`__GFP_ZERO` 的新堆分配也会被污`
- L109: `编译器插桩还会跟?shadow 值随代码使用的传播过程。在需要时，插桩代码会调用 `mm/kmsan/` 中的运行时库来持久化 shadow 值?`
- L111: `基本类型或复合类型的 shadow 值是一个与其等长的字节数组。当向内存写入常量值时，该内存被去污染。当从内存读取值时，也会获取其 shadow 内存，并将其传`
- L120: `在这种情况下，`a` ?shadow ?`0`，`b` ?shadow ?`0xffffffff`，`c` ?shadow ?`0xffffff00`。这意味着`
- L125: `内核内存的每四个字节也映射了一个所谓的 origin（来源）。该 origin 描述了在程序执行过程中创建该未初始化值的位置。每?origin 要么关联到完整的`
- L127: `当未初始化变量在栈或堆上分配时，会创建一个新?origin 值，并用该值填充该变量?origin。当从内存读取值时，也会读取其 origin，并?shadow `
- L136: `在这种情况下，`b` ?origin 在函数入口时生成，并在加法结果写入内存之前存储到 `c` ?origin 中?`
- L138: `如果多个变量存储在同一个四字节块中，它们可能会共享相同?origin 地址。在这种情况下，对任意一个变量的每次写入都会更新所有这些变量的 origin。在这种情`
- L153: `如果 `a` 已初始化?`b` 未初始化，则结果?shadow 将是 `0xffff0000`，结果的 origin 将是 `b` ?origin。`ret.s`
- L155: `如果两个函数参数都未初始化，则只保留第二个参数的 origin?`
- L160: `为了简化调试，KMSAN 会为每次将未初始化值存储到内存创建新的 origin。新?origin 同时引用其创建栈以及该值先前拥有的 origin。这可能导致内`
- L186: `编译器确保对于每一个被加载的值，?shadow ?origin 值都从内存中读取。当值被存储到内存时，其 shadow ?origin 也会使用元数据指针一并存`
- L191: `使用特殊函数为局部变量创建新?origin 值：`
- L218: `该结构被 KMSAN 用来在被插桩的函数之间传递参数的 shadow ?origin（除非参数被 `CONFIG_KMSAN_CHECK_PARAM_RETVA`
- L223: `Clang ?MemorySanitizer 插桩有一个选项 `-fsanitize-memory-param-retval`，它使编译器检查按值传递的函数参数`
- L225: `该选项?`CONFIG_KMSAN_CHECK_PARAM_RETVAL` 控制，默认启用，以使 KMSAN 能够更早地报告未初始化值。更多细节请参?`LKML`
- L232: `编译器会将对 `memcpy()`/`memmove()`/`memset()` 的调用替换为以下函数。当数据结构被初始化或复制时也会调用这些函数，确?shad`
- L243: `对于每次值的使用，编译器都会发出一?shadow 检查，调用?`
- L268: `代码位于 `mm/kmsan/`?`
- L270: `#### 每个任务?KMSAN 状?`
- L273: `每个 task_struct 都关联一?KMSAN 任务状态，用于保存 KMSAN?`
- L295: `但在内核运行于中断、softirq ?NMI 上下文的情况下：`
- L306: `1. 每个 `struct page` 实例都包含两个指向其 shadow ?origin 的指针：`
- L316: `在启动阶段，内核为每一个可用的内核页分?shadow ?origin 页。这一过程发生得相当晚，此时内核地址空间已经碎片化，因此普通数据页可能会与元数据页任意交`
- L320: `实际上，由同一?`alloc_pages()` 调用返回的连续内存页将具有连续的元数据，而如果这些页属于两次不同的分配，它们的元数据页则可能碎片化?`
- L322: `对于内核数据（`.data`、`.bss` 等）?percpu 内存区域，同样不保证元数据的连续性?`
- L324: `?`__msan_metadata_ptr_for_XXX_YYY()` 命中两个分配之间的边界时?`
- L333: `2. 对于 vmalloc 内存和模块，内存区间、其 shadow ?origin 之间存在直接映射。KMSAN ?vmalloc 区域缩减 3/4，使得只有第`
- L335: `当一组页被映射到连续的虚拟内存空间时，它们的 shadow ?origin 页也会被类似地映射到连续区域?`

## docs/linux-7.1.3/dev-tools/ubsan.md

- 总行数：68（含空行；非空 51）
- 干净行：58；恢复行：3（另有有损恢复 7 行）；恢复失败行：0
- 标点替换：6 处（句号 4，逗号 2）；未解决 `?`：7 处
- 自验乱码签名行率：10/51（19.6%）→ 0/51（0.0%）
- 注意：7 行因原文件中存在字节丢失（字面 `?`），仅能部分还原

### 未解决 `?`（不满足替换规则，保留原样）
- L2: `## 未定义行为消毒剂 ?UBSAN`
- L6: `UBSAN 使用编译期插桩来捕获未定义行为（UB）。编译器在可能引?UB 的操作之前插入执?特定类型检查的代码。如果检查失败（即检测到 UB），会调?__ubs`
- L8: `GCC ?4.9.x [1_] 开始具有该特性（参见 `-fsanitize=undefined` 选项及其子选项）?GCC 5.x 实现了更多检查器 [2_]`
- L67: `架构上（CONFIG_HAVE_EFFICIENT_UNALIGNED_ACCESS=y），它默认关闭。仍然可以在配置?启用它，只是要注意它会产生大?UBSAN`

