# AI 训练数据增强工具依赖

# ============ 核心依赖 ============
requests>=2.28.0,<3.0.0
pyyaml>=6.0,<7.0.0
numpy>=1.21.0,<3.0.0

# ============ 模型相关 ============
# sentence-transformers 6.x 依赖 transformers>=5.0.0；
# transformers 4.x 存在多个已知漏洞（PYSEC-2026-2288/2289/2290/3929 等），
# 5.0+ 才修复，因此这里必须 >=6.0.0。
# 使用的 API（SentenceTransformer() / .encode() / CrossEncoder）跨版本稳定。
sentence-transformers>=6.0.0,<7.0.0
scikit-learn>=1.0.0,<2.0.0

# ============ 可视化 ============
wordcloud>=1.8.0,<2.0.0
matplotlib>=3.5.0,<4.0.0

# ============ Web UI / API ============
fastapi>=0.78.0,<1.0.0
uvicorn>=0.18.0,<1.0.0
# 平台代码使用 pydantic v2 的 model_dump()，必须锁 v2
pydantic>=2.0.0,<3.0.0
python-multipart>=0.0.6

# ============ 旧版脚本 enhanceTXT.py 专用 ============
nlpaug>=1.1.0
nlpcda>=2.1.0

# ============ 多模型后端 (可选) ============
anthropic>=0.25.0
google-generativeai>=0.5.0

# ============ 向量数据库 (可选) ============
faiss-cpu>=1.7.4
chromadb>=0.4.24

# ============ 多模态处理 (可选) ============
Pillow>=10.0.0
soundfile>=0.12.1

# ============ LLM 框架集成 (可选) ============
langchain>=0.1.0
langchain-core>=0.1.0
llama-index-core>=0.10.0

# ============ 测试 ============
# 测试依赖单独维护在 requirements-dev.txt，避免版本定义重复
-r requirements-dev.txt
