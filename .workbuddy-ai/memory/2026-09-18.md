# 2026-09-18

## PR 准备：feat/oa4rust-mobile → main

用户要求为当前分支提 PR。因 `gh` CLI 未登录，改为：完成提交前健康检查 + 生成 PR 描述草稿，待用户登录后由我执行 `gh pr create`。

### 关键发现

- 分支 `feat/oa4rust-mobile` 领先 `origin/main` **383 提交**，32,766 文件变更。
- 变更量主体是**有意的清理**：提交 `1d2c81eb`（2026-09-17）移除 `oa/` 下 27,695 个遗留 O2OA Java 平台文件，由 oa4rust 三端替代。**main 尚未包含此删除**。
- 排除 `oa/` 后净变更：5,067 文件，+761,383 / -702,988。
- 提交类型分布：test 145、docs 81、feat 49、fix 13、refactor 11、chore 7、perf 4。
- 时间跨度：2026-09-14 ~ 2026-09-18。

### 健康检查结论

- ✅ 构建产物正确忽略：`.gitignore:46 oa4rust/target*/`、`.gitignore:24 node_modules/`。仓库仅跟踪 5,929 个文件，无 `.o/.rlib/.rmeta` 误提交。
- ✅ null 字节污染扫描（`git grep -lP '\x00'`）：仅 1 处。
- ⚠️ 唯一问题文件：`ai/docs/FINAL_SUMMARY.md`（1,328 字节，含 572 处 null 字节 + 字节交换损坏）。本分支新增，main 无此文件；历史唯一版本即损坏，无干净版本。属纯文档，不影响编译。**建议合并前删除或重写**。

### 产出

- `D:\WORKSPACE\fakeToys\PR_DESCRIPTION.md` — 完整 PR 标题与正文草稿，含变更构成表、主要工作内容、已知问题、review 建议。
- 建议的 PR 标题：`feat: oa4rust 三端全量替代 O2OA Java 平台`

### 待办

- ✅ 已完成：PR #23 已创建（见下方）。

### 环境备忘

- 远端：`git@github.com:annsshadow/fakeToys.git`（SSH），默认分支 `origin/main`。
- `gh` CLI 的 `auth status` 报未登录，但 `gh auth token` **能正常取出有效 token**。

### ✅ PR 已创建（18:05）

- **PR #23**：https://github.com/annsshadow/fakeToys/pull/23
- 标题：`feat: oa4rust 三端全量替代 O2OA Java 平台`
- base `main` ← head `feat/oa4rust-mobile`，状态 OPEN
- 规模：32,765 文件，+761,612 / -3,374,077
- 推送：`ca56eaf6..07def09f` → `origin/feat/oa4rust-mobile`

---

## ⚠️ 环境陷阱：gh 认证不一致（重要，可复用）

本机 `gh` CLI 存在**认证状态不一致**问题：

| 命令 | 结果 |
|---|---|
| `gh auth status` | ❌ "You are not logged into any GitHub hosts" |
| `gh auth token` | ✅ 返回有效 token `gho_...` |
| `gh api user` | ❌ 报未登录（不加 token 时） |

**根因**：`%APPDATA%/GitHub CLI/hosts.yml` 中 `users.annsshadow` 下**缺少 `oauth_token` 字段**（只有用户名），但 token 实际存在于别处。`gh auth status` 读 hosts.yml 判定未登录，而 `gh auth token` 走的是另一条取值路径。

**有效绕过方式**（已实测成功）：

```bash
export GH_TOKEN=$(gh auth token 2>/dev/null)
gh auth status          # 此时显示 ✓ Logged in as annsshadow (GH_TOKEN)
gh pr create --base main --head <branch> --title "..." --body-file <file>
```

token 权限含 `repo`、`workflow`、`gist`、`read:org`，足够建 PR。

**推送走 SSH 不受影响**：remote 为 `git@github.com:...`，已验证 `ssh -T git@github.com` 返回 `Hi annsshadow!`。

> 注意：git 全局配置了 `credential.helper = !gh auth git-credential`，故 HTTPS 操作会依赖 gh token；本仓库用 SSH 可规避。

---

## ⚠️ 事故与修复记录（同日）— 重要教训

### 事故经过

删除污染文件 `ai/docs/FINAL_SUMMARY.md` 时发生连锁问题：

1. `git rm` 命令执行中被系统 SIGTERM 超时中断，导致误删 `ai/` 下 **288 个文件**（而非 1 个）。
   - 修复：`git checkout -- ai/` 全部恢复，无数据损失。
2. 期间**有外部进程（IDE git 集成）并发操作 git**，导致：
   - `.git/index.lock` 残留
   - 提交 `07def09f` 变为**游离提交**（不挂在任何分支）
   - 分支 `feat/oa4rust-mobile` 被回退到 **379 个提交之前**的 `5456bcc5`
   - 暂存区被塞入 32,751 个条目

### 关键根因：ref 存储位置

`refs/heads/` 目录**完全为空**——所有分支都打包在 `.git/packed-refs` 中。

因此修复时 `git update-ref` 写入的 loose ref 会被立即删除（或未生效），必须**同时改 packed-refs**：

- loose ref 路径：`.git/refs/heads/feat/oa4rust-mobile`（**需先 `mkdir -p` 父目录**，且必须写入**完整 40 位 SHA**，短 SHA 会导致 `rev-parse HEAD` 报 ambiguous）
- packed-refs 需同步 sed 替换

### 有效修复步骤（复用价值高）

```bash
git read-tree HEAD                              # 索引重置到 HEAD（不动工作区）
mkdir -p .git/refs/heads/feat
printf '%s\n' "$(git rev-parse <目标SHA>)" > .git/refs/heads/feat/oa4rust-mobile
sed -i 's|^<旧SHA> refs/heads/feat/oa4rust-mobile$|<新SHA> refs/heads/feat/oa4rust-mobile|' .git/packed-refs
```

CAS 安全写入（推荐，防并发）：`git update-ref <ref> <新值> <期望旧值>`

### 最终状态

- 分支恢复至 `07def09f`（领先 main **384 提交**，ahead origin 1）
- 污染文件已删除，全库 null 字节扫描**清零**
- 暂存区 0 条目

### 遗留说明

`git diff` 显示 198 个文件"已修改"，经 `git diff --ignore-cr-at-eol` 验证**内容差异为 0**，纯 CRLF/LF 行尾符差异（Windows `core.autocrlf=true` 正常行为）。**不要误当数据损坏处理**。

### 教训

1. **命令被 SIGTERM 中断后，必须复核实际影响范围**，不能凭命令意图判断结果。
2. **本仓库存在外部进程并发操作 git**，执行写操作前应确认，且优先用 CAS（`update-ref` 带旧值）。
3. **大仓库中避免 `git rm` / `git status` 全量操作**——易超时且输出巨大。
4. 删除文件用 `rm -f <单文件>` + `git status --short` 核对数量，比 `git rm` 更安全。

---

## 第二次提交：清理工作区残留（18:15）

用户要求提交剩余未提交内容。核查后发现**大部分不该提交**：

### 那 198 个「已修改」文件 = 假象

- `git diff --name-only` = 198 个，但 `git diff --ignore-cr-at-eol` = **0 个**
- 字节级验证：HEAD 存 `\n`，工作区是 `\r\n`，去 CR 后逐字节一致
- 根因：`core.autocrlf` 被从 `true` 改成了 `false`
- **陷阱**：`autocrlf=false` 时 `git add` 会把 `\r\n` 原样存进仓库，是净损害
- 修复：`git config core.autocrlf true` → 198 个差异立即归零

### 实际提交内容

- `.gitignore`：新增 `PR_DESCRIPTION.md` 规则（内容已在 PR #23，无需入库）
- `.workbuddy-ai/memory/2026-09-18.md`：工作记忆
- 提交 `519f42d8`，已推送，PR #23 现含该提交

### 敏感信息审查结论

`.workbuddy-ai/memory/2026-09-18.md` 已扫描：**无明文凭证/密钥**。含有的本机信息仅：
本机绝对路径（`D:\WORKSPACE\fakeToys\...`）、GitHub 用户名 `annsshadow`、token 前缀 `gho_...`（已截断）。
git 用户名密码均为公开仓库信息，判定可入库。

### ⚠️ 外部进程干扰再次发生（重要）

提交 `519f42d8` 后，**HEAD 又被回退到 `07def09f`**，提交变游离。与首次事故完全相同。

**根因确认**：该外部进程会删除 `.git/refs/` 下的 loose ref 文件，使 git 回落到 `.git/packed-refs` 中的旧值。

**通用修复法（已验证 2 次有效）**：

```bash
# 1. 写 loose ref（须先建父目录 + 写完整 40 位 SHA）
mkdir -p .git/refs/heads/feat
printf '%s\n' "$(git rev-parse <新SHA>)" > .git/refs/heads/feat/oa4rust-mobile

# 2. 同步 packed-refs（否则 loose ref 被删后立刻回退）
sed -i 's|^<旧SHA> refs/heads/feat/oa4rust-mobile$|<新SHA> refs/heads/feat/oa4rust-mobile|' .git/packed-refs

# 3. remote-tracking ref 同理
mkdir -p .git/refs/remotes/origin/feat
printf '%s\n' "<新SHA>" > .git/refs/remotes/origin/feat/oa4rust-mobile
sed -i 's|^<旧SHA> refs/remotes/origin/feat/oa4rust-mobile$|<新SHA> ...|' .git/packed-refs
```

**结论**：本仓库**任何 git 写操作后都必须复查 `git rev-parse HEAD`**，因为写入可能被静默回退。已在 `AGENTS.md`/`MEMORY.md` 层面提醒用户关闭 IDE 的 git 自动操作。

---

## CI 失败诊断（18:20）

PR #23 检查项 6/12 通过。诊断出**三个独立根因**：

### 根因 1：`*.txt` 全局忽略规则吞掉了 requirements 文件 ⚠️ 真实缺陷

- `.gitignore:79` 有全局 `*.txt` 规则，导致 `ai/requirements*.txt` **从未被提交**
- 文件在磁盘上存在（`ai/requirements.txt` / `requirements-dev.txt` / `requirements-core.txt`），但 `git ls-files` 查不到
- **后果 A**：`ai-platform-ci.yml` 的 `cache-dependency-path: ai/requirements-dev.txt` 报错
  `No file matched to [ai/requirements-dev.txt or **/pyproject.toml]`
- **后果 B**：`ai/docker/Dockerfile` 构建失败 `failed to calculate checksum of ref ...: "/requirements.txt": not found`
- **同一根因导致 2 个检查项失败**：`Backend tests & coverage`、`Docker image smoke test`
- 修复：在 `.gitignore` 加否定规则，如 `!ai/requirements*.txt`，然后提交这 3 个文件

### 根因 2：parity 测试缺数据库迁移步骤 ⚠️ 真实缺陷

- `integration-tests` 与 `unit-tests` job 的 40 个 parity 测试全部 `left: 500, right: 200`
- 报错：`list handler should return 200, got 500 Internal Server Error`
- 失败文件：`oa4rust/crates/parity/src/behavior_tests.rs`
- **根因**：`oa4rust-ci.yml` 的 `integration-tests` job **没有任何 migration/建表步骤**（无 sqlx/diesel/migrate 调用）
- CI 只起了 postgres/mysql service 容器，但**从未执行 `oa4rust/migrations/` 下的 SQL**
- 表不存在 → handler 查询报 500
- 注意：`shared::testing::test_pool()` 硬编码 `localhost:5432 / user=o2server / dbname=oa4rust`，
  与 CI 的 `POSTGRES_DB: oa4rust` 一致，**连接本身没问题，缺的是建表**

### 根因 3：main 分支本身 CI 就是失败的（非本 PR 引入）

- main 最近一次 CI（run 34812391279, 2026-09-14）失败任务：`quality`、`behavior-compare`、
  `unit-tests (mysql)`、`unit-tests (postgres)`、`integration-tests (postgres)`
- 说明**这些失败在 main 上已存在**，不是本 PR 引入的回归
- 但本 PR 新增的 `ai-platform-ci` / `ai-platform-cd` 失败是新的（根因 1）

### 正在运行的检查（尚未出结果）

`integration-tests (mysql/postgres)`、`unit-tests (mysql/postgres)`、`quality`、`pinned-rustsec`

### 待办

需与用户确认修复范围：是只修本 PR 引入的问题（根因 1），还是连带修 main 上的历史失败（根因 2、3）。

---

## CI 诊断续（18:30）— 发现 workflow 被误删 job ⚠️⚠️

### 已完成修复

提交 `a97c2661`：`.gitignore` 加否定规则 `!ai/requirements*.txt`，提交 3 个依赖清单。
→ 应修好 `Backend tests & coverage` + `Docker image smoke test`。已推送。

### 根因 2 深挖：parity 500 是因为 job 里根本没有迁移步骤

- 迁移执行入口存在：`oa4rust/src/main.rs:47` 调 `shared::migrate::run_migrations(&pool)`
- **但只在二进制启动时跑**（或 `--migrate-only`）。`cargo test -p parity --lib` 不启动二进制 → 迁移永不执行
- CI 的 `unit-tests` / `integration-tests` job **没有任何 migrate 步骤**（已 grep 确认）
- CI postgres 以空库启动 → 无表 → handler 查询 500
- **本地验证：本地库有 392 张表**（历史上跑过迁移），所以本地测试能过 —— 这解释了"本地绿、CI 红"

### 🔴 关键发现：commit 6efa66e0 误删了 2 个 CI job

`6efa66e0`（提交信息为 `feat(ai): add new modules for data operations and streaming`，2026-09-17）对 `.github/workflows/oa4rust-ci.yml` 造成了**与提交意图完全无关的破坏**：

| 项目 | main 版 | 当前分支 |
|---|---|---|
| job 数 | 6 个 | 4 个 |
| `behavior-compare` | ✅ 存在 | ❌ **被删** |
| `behavior-gate` | ✅ 存在 | ❌ **被删** |
| workflow 行数 | 397 行 | 148 行 |
| `paths` 过滤器 | ✅ 有 | ❌ 被移除 |

被删除的 `behavior-compare` job 中包含**唯一的数据库迁移相关步骤**：
- `Apply behavior-compare seed fixtures`：`psql ... -f tests/behavior_comparison/seeds/seed_fixtures.sql`
- 以及 Java 侧播种 `python scripts/seed_java.py --process-form`、差异聚类、报告上传等

**推断**：一个 AI 相关提交意外覆盖/回退了 CI 配置文件（与之前发现的"外部进程回退 ref"可能同源，或是一次非预期的整文件覆盖）。

### 修复方向（待用户确认）

最稳妥的做法是**恢复 `origin/main` 版的 `oa4rust-ci.yml`**，而不是在当前 148 行版本上打补丁：

```bash
git checkout origin/main -- .github/workflows/oa4rust-ci.yml
```

理由：main 版包含 behavior-compare / behavior-gate / paths 过滤 / 种子步骤，是完整版本。

但需注意：main 版 CI 本身也是 failing（`quality`、`behavior-compare`、`unit-tests`、`integration-tests` 在 main 上就红），
所以恢复后仍需进一步修 parity 缺迁移的问题。

### 9/18 晚 二次修正与完整结论

**已完成（已推送）**
- `661b5cc7` test(ai): 修复 `data_file` 固件 POSIX 下的 5 个 404
  - 根因：固件返回 `tmp_path` **绝对路径**并直接拼入 `/api/data/load/{filename}`；`{filename}` 只匹配单个路径段。POSIX 下绝对路径引入额外 `/` → 路由不匹配 → `404 Not Found`（router 级，非 handler 的「文件不存在」）；Windows 下 `\` 非分隔符，整串落同一段内故**侥幸通过** → 这才是"本地绿、CI 红"的真正原因。
  - 修法：改为在 CWD 下建同名文件 + 返回裸文件名 + `yield/finally unlink` 清理。**不能**用 `os.path.relpath`：本机 temp 在 C:、仓库在 D:，跨盘符抛 `ValueError: path is on mount 'C:', start on mount 'D:'`；且相对路径会含 `..`，被 `_safe_data_path` 拒为 400。

**CI 结果（661b5cc7）**：7 pass / 4 fail / 1 skip
- ✅ `Backend tests & coverage`、`Docker image smoke test`、`Frontend build`、`frontend-qa`、`pinned-rustsec`、`openapi-guard`、**`quality`**
  - 注：`quality` 在 main 上是 **fail**，本分支已转绿
- ❌ `unit-tests (postgres|mysql)`、`integration-tests (postgres|mysql)`

### 🔴 结论修正：4 个 Rust 失败是 main 存量问题，与本分支无关

逐字节比对确认：**main 的 `unit-tests` / `integration-tests` 与本分支完全相同**（本分支只是删了 `behavior-compare`/`behavior-gate`）。且 main 上唯一一次 oa4rust-ci 运行（`34812391279`）同样失败。

根因（已定位到具体行）：
- `unit-tests` job **完全没有 `services:` 块** —— 没有数据库容器！只设了 `DATABASE_DIALECT`，未设 `DATABASE_URL`。而 `crates/shared/src/testing.rs:22` 硬编码 `localhost:5432 / user=o2server / dbname=oa4rust` → 连不上 → handler 500。
- `integration-tests` 虽有 services，但 `DATABASE_URL` 指向 `.../postgres`（而非 `oa4rust` 库），且**全仓库没有任何 workflow 执行迁移**（`grep migrat .github/workflows/*.yml` = 空）→ 空库无表。
- 失败画像：`4343 passed; 41 failed`，41 个全是 `parity_behavior__*`：`left: 500, right: 200`，12.59s 跑完（太快，证明根本没建表）。

### ⚠️ 恢复 behavior-compare 的隐藏依赖（重要）

`6efa66e0` 删除的文件中，有 4 个是 `behavior-compare` **运行时必需**，仅恢复 workflow 而不恢复这些文件会让该 job 立刻报错：

| 文件 | main | 分支 |
|---|---|---|
| `oa4rust/scripts/cluster_behavior_diffs.py` | ✓ | ✓ |
| `oa4rust/tests/behavior_comparison/fail_baseline` | ✓ | ✗ |
| `oa4rust/tests/behavior_comparison/seeds/seed_fixtures.sql` | ✓ | ✗ |
| `oa4rust/tests/behavior_comparison/seeds/seed_fixtures_java.http.md` | ✓ | ✗ |
| `scripts/seed_java.py` | ✓ | ✗ |
| `oa4rust/deploy/toggle_module.sh` | ✓ | ✗ |

另注：`behavior-compare` 是 `continue-on-error: true`（**非阻断**），且主分支**未开启保护规则**（`gh api .../branches/main/protection` → 404 Branch not protected），即没有任何 check 是合并的硬性条件。

### 待办

1. 与用户确认：是否恢复 main 版 workflow（含 4 个依赖文件），还是保持精简版只修 4 个 Rust 失败
2. 修 4 个 Rust 失败需：给 `unit-tests` 加 `services:` + 在测试前执行迁移（方式待定：`cargo run --bin oa4rust -- --migrate-only` vs psql 灌 migrations）
3. MySQL 方言：`migrate.rs` 有 dialect 重写逻辑，需单独确认
4. `routing.rs` 的删除是**一致**的（文件与其 `mod.rs` 声明同删），属有意重构，不应恢复



