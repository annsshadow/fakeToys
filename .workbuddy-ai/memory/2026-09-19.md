# 2026-09-19 工作日志

## PR #23 CI 变绿（续 2026-09-18 的工作）

起点：`a55444dc` 上 `CI` 的 6 个 job 只剩 `unit-tests` 红；另有
`AI platform CI`（Python 侧）红。

### 1. 修 `AI platform CI`（提交 `04838ae5`）

**现象**：`Backend tests & coverage` 的 `Run tests with coverage gate` 失败，
3 failed。

**关键做法：用"CI 完全对齐"的隔离环境复现，而不是在有全套可选依赖的本机跑。**
CI 装的是 `ai/requirements-dev.txt` -> `-r requirements-core.txt`，其中
**刻意不含** pandas / chromadb（重型可选依赖）。

复现步骤：
```bash
# 1) 建 venv（本机是 Python 3.14，CI 是 3.11，但差异无关本次问题）
rm -rf .tmp_ci/venv311
C:/Python314/python.exe -m venv .tmp_ci/venv311
# 2) 只装 CI 会装的东西
cd ai && ../.tmp_ci/venv311/Scripts/python.exe -m pip install -r requirements-dev.txt
# 3) 确认真的没有 chromadb
../.tmp_ci/venv311/Scripts/python.exe -c "import importlib.util; print(importlib.util.find_spec('chromadb'))"  # None
# 4) 跑
../.tmp_ci/venv311/Scripts/python.exe -m pytest -q
```
结果：`3 failed, 3058 passed, 2 skipped` —— **精确复现 CI**。

三个失败用例（`ai/tests/unit/`）：

| 用例 | 根因 |
|---|---|
| `test_micro_branches_l61::TestPandasMissingFallback::test_has_pandas_false_degrades` | 末行无条件 `assert cei.HAS_PANDAS is True`，只在装了 pandas 的机器成立 |
| `test_micro_branches_l61::TestPandasMissingFallback::test_export_excel_missing_column_raises` | `export_to_excel` 的 `HAS_PANDAS` 守卫在"缺列校验"**之前**，无 pandas 时先抛 ImportError，永远到不了 ValueError |
| `test_micro_branches_l66::TestVectorFactory::test_create_chromadb_backend` | 直接 `create_vector_db("chromadb")`，无 chromadb 时抛 ImportError |

修复方式（保持意图、去掉环境依赖）：
1. 第一个：改为断言"与环境真实安装情况一致"`assert cei.HAS_PANDAS is was_installed`，
   并在 `finally` 里把真实 pandas 模块原样还原（原来只是 `sys.modules.pop`，
   污染面更大）。
2. 第二个：`@pytest.mark.skipif(not _pandas_installed())`，
   **并新增** `test_export_excel_missing_column_raises_without_pandas`
   覆盖"无 pandas 时应明确报缺依赖"这条真实分支。
3. 第三个：按 `test_chromadb_backend.py` 既有惯例注入假 chromadb 模块
   （`_FakeChromaModule` / `_FakeClient` / `_FakeCollection`），不再依赖真实 chromadb。

验证：
- CI 对齐环境：修复后 `3061 passed, 3 skipped, 0 failed`，覆盖率 99.45%（闸门 80%）
- 本机（有 pandas/chromadb）：这两个文件 `16 passed`，无回归
- 真实 CI：`04838ae5` 的 `AI platform CI` = **success** ✅

> 注：`ai/tests/unit/test_micro_branches_l*.py` 是**并发 agent** 写的文件；
> `a55444dc` 之前 `AI platform CI` 是绿的，是并发提交把红带进来的。

### 2. 修 `CI / unit-tests`（提交 `aadddf5c`）——本日最大发现

**先说结论性事实**：`unit-tests` 在本仓库**从未成功过一次**。
查 `actions/runs?branch=...` 全历史共 19 次 CI run，success 数 = **0**；
单独筛 `unit-tests` job，历史上全是 `failure` / `cancelled`。

**用的是"差异对照法"定位根因**：

| job | 命令 | 结果 |
|---|---|---|
| `unit-tests` | `cargo test --workspace --lib` | ❌ exit **101** |
| `integration-tests` | `cargo test -p parity --lib` + `cargo test --test integration_runner` | ✅ 通过 |

两者都包含 parity 测试，为何一个挂一个过？因为 `--workspace --lib` **多**编译并
运行了 `oa4rust` facade 自己的 lib 测试，其中只有一个用例：

`oa4rust/src/lib.rs::cors_guard::create_app_builds_without_panic`

而 `create_app` 把 **80 个 crate 的路由链式 `.merge()`** 成一个巨型 axum Router。

**关键证据是仓库自己的注释**（`oa4rust/.cargo/config.toml`）：
> `create_app` 将 80 个 crate 路由链式 `.merge()` 为一个巨型 axum Router。
> 该构造在默认约 1–2MB 的线程栈上会栈溢出（已确认是栈大小需求，而非无限递归——
> 64MB 栈可确定性通过）。

而该文件里唯一的缓解手段是：
```toml
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "link-args=/STACK:67108864"]
```
**只对 Windows 主线程生效。** Linux/CI 上 `#[tokio::test]` 的 worker 线程仍是默认栈
（约 2MB）→ 栈溢出 → panic → **exit code 101**（与 annotation 完全吻合）。
`.github/workflows/` 里**没有** `RUST_MIN_STACK`；`integration-tests` 之所以过，
是因为它从不构造那个巨型合并 Router（`integration_runner` 的用例还全部 `#[ignore]`）。
另外注释里提到的"与集成测试的 64MB worker 栈一致"——**该 64MB 设置在仓库里根本不存在**，
是个失效的引用。

**本机验证（决定性）**：
```
# 修复前：RUST_MIN_STACK=262144 跑 cors_guard
thread 'cors_guard::create_app_builds_without_panic' has overflowed its stack   ← 复现！
# 修复后：同样的 262144
test cors_guard::create_app_builds_without_panic ... ok. 1 passed
```
（在 1MB/2MB/4MB 下修复前后都过——因为 Windows 二进制有 `/STACK:67108864`，
`RUST_MIN_STACK` 覆盖不了主线程。必须压到 256KB 才能把 Windows 上的这个缺陷暴露出来，
这也解释了为什么这个 bug 能潜伏这么久。）

**修复**：不再依赖平台链接参数，改为在显式 `stack_size(64MB)` 的线程上构造 Router：
```rust
const CREATE_APP_STACK_SIZE: usize = 64 * 1024 * 1024;
let handle = std::thread::Builder::new()
    .name("create_app_build".to_string())
    .stack_size(CREATE_APP_STACK_SIZE)
    .spawn(move || {
        tokio::runtime::Builder::new_current_thread()
            .enable_all().build().expect("...")
            .block_on(async move { create_app(pool, session_manager, rate_limiter).await
                .expect("unified create_app must build without panic") })
    }).expect("spawn create_app build thread");
let _app = handle.join().expect("create_app must build without panic");
```

**CI 对齐的干净库上全量验证**：
`cargo test --workspace --lib -j 1 --no-fail-fast` → 97 个目标，
**7621 passed / 0 failed / 136 ignored，EXIT=0**。

### 3. 复现"CI 完全对齐数据库"的完整流程（本日方法论沉淀）

```bash
# 1) 建全新库
psql -h localhost -U o2server -d postgres -c "CREATE DATABASE oa4rust_ciexact;"
# 2) 用 Rust 迁移运行器（与 CI 的 `cargo run --bin oa4rust -- --migrate-only` 一致）
cd oa4rust && DATABASE_URL="postgres://o2server:password@localhost:5432/oa4rust_ciexact" \
  DATABASE_DIALECT=postgres ./target/debug/oa4rust.exe --migrate-only
#    -> 378 张表 / 99 条迁移
# 3) 以它为模板替换测试库（test_pool() 硬编码 dbname=oa4rust，忽略 DATABASE_URL）
psql -d postgres -c "DROP DATABASE oa4rust;"
psql -d postgres -c "CREATE DATABASE oa4rust TEMPLATE oa4rust_ciexact;"
# 4) 跑测试
source scripts/rust-env.sh && CARGO_INCREMENTAL=0 DATABASE_DIALECT=postgres \
  DATABASE_URL="postgres://o2server:password@localhost:5432/oa4rust" \
  cargo test --workspace --lib -j 1 --no-fail-fast
```

> **重要**：本机 `oa4rust` 数据库已被替换为上述复现库；
> 原库备份在 `.tmp_ci/oa4rust_backup_0143.sql`（1.4MB）。

### 4. 环境/工具教训

- **`cargo build --bin` 最后一步可能失败**：`failed to remove target/debug/oa4rust.exe:
  拒绝访问 (os error 5)`——有**残留的 oa4rust.exe 进程**占着文件（本次 PID 5248）。
  `taskkill //F` 在 Git Bash 下参数会被转义坏（报"无效参数/选项 - '//F'"），
  要用 PowerShell `Stop-Process -Id <pid> -Force`。杀掉后重新 `cargo build` 25s 完成。
- **`cargo test` 的 debug test profile 编译非常久**（`parity` 有 94336 行生成测试，
  4385 条 warning），本次 `--workspace --lib` 全量编译+运行约 **36 分钟**。
- **git ref 被并发进程清空（本会话第 15、16 次）**：这次更狠——`.git/refs/heads/feat/`
  整个目录被删 + `packed-refs` 被回退，导致 `git rev-parse HEAD` 直接报
  "does not have any commits yet"。
  恢复方法（已验证有效）：
  ```bash
  git reflog -5                    # 找游离提交 SHA
  git cat-file -t <sha>            # 确认对象存在
  git merge-base --is-ancestor <旧sha> <新sha>   # 确认是超集
  mkdir -p .git/refs/heads/feat && git rev-parse <sha> > .git/refs/heads/feat/oa4rust-mobile
  # 同时用 python 改 .git/packed-refs 对应行（sed 里嵌 $(...) 会失效）
  git push origin feat/oa4rust-mobile
  ```

### 5. 仍未解决

- `Code scanning AI findings on PR #23`：GitHub Copilot Autofind **服务侧**失败，
  历史上同样失败，非代码问题，不属 `CI` workflow。
- 全仓仍有大量"非 Option 读可空列 / TIMESTAMP 缺 `::text`"的同类隐患
  （`.tmp_ci/scan5.py` 扫描结果，例如 `organization_assemble_control` 里
  `Value::String(row.get("unit_id"))` 62 处、`"creator"` 83 处、
  `"create_time"` 91 处；当前因 seed 都给了非 NULL 值而没暴露）。
