## 目标
处理剩余三个遗留项，并严格遵守“每次修改前分批提交工作区内容”：任何修改前先检查并提交当前工作区已有变更；每个逻辑修改单独提交。

## 1. 清理 39GB Rust 增量缓存
- 先确认 `git status`、没有正在运行的 cargo/rustc 进程，并记录 `oa4rust/target/debug/incremental` 大小。
- 仅删除未跟踪且被 `.gitignore` 忽略的 `oa4rust/target/debug/incremental`，不执行全量 `cargo clean`，保留其他 debug/release 产物，缩小影响范围。
- 删除后复核磁盘占用和工作区状态。

## 2. 修复 cms_express 的 `test_template_form_list`
- 先查询测试数据库 `information_schema.columns`，确认 `x_cms_templateform.xid` 的实际类型；同时读取当前测试与 SQL 实现，确认复现路径。
- 采用最小、兼容旧库的代码修复：查询字段显式使用 `xid::text AS xid`、`xname::text AS xname`、`xcategory::text AS xcategory`，避免 PostgreSQL `uuid`/其他类型直接反序列化为 Rust `String`；不改变 API 输出结构。
- 将生成测试从“仅非 404/固定 500”改为在数据库可用时验证成功响应结构，在数据库不可用时沿用项目现有 guard/跳过约定，避免把 schema 故障固化为预期行为。若现有测试环境确实只提供错误路径，则根据实际测试模式采用最小断言调整，不扩大范围。
- 先提交 Rust 查询修复，再提交测试修复（若测试需要独立修改），每次修改前确认工作区干净/已提交。
- 验证：`cargo test -p cms_express --lib`，随后运行相关 workspace 测试；若仍受本地数据库 schema 影响，明确记录实际失败原因，不伪报通过。

## 3. 完成 ECharts 5→6 遗留项
- 现状调查显示升级已完成并已提交：根 `package.json`/锁文件均为 `echarts 6.1.0`，源码使用的 `init/setOption/dispose` 均兼容，TypeScript 与 Vite 构建已通过。
- 不重复修改已解决的版本升级；补做 `pnpm install --frozen-lockfile`、desktop typecheck、生产构建验证。
- 为避免 workspace 依赖隔离问题，检查 `apps/desktop/package.json` 的依赖归属。只有在仓库约定允许且验证确有必要时，才把 `echarts` 移/补充为 desktop 直接依赖；若采用，则先提交当前工作区，再单独修改锁文件并单独提交。
- 不新增无关组件重构；`table` 类型、ResizeObserver、类型收紧等属于后续质量改进，不作为本次“升级遗留项”扩大范围。

## 4. 最终验证与汇报
- 检查 `git status`、提交列表和每个提交的文件范围。
- 汇总：缓存释放量、cms_express 测试结果、ECharts 构建/typecheck 结果，以及任何仍受外部数据库条件限制的项。