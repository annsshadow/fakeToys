#[cfg(test)]
mod tests {

    // SKIPPED: calendar_list_public requires tower (not available)
    // SKIPPED: calendar_list_my requires tower (not available)
    // SKIPPED: calendar_get requires tower (not available)
    // SKIPPED: calendar_create requires tower (not available)
    // SKIPPED: calendar_update requires tower (not available)
    // SKIPPED: calendar_remove requires tower (not available)
    // SKIPPED: event_create requires tower (not available)
    // SKIPPED: event_update requires tower (not available)
    // SKIPPED: event_remove requires tower (not available)
    // SKIPPED: event_list requires tower (not available)
}