// AUTO-GENERATED by scripts/generate_parity_tests.py — DO NOT EDIT.
//
// Parity regression suite (Phase 4 U4.1).
//
// For each o2server @Path mapped in docs/audits/o2server-parity-report.json,
// this file contains a test that verifies the the corresponding Rust axum route
// is registered on the crate's Router.  A NOT_FOUND (404) response means the
// route is missing from the Rust implementation — a parity gap.
//
// Generated: 2026-09-01 16:38:12
// Crates: 55   Routes: 4117   Tests: 4117

#[allow(unreachable_code, unused_variables, non_snake_case)]

use axum::body::Body;
use axum::http::{Request, Method, StatusCode};
use tower::util::ServiceExt;
use shared::testing::test_pool;

    // ── x_ai_assemble_control → ai_assemble_control (43 routes) ──
    #[tokio::test]
    async fn parity__ai_assemble_control__config_create_mcp() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/assemble/control/config/create/mcp")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_delete_mcp_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/assemble/control/config/delete/mcp/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_get_mcp_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/assemble/control/config/get/mcp/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_list_mcp_paging_page_size_size() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/assemble/control/config/list/mcp/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_update_mcp_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/assemble/control/config/update/mcp/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__chat_completion() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/chat/completion")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__chat_completion_stream() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/chat/completion/stream")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__chat_delete_clue_id() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/chat/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__chat_list_completion_clue_id_paging_page_size_size() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/chat/list/completion/test-id/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__chat_list_paging_page_size_size() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/chat/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__chat_write_completion_extra() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/chat/write/completion/extra")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_base_config() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/base/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_create_mcp_1() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/create/mcp")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_create_model() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/create/model")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_delete_mcp_flag_1() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/delete/mcp/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_delete_model_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/delete/model/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_get() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_get_mcp_ext_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/get/mcp/ext/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_get_mcp_flag_1() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/get/mcp/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_get_model_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/get/model/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_list_enable_model() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/list/enable/model")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_list_mcp_paging_page_size_size_1() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/list/mcp/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_list_model_paging_page_size_size() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/list/model/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_save() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/save")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_update_mcp_flag_1() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/update/mcp/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_update_model_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/update/model/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_copy_file() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/copy/file")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_delete_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_list_with_ids() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_list_paging_page_size_size() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_upload() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_id_download() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_id_download_scale() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/test-id/download/scale")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__get_ai_control_config() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/get/ai/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__get_usage_stats() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/get/usage/stats")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__index_cms_doc_with_app_appId() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/index/cms/doc/with/app/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__index_cms_doc_docId() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/index/cms/doc/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__index_delete_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/index/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__index_list_paging_page_size_size() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/index/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__index_sync_to_knowledge() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/index/sync/to/knowledge")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__list_ai_models() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/list/ai/models")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__update_ai_control_config() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/update/ai/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_ai_core_entity → ai (21 routes) ──
    #[tokio::test]
    async fn parity__ai__app_list() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/app/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__chat_delete() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/chat/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__chat_list_completion_paging() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/chat/list/completion/test-id/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__chat_list_paging() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/chat/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_base_config() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/base/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_get() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_get_mcp() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/get/mcp/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_get_model() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/get/model/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__list_enable_model() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/list/enable/model")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_list_mcp_paging() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/list/mcp/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_list_model_paging() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/list/model/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__conversation_list() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/conversation/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__file_delete() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/file/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__file_get() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__file_download() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/file/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__file_download_scale() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/file/test-id/download/scale")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__index_cms_doc_with_app() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/index/cms/doc/with/app/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__index_cms_doc() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/index/cms/doc/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__index_delete() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/index/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__sync_to_knowledge() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/index/sync/to/knowledge")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__ai__model_list() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/model/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_attendance_assemble_control → attendance_assemble_control (228 routes) ──
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceadmin")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_1() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceadmin/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_2() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceadmin/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_3() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceadmin/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_4() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/appeal/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_5() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/appeal/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_6() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/archive/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_7() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/archive/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_8() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/audit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_9() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/audit")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_10() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/check")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_11() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/check")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_12() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_13() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_14() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_15() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_16() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/manager/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_17() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/manager/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_18() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/workflow/appeal/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_19() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/workflow/appeal/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_20() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/workflow/sync")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_21() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/workflow/sync")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_22() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_23() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_24() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_25() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/analyse")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_26() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/analyse/id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_27() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/analyse/id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_28() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/analyse/redo")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_29() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/analyse/redo")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_30() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/analyse/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_31() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/analyse/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_32() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/archive/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_33() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/archive/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_34() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/checkDetailWithPersonByCycle/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_35() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/checkDetailWithPersonByCycle/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_36() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_37() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_38() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/topUnit")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_39() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/topUnit")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_40() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/unit")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_41() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/unit")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_42() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/user")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_43() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/user")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_44() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_45() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_46() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_47() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_48() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/list/persons/nonesign")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_49() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/list/persons/nonesign")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_50() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_51() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/filter/list/page/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_52() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/filter/list/page/test-id/count/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_53() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/mobilepreview")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_54() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/mobilepreview")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_55() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/my")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_56() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/my")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_57() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/recive")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_58() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_59() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_60() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/recive")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_61() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/reciveSingle")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_62() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_63() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_64() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceemployeeconfig")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_65() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceemployeeconfig/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_66() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceemployeeconfig/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_67() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceemployeeconfig/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_68() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceimportfileinfo/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_69() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceimportfileinfo/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_70() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceimportfileinfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_71() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceschedulesetting")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_72() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceschedulesetting/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_73() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceschedulesetting/list/topUnit/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_74() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceschedulesetting/list/unit/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_75() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceschedulesetting/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_76() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceschedulesetting/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_77() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_78() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_79() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_80() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_81() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_82() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_83() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_84() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_85() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancesetting")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_86() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancesetting/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_87() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancesetting/enable/type")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_88() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancesetting/enable/type")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_89() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancesetting/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_90() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancesetting/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_91() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancesetting/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_92() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticalcycle")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_93() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticalcycle/cycleDetail/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_94() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticalcycle/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_95() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticalcycle/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_96() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticalcycle/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_97() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticrequirelog")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_98() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticrequirelog/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_99() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticrequirelog/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_100() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticrequirelog/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_101() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceworkdayconfig")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_102() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceworkdayconfig/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_103() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceworkdayconfig/filter")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_104() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceworkdayconfig/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_105() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceworkdayconfig/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_106() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceworkdayconfig/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_107() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/dingding/all")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_108() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/dingding/attendance/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_109() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/dingding/statistic/person/year/test-id/month/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_110() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/dingding/statistic/unit/year/test-id/month/test-id/day/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_111() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/dingding/sync/from/test-id/to/test-id/start")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_112() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/dingding/sync/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_113() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/dingdingstatistic/person/unit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_114() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/dingdingstatistic/person/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_115() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/dingdingstatistic/unit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_116() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/qywx/all")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_117() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/qywx/attendance/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_118() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/qywx/statistic/person/year/test-id/month/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_119() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/qywx/statistic/unit/year/test-id/month/test-id/day/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_120() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/qywx/sync/from/test-id/to/test-id/start")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_121() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/qywx/sync/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_122() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/qywxstatistic/person/unit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_123() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/qywxstatistic/person/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_124() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/qywxstatistic/unit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_125() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/rule/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_126() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/rule/test-id/toggle")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_127() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/rule/test-id/toggle")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_128() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/selfholidaysimple")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_129() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/selfholidaysimple/docId/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_130() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/selfholidaysimple/docId/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_131() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statistic/do")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_132() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statistic/do")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_133() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/personMonth/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_134() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/personMonth/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_135() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/personMonth/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_136() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/personMonth/list/test-id/prev/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_137() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitDay/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_138() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitDay/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_139() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitDay/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_140() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitDay/list/test-id/prev/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_141() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitMonth/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_142() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitMonth/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_143() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitMonth/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_144() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitMonth/list/test-id/prev/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_145() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitDay/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_146() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitDay/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_147() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitDay/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_148() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitDay/list/test-id/prev/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_149() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitMonth/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_150() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitMonth/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_151() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitMonth/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_152() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitMonth/list/test-id/prev/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_153() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/person/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_154() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/persons/unit/subnested/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_155() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/persons/unit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_156() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/topUnit/day/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_157() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/topUnit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_158() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/day/topUnit/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_159() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/day/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_160() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/day/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_161() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/subnested/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_162() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/sum/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_163() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/topUnit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_164() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_165() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/uuid/random")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_166() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/appeal/list/manager/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_167() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/appeal/list/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_168() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/appeal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_169() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/appeal/test-id/end/process")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_170() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/appeal/test-id/manager/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_171() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/appeal/test-id/reset/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_172() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/appeal/test-id/start/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_173() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/appeal/test-id/start/process")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_174() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_175() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/config")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_176() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/config/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_177() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/config/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_178() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/detail/list/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_179() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/detail/rebuild/person/test-id/date/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_180() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/detail/statistic/export/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_181() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/detail/statistic/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_182() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/detail/statistic/test-id/list/record")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_183() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/group")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_184() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/group/list/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_185() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/group/person/test-id/date/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_186() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/group/rebuild/detail/group/test-id/date/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_187() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/group/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_188() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/group/test-id/delete")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_189() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/group/test-id/refresh/participate")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_190() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/groupschedule")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_191() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/groupschedule/config/group/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_192() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/groupschedule/list/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_193() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/groupschedule/list/group/test-id/month/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_194() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/leave")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_195() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/leave/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_196() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/leave/import")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_197() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/leave/import/result/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_198() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/leave/list/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_199() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/leave/template")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_200() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/mobile/check")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    // skipped: request path not representable as a URI (Java-parity quirk): /jaxrs/attendance/assemble/control/v2/mobile/check/ from/out (POST)

    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_202() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/mobile/check/%20from/out")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_203() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/mobile/check/pre")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_204() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/my/controls")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_205() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/my/detail/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_206() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/my/rest/date/check")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_207() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/my/statistic")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_208() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/my/version")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_209() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/record/delete/people/test-id/date/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_210() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/record/import")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_211() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/record/import/daily")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_212() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/record/list/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_213() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/record/template")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_214() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/record/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_215() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/shift/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_216() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/shift/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_217() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/shift/list/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_218() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/shift/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_219() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/shift/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_220() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/workplace")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_221() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/workplace/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_222() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/workplace/list/ids")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_223() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/v2/workplace/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_224() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/workplace")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_225() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/workplace/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_226() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/workplace/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_227() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/workplace/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_attendance_core_entity → attendance (9 routes) ──
    #[tokio::test]
    async fn parity__attendance__crate() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/admin/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_1() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/appeal/archive/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_2() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/appeal/audit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_3() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/appeal/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_4() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/appeal/submit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_5() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/employee/config/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_6() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/record/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_7() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/rule/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_8() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/statistical/cycle/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_base_core_project → base (9 routes) ──
    #[tokio::test]
    async fn parity__base__crate() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/cache")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__base__crate_1() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/cache/commonscript/flush")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__base__crate_2() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/cache/config/flush")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__base__cache_detail() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/cache/detail")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__base__echo_get() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/echo")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__base__echo_get_1() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/echo/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__base__crate_3() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/fireschedule/classname/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__base__openapi_info() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/openapi/info")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__base__crate_4() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/sysresource/filePath/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_bbs_assemble_control → bbs_assemble_control (38 routes) ──
    #[tokio::test]
    async fn parity__bbs_assemble_control__get_control_config() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/delete/forum")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_1() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/delete/forum")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_2() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/delete/reply")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_3() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/delete/reply")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_4() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/delete/subject")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_5() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/delete/subject")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__list_forums() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/forum/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_6() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/list/reply/filter")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_7() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/list/subjects/filtered")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_8() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/list/subjects/index")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_9() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/list/subjects/recommended/index")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_10() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/list/topics/creamed")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_11() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/list/topics/recommended")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_12() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/picture/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__create_reply() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/reply/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__list_control_sections() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/section/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__shutup_create() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/shutup/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__shutup_delete() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/shutup/delete")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__shutup_delete_1() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/shutup/delete")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__subject_creamed_list() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/subject/creamed/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__subject_filter_list() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/subject/filter/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__subject_index_list() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/subject/index/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_13() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/subject/search")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__subject_statgrade() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/subject/statgrade")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_14() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/topic/creamed/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__create_topic() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/topic/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_15() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/topic/filter/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_16() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/topic/filter/listsubjectinfo")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_17() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/topic/index/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_18() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/topic/list/forum/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_19() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/topic/recommended/index")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_20() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/topic/recommended/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__crate_21() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/topic/search")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__update_control_config() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/update/control/config")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__update_control_config_1() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/update/control/config")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__user_info() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/user/info")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__uuid_generate() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/uuid")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_bbs_core_entity → bbs (9 routes) ──
    #[tokio::test]
    async fn parity__bbs__forum() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/forum/view/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs__forum_1() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/forum/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs__section() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/section/view/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs__section_1() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/section/viewforum/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs__subject() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/subject/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs__subject_1() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/subject/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs__subject_2() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/subject/search")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs__subject_3() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/subject/top/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__bbs__subject_4() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/subject/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_calendar_assemble_control → calendar_assemble_control (35 routes) ──
    #[tokio::test]
    async fn parity__calendar_assemble_control__get_calendar_detail() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/assemble/control/calendar/detail/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/calendar")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_1() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/calendar/follow/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_2() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/calendar/follow/test-id/cancel")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_3() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/calendar/ismanager")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_4() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/calendar/ismanager/calendar/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_5() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/calendar/list/filter")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_6() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/calendar/list/my")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_7() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/calendar/list/public")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_8() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/calendar/manager/list/with/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_9() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/calendar/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_10() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/calendar/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_11() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_12() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/after/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_13() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/all/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_14() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/list/filter")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_15() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/list/filter/sample")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_16() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/list/filter/sample/manager")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_17() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_18() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/rfc/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_19() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/single/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_20() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/update/after/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_21() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/update/all/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_22() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/update/single/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_23() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/event/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__get_control_config() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/get/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__list_control_calendars() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/list/control/calendars")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_24() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/message")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_25() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/setting")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_26() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/setting/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_27() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/setting/ismanager")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_28() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/setting/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_29() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/setting/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__crate_30() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/test/1")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__update_control_config() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/update/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_calendar_core_entity → calendar (10 routes) ──
    #[tokio::test]
    async fn parity__calendar__calendar_create() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar__calendar_list_my() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/list/my")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar__calendar_list_public() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/list/public")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar__calendar_remove() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/remove")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar__calendar_update() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar__calendar_get() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar__event_create() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/event/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar__event_list() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/event/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar__event_remove() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/event/remove")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__calendar__event_update() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/event/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_cms_assemble_control → cms_assemble_control (449 routes) ──
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_document_filter_list_id_next_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/document/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_document_filter_list_id_next_count_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/document/filter/list/test-id/next/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_document_filter_list_page_size_size() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/document/filter/list/test-id/size/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_document_filter_list_page_size_size_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/document/filter/list/test-id/size/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_document_id_view() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/document/test-id/view")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_fileinfo_download_document_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/fileinfo/download/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_fileinfo_download_document_id_stream() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/fileinfo/download/document/test-id/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_fileinfo_list_document_documentId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/fileinfo/list/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_id_document_documentId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/fileinfo/test-id/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_form_v2_lookup_document_docId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/form/v2/lookup/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_form_v2_lookup_document_docId_mobile() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/form/v2/lookup/document/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_form_v2_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/form/v2/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_form_v2_id_mobile() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/form/v2/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_form_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/form/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_surface_appdict_list_appInfo_appInfoFlag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/surface/appdict/list/appInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_surface_appdict_appDictFlag_appInfo_appInfoFlag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/surface/appdict/test-id/appInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_surface_appdict_appDictFlag_appInfo_appInfoFlag_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/surface/appdict/test-id/appInfo/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/surface/appdict/test-id/appInfo/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/surface/appdict/test-id/appInfo/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path_2() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path_3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path_4() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appconfig_u2_get() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appconfig/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appconfig_u2_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appconfig/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_u2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_alias_alias() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/alias/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_erase_app_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/erase/app/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_erase_app_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/erase/app/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_filter_list_id_next_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_filter_list_id_next_count_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/filter/list/test-id/next/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_filter_list_id_prev_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_filter_list_id_prev_count_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/filter/list/test-id/prev/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_flag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_get_user_publish_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/get/user/publish/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_all() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_appType() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/appType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_appType_manager() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/appType/manager")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_has_document() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/has/document")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_has_document_appType() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/has/document/appType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_has_document_type_appType() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/has/document/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_manage() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_manage_type_appType() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/manage/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_user_publish() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/user/publish")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_user_publish_type_appType() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/user/publish/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_user_publish_with_process() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/user/publish/with/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_user_view() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/user/view")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_user_view_all() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/user/view/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_user_view_all_type_appType() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/user/view/all/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_user_view_article_type_appType() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/user/view/article/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_user_view_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/user/view/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_list_user_view_data_type_appType() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/list/user/view/data/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_u2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_id_control() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/test-id/control")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_appId_icon_size_size() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/test-id/icon/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__appinfo_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_u2_app_info() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/appinfo/test-id/permission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__application_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_u2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_alias_alias() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/alias/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_bind_categoryId_view() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/bind/test-id/view")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_bind_categoryId_view_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/bind/test-id/view/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_erase_category_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/erase/category/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_erase_category_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/erase/category/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_ext_content_save_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/extContent")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_filter_list_id_next_count_app_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/filter/list/test-id/next/test-id/app/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_filter_list_id_next_count_app_appId_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/filter/list/test-id/next/test-id/app/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_filter_list_id_prev_count_app_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/filter/list/test-id/prev/test-id/app/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_filter_list_id_prev_count_app_appId_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/filter/list/test-id/prev/test-id/app/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_filter_list_page_size_size() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/filter/list/test-id/size/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_filter_list_page_size_size_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/filter/list/test-id/size/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_flag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_list_all() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_list_manage_app_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/list/manage/app/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_list_objects_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/list/objects")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_list_publish_app_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/list/publish/app/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_list_view_app_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/list/view/app/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_list_view_app_appId_all() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/list/view/app/test-id/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_list_view_app_appId_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/list/view/app/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_u2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_id_control() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/test-id/control")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_id_execute_projection() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/test-id/execute/projection")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__categoryinfo_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_u2_category_info() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/categoryinfo/test-id/permission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_search() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms_assemble_control/document/search")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__get_control_config() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms_assemble_control/get/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__list_control_sections() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms_assemble_control/list/control/sections")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__update_control_config() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms_assemble_control/update/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__update_control_config_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms_assemble_control/update/control/config")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__commend_list_paging() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/commend/list/paging/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__commend_list_paging_page_size_size() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/commend/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__commend_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/commend/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_u2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_list_id_next_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_list_id_next_count_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment/list/test-id/next/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_list_id_prev_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment/list/test-id/prev/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_list_id_prev_count_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment/list/test-id/prev/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_u2_list_page_size_size() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment/list/test-id/size/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_u2_list_page_size_size_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment/list/test-id/size/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_u2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_commend_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment/test-id/commend")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__comment_uncommend_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/comment/test-id/uncommend")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__correlation_doc_docId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/doc/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__correlation_u2_doc_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/doc/test-id/delete")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__correlation_list_doc_docId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/list/doc/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__correlation_list_doc_docId_site_site() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/list/doc/test-id/site/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__correlation_update_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/update/doc/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_array_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/array/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_path7_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_path7() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_path7_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_path7_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_path7_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_path7_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__design_appdict_create_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/design/appdict")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__design_appdict_list_appInfo_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/design/appdict/list/appInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__design_appdict_list_paging_page_size_size() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/design/appdict/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__design_appdict_delete_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/design/appdict/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__design_appdict_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/design/appdict/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__design_appdict_update_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/design/appdict/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__design_appdict_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/design/appdict/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__design_appdict_id_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/design/appdict/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__designer_u2_search() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/designer/search")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_management_refresh_all() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/docpermission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_achive_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/achive/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_batch_modify_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/batch/data/modify")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_batch_modify_mock_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/batch/data/modify/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_batch_status_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/batch/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_batch_delete_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/batch/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_batch_delete_mock_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/batch/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_batch_name_status_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/batch/test-id/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_category_change() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/category/change")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_category_change_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/category/change/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_cipher_filter_list_page_size_size() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/cipher/filter/list/test-id/size/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_cipher_filter_list_page_size_size_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/cipher/filter/list/test-id/size/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_cipher_publish_workflow_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/cipher/publish/content")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_cipher_publish_workflow_mock_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/cipher/publish/content/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_cipher_permission_read_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/cipher/test-id/permission/read/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_cipher_id_persist_view_record() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/cipher/test-id/persist/view/record")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_fields() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/document/fields")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_draft_next_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/draft/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_draft_next_u3_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/draft/list/test-id/next/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_filter_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/filter/count")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_filter_count_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/filter/count/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_filter_next_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_filter_next_u3_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/filter/list/test-id/next/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_filter_prev_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_filter_prev_u3_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/filter/list/test-id/prev/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_filter_paging_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/filter/list/test-id/size/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_filter_paging_manager_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/filter/list/test-id/size/test-id/manager")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_filter_paging_u3_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/filter/list/test-id/size/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_list_document() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/list/document")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_list_document_data_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/list/document/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_publish_content_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/publish/content")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_publish_content_mock_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/publish/content/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_publish() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/publish/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_publish_cancel() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/publish/test-id/cancel")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_publish_cancel_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/publish/test-id/cancel/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_publish_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/publish/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_get() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_commend() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/commend")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_control_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/control")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_document_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/document/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_get_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_notify_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/notify")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_permission_read_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/permission/read")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_persons_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/persons")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_publish_html_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/publish/html")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_top() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_un_top() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/unTop")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_uncommend() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/uncommend")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_u2_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_document_id_view_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/view")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_id_view_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/view/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__export_app_info_app_info_flag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/export/appInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_u2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_flag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_list_appInfo_appInfoFlag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/appInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_list_id_next_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_list_id_prev_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_copy_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id/appInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_flag_appInfo_appInfoFlag_content() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id/appInfo/test-id/content")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_download_with_app_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id/appInfo/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_delete_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_update_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_id_content() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id/content")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_id_download() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_flag_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_u2_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__file_id_upload() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_batch_download_doc_docId_site_site() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/batch/download/doc/test-id/site/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_u2_copy_to_doc() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/copy/to/doc/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_download_document_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/download/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_download_document_id_stream() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/download/document/test-id/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_download_transfer_flag_flag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/download/transfer/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_edit_id_doc_docId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/edit/test-id/doc/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_edit_id_doc_docId_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/edit/test-id/doc/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_list_all() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_list_document_documentId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/list/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_u2_filter() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/list/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_u2_replace_to_doc() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/replace/to/doc/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_update_document_docId_attachment_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/update/document/test-id/attachment/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_update_document_docId_attachment_id_callback_callback() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/update/document/test-id/attachment/test-id/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_update_id_content() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/update/test-id/content")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_upload_doc_docId_save_as_flag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/upload/doc/test-id/save/as/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_upload_document_docId_callback_callback() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/upload/document/test-id/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_upload_document_docId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/upload/document/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_upload_with_url_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/upload/with/url")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_u2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_binary_base64_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/test-id/binary/base64/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_id_doc_docId_change_seqnumber_seqNumber() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/test-id/doc/test-id/change/seqnumber/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_id_document_documentId_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/test-id/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_id_online_info() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/test-id/online/info")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_id_preview_pdf() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/test-id/preview/pdf")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_u2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_filter_list_id_next_count_app_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/filter/list/test-id/next/test-id/app/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_filter_list_id_next_count_app_appId_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/filter/list/test-id/next/test-id/app/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_filter_list_id_prev_count_app_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/filter/list/test-id/prev/test-id/app/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_filter_list_id_prev_count_app_appId_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/filter/list/test-id/prev/test-id/app/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_list_all() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_list_app_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/list/app/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_list_formfield_appInfo_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/list/formfield/appInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_list_id_formfield() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/list/test-id/formfield")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_v2_lookup_document_docId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/v2/lookup/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_v2_lookup_document_docId_mobile() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/v2/lookup/document/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_v2_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/v2/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_v2_id_mobile() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/v2/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_u2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_u2_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_get_with_appinfo_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/test-id/appinfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__form_id_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/form/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__formversion_list_form_formId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/formversion/list/form/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__formversion_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/formversion/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__image_encode_base64() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/image/encode/base64")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__image_encode_base64_size_size() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/image/encode/base64/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__image_resize_id_id_width_width_height_height() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/image/resize/id/test-id/width/test-id/height/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__import_app_info_app_info_flag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/import/appInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__input_compare() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/input/compare")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__input_compare_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/input/compare/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__input_cover() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/input/cover")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__input_cover_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/input/cover/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__input_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/input/create")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__input_create_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/input/create/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__input_prepare_cover() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/input/prepare/cover")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__input_prepare_cover_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/input/prepare/cover/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__input_prepare_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/input/prepare/create")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__input_prepare_create_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/input/prepare/create/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__log_filter_list_id_next_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/log/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__log_filter_list_id_prev_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/log/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__log_list_app_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/log/list/app/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__log_list_category_categoryId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/log/list/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__log_list_document_documentId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/log/list/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__log_list_filter_page_size_size() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/log/list/filter/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__log_list_level_operationLevel() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/log/list/level/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__log_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/log/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__output_list() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/output/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__output_appInfoFlag_select() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/output/test-id/select")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__output_appInfoFlag_select_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/output/test-id/select/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_appInfo_id_manageable() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/appInfo/test-id/manageable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_appInfo_id_managers() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/appInfo/test-id/managers")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_appInfo_id_publishers() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/appInfo/test-id/publishers")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_appInfo_id_viewers() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/appInfo/test-id/viewers")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_category_id_managers() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/category/test-id/managers")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_category_id_publishers() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/category/test-id/publishers")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_category_id_viewers() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/category/test-id/viewers")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_categoryInfo_id_manageable() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/categoryInfo/test-id/manageable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_management_refresh_all_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/management/refresh/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_management_refresh_category_categoryId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/management/refresh/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_save_manager_app_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/manager/appInfo/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_save_manager_category_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/manager/categoryInfo/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_save_publisher_app_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/publisher/appInfo/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_save_publisher_category_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/publisher/categoryInfo/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_save_viewer_app_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/viewer/appInfo/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__permission_save_viewer_category_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/permission/viewer/categoryInfo/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__queryview_flag_definition() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/flag/test-id/definition/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__review_v2_search_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/review/v2/search")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_u2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_list_app_appId_name_name() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/list/app/test-id/name/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_list_app_flag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/list/app/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_u2_list_manager() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/list/manager")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_list_paging_page_size_size() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_list_id_next_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_list_id_prev_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_u2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_u2_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_id_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_post_nested_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/test-id/app/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_uniqueName_app_flag_imported() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/test-id/app/test-id/imported")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__script_load_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/script/test-id/appInfo/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__scriptversion_list_script_scriptId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/scriptversion/list/script/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__scriptversion_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/scriptversion/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__searchfilter_list_archive_filter_category_categoryId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/searchfilter/list/archive/filter/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__searchfilter_list_draft_filter_category_categoryId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/searchfilter/list/draft/filter/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__searchfilter_list_publish_filter_category_categoryId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/searchfilter/list/publish/filter/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_list_appInfo_appInfoFlag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/list/appInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_update_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_data_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_data_post() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_data_put() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_data_delete_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_data_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_data_post() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_data_put() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_data_delete_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_data_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_data_post() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_data_put() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_data_delete_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_data_put_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_data_del() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_data_pos() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_data_put() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_data_del_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_data_put_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_da() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_da_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_da_2() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_da_3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_da_4() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_da_5() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_2() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_4() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_5() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_6() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_7() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_8() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_9() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_10() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_11() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_12() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_13() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_14() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_15() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_16() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__surface_appdict_appDictFlag_appInfo_appInfoFlag_path0_path1_path2_path3_path4_pa_17() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/surface/appdict/test-id/appInfo/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__templateform_u2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/templateform")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__templateform_list() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/templateform/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__templateform_list_category() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/templateform/list/category")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__templateform_list_category_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/templateform/list/category")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__templateform_list_category_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/templateform/list/category/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__templateform_u2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/templateform/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__templateform_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/templateform/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__templateform_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/templateform/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__uuid_random() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/uuid/random")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__view_u2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/view")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__view_list_all() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/view/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__view_list_app_appId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/view/list/app/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__view_list_category_categoryId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/view/list/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__view_list_form_formId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/view/list/form/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__view_viewdata_list_id_next_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/view/viewdata/list/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__view_u2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/view/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__view_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__view_u2_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/view/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__view_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/view/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__view_id_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/view/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewcategory_u2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewcategory")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewcategory_list_all() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewcategory/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewcategory_list_category_categoryId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewcategory/list/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewcategory_list_view_viewId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewcategory/list/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewcategory_u2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewcategory/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewcategory_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewcategory/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewcategory_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewcategory/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewfieldconfig_u2_create() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewfieldconfig")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewfieldconfig_list_all() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewfieldconfig/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewfieldconfig_list_view_viewId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewfieldconfig/list/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewfieldconfig_u2_delete() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewfieldconfig/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewfieldconfig_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewfieldconfig/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewfieldconfig_u2_update() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewfieldconfig/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewfieldconfig_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewfieldconfig/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewfieldconfig_id_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewfieldconfig/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewrecord_document_docId_filter_list_id_next_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewrecord/document/test-id/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewrecord_document_docId_has_view() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewrecord/document/test-id/has/view")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewrecord_list_install_log_paging_page_size_size() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewrecord/list/install/log/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewrecord_by_person_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewrecord/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewrecord_unread_u3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewrecord/unread")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__viewrecord_unread_u3_1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/viewrecord/unread/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_cms_core_entity → cms_core_entity (6 routes) ──
    #[tokio::test]
    async fn parity__cms_core_entity__article_create() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/article/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_entity__article_list() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/article/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_entity__article_get() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/article/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_entity__category_create() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/category/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_entity__category_list() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/category/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_entity__category_get() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_cms_core_express → cms_core_express (2 routes) ──
    #[tokio::test]
    async fn parity__cms_core_express__content_detail() {
        let router = oa4rust::cms_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/core/express/content/detail/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_express__content_list() {
        let router = oa4rust::cms_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/core/express/content/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_component_assemble_control → component_assemble_control (14 routes) ──
    #[tokio::test]
    async fn parity__component_assemble_control__component_delete_all() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component/assemble/control/component/delete/all")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__component_delete_all_1() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component/assemble/control/component/delete/all")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__status_list() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component/assemble/control/status/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__create_component() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/component")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__delete_component() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/component/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__save_component() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/component/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__create_component_1() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/create/component")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__delete_component_1() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/delete/component")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__get_component() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/get/component/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__get_control_config() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/get/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__list_components() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/list/components")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__list_control_categories() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/list/control/categories")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__save_component_1() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/save/component")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__update_control_config() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/update/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_component_core_entity → component (3 routes) ──
    #[tokio::test]
    async fn parity__component__count() {
        let router = oa4rust::component::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component__list_all() {
        let router = oa4rust::component::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__component__get_component() {
        let router = oa4rust::component::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_console → console (7 routes) ──
    #[tokio::test]
    async fn parity__console__clear_cache() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/cache/clear/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__console__execute_command() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/command/execute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__console__get_logs() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/logs/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__console__get_metric() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/metric/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__console__send_message() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/send/message")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__console__get_status() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__console__get_system_info() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/system/info")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_correlation_core_entity → correlation (3 routes) ──
    #[tokio::test]
    async fn parity__correlation__list_cms_correlations() {
        let router = oa4rust::correlation::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/type/cms/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation__check_cms_readable() {
        let router = oa4rust::correlation::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/type/cms/readable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation__list_process_platform_correlations() {
        let router = oa4rust::correlation::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/type/processplatform/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_correlation_core_express → correlation_core_express (2 routes) ──
    #[tokio::test]
    async fn parity__correlation_core_express__get_status() {
        let router = oa4rust::correlation_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/core/express/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_core_express__sync_correlation() {
        let router = oa4rust::correlation_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/core/express/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_correlation_service_processing → correlation_service_processing (20 routes) ──
    #[tokio::test]
    async fn parity__correlation_service_processing__u2() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/delete/type/cms/document/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__u2_1() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/delete/type/processplatform/job/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__u2_2() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/list/type/cms/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__u2_3() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/list/type/cms/document/test-id/site/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__u2_4() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/list/type/processplatform/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__u2_5() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/list/type/processplatform/job/test-id/site/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__u2_6() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/readable/type/cms")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__u2_7() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/readable/type/processplatform")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_type_cms_document_document() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/type/cms/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_type_processplatform_job_job() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/type/processplatform/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__u2_8() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/update/type/cms/document/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__u2_9() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/update/type/processplatform/job/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__create_correlation() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__delete_correlation() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__link_service() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/link")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__get_link() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/link/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__list_correlations() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__save_correlation() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__unlink_service() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/unlink/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__get_correlation() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_file_assemble_control → file_assemble_control (166 routes) ──
    #[tokio::test]
    async fn parity__file_assemble_control__anonymous_file_id_download() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/file/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__anonymous_file_id_download_stream() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/file/test-id/download/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__anonymous_file_id_download_stream_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/file/test-id/download/stream")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_download_stream() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/download/test-id/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_list_editor_owner() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/list/editor/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_list_folder_folderId() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/list/folder/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_list_share_owner() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/list/share/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_list_top() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/list/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/update/callback/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_update_callback_callback() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/update/callback/callback/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/update/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_update() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/update/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate_2() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/upload/folder/callback/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate_3() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/upload/folder/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate_4() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/upload/folder/test-id/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_binary_base64() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/test-id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_download() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_download_stream_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/test-id/download/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_image_scale_scale_binary_base64() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/test-id/image/scale/test-id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_image_width_width_height_height_binary_base64() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/test-id/image/width/test-id/height/test-id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_attachment_update_content() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/test-id/update")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_attachment_update_content_callback() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/test-id/update/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_exist_file_fileMd5() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/exist/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_list_editor_owner() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/list/editor/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_list_filter_name() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/list/filter/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_list_folder_folderId() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/list/folder/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_list_share_owner() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/list/share/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_list_top() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/list/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_attachment2_list_type_page_size_size() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/list/type/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate_5() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/upload/folder/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_user_capacity() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/user/capacity")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_binary_base64() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/test-id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_download() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_download_image_width_width_height_height() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/test-id/download/image/width/test-id/height/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_download_stream() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/test-id/download/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_image_scale_scale_binary_base64() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/test-id/image/scale/test-id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_image_width_width_height_height_binary_base64() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/test-id/image/width/test-id/height/test-id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_office_preview_type_type() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment2/test-id/office/preview/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__complex_folder_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/complex/folder/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__complex_top() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/complex/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_config_save_system_config() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/config")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__config_is_file_manager() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/config/is/file/manager")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__config_system_config() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/config/system/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__editor_list() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/editor/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__anonymous_file_id_download_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/anonymous/file/id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_office_preview_type_type_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/attachment2/test-id/office/preview/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_clean_unused_referencetype_cmsdocument_manage() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/clean/unused/referencetype/cmsdocument/manage")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_copy_attachment_attachmentId_referencetype_referenceType_reference_referenc() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/copy/attachment/test-id/referencetype/test-id/reference/test-id/scale/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__create_file() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__delete_file() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__delete_file_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_file_list_reference_types() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/list/referencetype")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_referencetype_referenceType_reference_reference() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/list/referencetype/test-id/reference/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_unused_referencetype_cmsdocument_manage() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/list/unused/referencetype/cmsdocument/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__list_files() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_next_count() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_next_count_all() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/list/test-id/next/test-id/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_next_count_referencetype_referenceType() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/list/test-id/next/test-id/referencetype/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_prev_count() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_prev_count_all() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/list/test-id/prev/test-id/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_prev_count_referencetype_referenceType() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/list/test-id/prev/test-id/referencetype/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_file_delete_by_reference() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/referencetype/test-id/reference/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__upload_file() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_file_upload_octet_stream() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/upload/referencetype/test-id/reference/test-id/scale/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_file_upload_callback() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/upload/referencetype/test-id/reference/test-id/scale/test-id/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_upload_with_url() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/upload/with/url")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_file_delete_by_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__get_file() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_id_binary_base64() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/test-id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_id_download() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_id_download_stream() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/test-id/download/stream")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__update_control_config() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/update/control/config")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_binary_base64_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment/id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_download_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment/id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_image_scale_scale_binary_base64_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment/id/image/scale/scale/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_image_width_width_height_height_binary_base64_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment/id/image/width/width/height/height/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_list_folder_folderId_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment/list/folder/folderId")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_list_top_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment/list/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_exist_file_fileMd5_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/exist/file/fileMd5")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_binary_base64_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_download_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_download_image_width_width_height_height_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/id/download/image/width/width/height/height")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_download_stream_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/id/download/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_image_scale_scale_binary_base64_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/id/image/scale/scale/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_image_width_width_height_height_binary_base64_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/id/image/width/width/height/height/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_list_filter_name_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/list/filter/name")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_list_folder_folderId_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/list/folder/folderId")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_list_top_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/list/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_list_type_page_size_size() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/attachment2/list/type/page/size/size")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__complex_folder_id_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/complex/folder/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__complex_top_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/complex/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__create_file_entity() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/core/entity/file/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__delete_file_entity() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/core/entity/file/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__delete_file_entity_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/core/entity/file/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__update_file_entity() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/core/entity/file/update/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__update_file_entity_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/core/entity/file/update/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__editor_list_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/editor/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_clean_unused_referencetype_cmsdocument_manage_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/clean/unused/referencetype/cmsdocument/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_copy_attachment_attachmentId_referencetype_referenceType_reference_referenc_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/copy/attachment/attachmentId/referencetype/referenceType/reference/reference/scale/scale")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_id_binary_base64_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_id_download_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_next_count_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/id/next/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_next_count_all_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/id/next/count/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_next_count_referencetype_referenceType_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/id/next/count/referencetype/referenceType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_prev_count_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/id/prev/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_prev_count_all_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/id/prev/count/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_id_prev_count_referencetype_referenceType_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/id/prev/count/referencetype/referenceType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_referencetype() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/referencetype")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_referencetype_referenceType_reference_reference_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/referencetype/referenceType/reference/reference")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_list_unused_referencetype_cmsdocument_manage_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/list/unused/referencetype/cmsdocument/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_referencetype_referenceType_reference_reference() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/referencetype/referenceType/reference/reference")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder_list_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/list/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder_list_top() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/list/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder2_batch_download() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder2/batch/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder2_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder2/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder2_id_download() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder2/id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder2_list_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder2/list/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder2_list_top() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder2/list/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__recycle_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/recycle/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_download_share_shareId_file_fileId() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/share/download/share/shareId/file/fileId")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/share/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_id_password_password() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/share/id/password/password")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_list_att_share_shareId_folder_folderId() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/share/list/att/share/shareId/folder/folderId")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_list_folder_share_shareId_folder_folderId() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/share/list/folder/share/shareId/folder/folderId")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_share_shareId_file_fileId_folder_folderId() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/share/share/shareId/file/fileId/folder/folderId")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_shield_id() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/share/shield/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate_6() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/upload/referencetype/reference/reference/scale/scale/callback/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate_7() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/upload/referencetype/reference/reference/scale/scale/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_id_download_stream_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id/download/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_folder_create() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/folder")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder_list_top_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/folder/list/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder_list_id_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/folder/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder_id_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/folder/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_folder_create_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/folder2")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder2_batch_download_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/folder2/batch/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder2_list_top_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/folder2/list/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder2_list_id_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/folder2/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder2_id_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/folder2/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__folder2_id_download_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/folder2/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__recycle_id_delete() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/recycle/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate_8() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/recycle/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__recycle_empty() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/recycle/empty")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__recycle_list() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/recycle/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate_9() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/recycle/resume/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__recycle_id_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/recycle/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__recycle_id_delete_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/recycle/test-id/delete")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__recycle_id_resume() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/recycle/test-id/resume")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_share_create() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_download_share_shareId_file_fileId_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/download/share/test-id/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_list() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_list_att_share_shareId_folder_folderId_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/list/att/share/test-id/folder/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_list_folder_share_shareId_folder_folderId_1() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/list/folder/share/test-id/folder/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_list_my() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/list/my")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate_10() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/list/my2/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__share_list_to_me() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/list/to/me")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__crate_11() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/list/to/me2/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_share_save_to_folder() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/share/test-id/file/test-id/folder/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_share_shield() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/shield/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_share_get() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__u2_share_get_with_password() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/share/test-id/password/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_file_core_entity → file (7 routes) ──
    #[tokio::test]
    async fn parity__file__file_download() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file__folder_create() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file__folder_list_with_folder() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file__folder_remove() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/remove")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file__folder_update() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file__permission_set() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/permission/set")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__file__file_upload() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_general_assemble_control → general_assemble_control (95 routes) ──
    #[tokio::test]
    async fn parity__general_assemble_control__crate() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_1() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_2() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_3() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_4() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/list/province/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_5() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/list/province/test-id/city/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_6() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/list/province/test-id/city/test-id/district/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_7() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/update/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_8() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/update/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_9() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_10() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_11() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_12() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_13() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_14() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_15() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/save/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_16() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_17() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/ecnet/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_18() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/ecnet/check")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_19() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/excelName/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_20() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/excelName/test-id/sheetList")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_21() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/result/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_22() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_23() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/upload/with/url")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_24() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_25() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/test-id/sheetList")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_26() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_27() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_28() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/generalfile")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_29() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/generalfile/download/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_30() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/generalfile/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_31() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/generalfile/flag/test-id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_32() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_33() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_34() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_35() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_36() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/download/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_37() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_38() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_39() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_40() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/update/apply/status/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_41() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/update/apply/status/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_42() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/update/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_43() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/update/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_44() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_45() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/upload/for/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_46() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/upload/with/url")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_47() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/office")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_48() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/office/html/to/word")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_49() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/office/html/to/word/result/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_50() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/office/html/to/word/result/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_51() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/permissions/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_52() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/qrcode")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_53() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/qrcode/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_54() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/qrcode/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_55() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/qrcode/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_56() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/qrcode/width/test-id/height/test-id/text/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_57() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/qrcode/width/test-id/height/test-id/text/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_58() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/qrcode/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_59() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_60() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_61() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_62() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_63() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/enable")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_64() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/object")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_65() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/subject")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_66() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/system")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_67() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/update/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_68() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/update/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_69() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_70() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_71() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/status/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_72() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/status/update")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_73() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/upgrade/2021090901")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_74() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/upgrade/2021090902")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_75() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/between/holiday/count/start/test-id/end/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_76() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/between/minutes/start/test-id/end/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_77() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/betweenholidaycount/start/test-id/end/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_78() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/betweenminutes/start/test-id/end/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_79() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/forward/days/start/test-id/days/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_80() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/forward/minutes/start/test-id/minutes/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_81() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/forwarddays/start/test-id/days/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_82() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/forwardminutes/start/test-id/minutes/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_83() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/indefined/holiday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_84() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/indefined/workday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_85() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/indefinedholiday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_86() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/indefinedworkday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_87() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/is/holiday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_88() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/is/workday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_89() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/is/worktime/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_90() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/isholiday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_91() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/isworkday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_92() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/isworktime/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_93() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/minutes/of/workday")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_94() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/minutesofworkday")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_hotpic_assemble_control → hotpic_assemble_control (43 routes) ──
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_bbs_id() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/cipher/hotpic/bbs/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_bbs_id_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/cipher/hotpic/bbs/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_cms_id() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/cipher/hotpic/cms/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_cms_id_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/cipher/hotpic/cms/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_filter_list_page_page_count_count() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/cipher/hotpic/filter/list/page/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_filter_list_page_page_count_count_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/cipher/hotpic/filter/list/page/test-id/count/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_id() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/cipher/hotpic/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__get_control_config() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_control_applications() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/list/control/applications")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_control_panels() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/list/control/panels")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__update_control_config() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/update/control/config")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__create_hotpic() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_application_infoId() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_changeTitle() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/changeTitle")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_exists_check() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/exists/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_filter_list_page_page_count_count() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/filter/list/page/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_filter_list_page_page_count_count_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/filter/list/page/test-id/count/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_id() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_id_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_delete_by_ids() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__create_hotpic_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/create/hotpic")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__delete_hotpic() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/delete/hotpic")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__get_hotpic() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/get/hotpic/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_hotpics() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/list/hotpics")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__save_hotpic() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/save/hotpic")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_bbs_id_2() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/cipher/hotpic/bbs/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_cms_id_2() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/cipher/hotpic/cms/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_filter_list_page_page_count_count_2() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/cipher/hotpic/filter/list/page/page/count/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_id_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/cipher/hotpic/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__create_hotpic_2() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/create/hotpic")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__delete_hotpic_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/delete/hotpic")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__get_control_config_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/get/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__get_hotpic_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/get/hotpic/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_control_applications_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/list/control/applications")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_control_panels_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/list/control/panels")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_hotpics_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/list/hotpics")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__save_hotpic_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/save/hotpic")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__update_control_config_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/update/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_application_infoId_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/user/hotpic/application/infoId")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_changeTitle_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/user/hotpic/changeTitle")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_exists_check_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/user/hotpic/exists/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_filter_list_page_page_count_count_2() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/user/hotpic/filter/list/page/page/count/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_id_2() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/user/hotpic/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_hotpic_core_entity → hotpic (3 routes) ──
    #[tokio::test]
    async fn parity__hotpic__exists_check() {
        let router = oa4rust::hotpic::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/user/hotpic/exists/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic__list_by_application_and_info_id() {
        let router = oa4rust::hotpic::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/user/hotpic/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__hotpic__get_by_id() {
        let router = oa4rust::hotpic::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/user/hotpic/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_jpush_assemble_control → jpush_assemble_control (37 routes) ──
    #[tokio::test]
    async fn parity__jpush_assemble_control__get_control_config() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_admin_unbind_all_person() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/admin/unbind/all/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_bind() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/bind")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_check_deviceName_deviceType_pushType() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/check/deviceName/deviceType/pushType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_config_push_type() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/config/push/type")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_list_pushType() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_unbind_deviceName_deviceType() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/unbind/deviceName/deviceType")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_unbind_new_deviceName_deviceType_pushType() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/unbind/new/deviceName/deviceType/pushType")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__list_control_apps() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/list/control/apps")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__message_test_send() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/message/test/send")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__update_control_config() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/update/control/config")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__create_jpush() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__delete_jpush() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__get_jpush() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__list_jpushs() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__save_jpush() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__create_jpush_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/create/jpush")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__delete_jpush_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/delete/jpush")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_admin_unbind_all_person_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/admin/unbind/all/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_bind_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/bind")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_check_deviceName_deviceType_pushType_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/check/deviceName/deviceType/pushType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_check_deviceName_deviceType_pushType_2() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/check/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_config_push_type_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/config/push/type")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_list_pushType_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_unbind_deviceName_deviceType_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/unbind/deviceName/deviceType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_unbind_new_deviceName_deviceType_pushType_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/unbind/new/deviceName/deviceType/pushType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_unbind_new_deviceName_deviceType_pushType_2() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/unbind/new/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_unbind_deviceName_deviceType_2() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/unbind/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__get_control_config_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/get/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__get_jpush_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/get/jpush/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__list_control_apps_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/list/control/apps")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__list_jpushs_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/list/jpushs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__message_send() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/message/send")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__message_test_send_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/message/test/send")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__message_test_send_2() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/message/test/send")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__save_jpush_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/save/jpush")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__update_control_config_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/update/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_jpush_core_entity → jpush (6 routes) ──
    #[tokio::test]
    async fn parity__jpush__hello() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/hello/world")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush__device_create() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/device/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush__device_list() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/device/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush__device_get() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/device/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush__template_list() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/template/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__jpush__template_get() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/template/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_meeting_assemble_control → meeting_assemble_control (104 routes) ──
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/attachment/create/from/processplatform")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/attachment/list/meeting/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_2() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/attachment/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_3() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/attachment/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_4() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/attachment/meeting/test-id/upload/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_5() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/attachment/meeting/test-id/upload/test-id/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_6() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/attachment/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_7() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/attachment/test-id/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_8() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/attachment/test-id/update")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_9() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/attachment/test-id/update/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_10() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__building_list() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_11() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/completed/completed/allmeeting/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_12() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/completed/completed/room/room/meeting/meeting/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_13() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/completed/completed/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__building_list_like_pinyin_key() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/like/pinyin/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__building_list_like_key() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/like/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__building_list_pinyininitial_key() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/pinyininitial/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_14() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/start/test-id/completed/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_15() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/start/test-id/completed/test-id/allmeeting")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_16() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/start/test-id/completed/test-id/room/test-id/meeting/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__building_id() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_17() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_18() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/config")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__config_system_config() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/config/system/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_19() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/config/system/config/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__config_system_config_manage() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/config/system/config/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__create_meeting_control() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__delete_meeting_control() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_20() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/list/meeting/controls")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__list_meeting_controls() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__create_meeting() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_21() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/checkin/code/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__create_meeting_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__delete_meeting() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__delete_meeting_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_applied_completed() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/applied/completed")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_applied_processing() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/applied/processing")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_applied_wait() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/applied/wait")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_apply_page_size_size() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/apply/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_22() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/apply/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_coming_day_count() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/coming/day/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_23() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/coming/month/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_24() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/coming/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_25() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/forward/monthcount/all/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_26() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/forward/monthcount/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_27() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/forward/monthcount/test-id/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_28() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/invite/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_29() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/invite/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_invited_completed() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/invited/completed")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_invited_processing() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/invited/processing")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_invited_rejected() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/invited/rejected")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_invited_wait() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/invited/wait")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_30() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_wait_accept() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/wait/accept")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_wait_confirm() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/wait/confirm")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_year_year_month_month() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/year/test-id/month/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_year_year_month_month_all() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/year/test-id/month/test-id/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_year_year_month_month_day_day() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/year/test-id/month/test-id/day/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_year_year_month_month_day_day_all() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/year/test-id/month/test-id/day/test-id/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_31() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/year/test-id/month/test-id/day/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_32() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_33() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_34() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_35() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_36() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/test-id/size/test-id/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_37() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__save_meeting() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__save_meeting_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/save/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_38() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_accept() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/accept")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_accept_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/accept")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_add_invite() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/add/invite")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_add_invite_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/add/invite")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_39() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/checkin")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_checkin() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/checkin")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_40() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/checkin/code")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_confirm_allow() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/confirm/allow")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_confirm_allow_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/confirm/allow")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_confirm_deny() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/confirm/deny")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_confirm_deny_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/confirm/deny")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_delete_invite() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/delete/invite")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_delete_invite_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/delete/invite")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_delete_invite_2() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/delete/invite")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_manual_completed() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/manual/completed")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_manual_completed_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/manual/completed")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_modify_completedtime() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/modify/completedtime")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_modify_completedtime_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/modify/completedtime")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_modify_starttime() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/modify/starttime")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_modify_starttime_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/modify/starttime")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_reject() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/reject")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_reject_1() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/reject")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_41() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/openmeeting")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__openmeeting_list_room() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/openmeeting/list/room")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_42() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/room")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__room_list() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/room/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_43() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/room/list/like/pinyin/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_44() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/room/list/like/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_45() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/room/list/pinyininitial/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_46() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/room/photo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__room_id() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/room/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_47() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/room/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__crate_48() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/room/test-id/photo")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_meeting_core_entity → meeting (9 routes) ──
    #[tokio::test]
    async fn parity__meeting__building_list() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/building/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting__create_meeting() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting__list_meetings() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting__openmeeting_list_room() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/openmeeting/list/room")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting__room_list() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/room/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting__list_schedule() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/schedule/days/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting__add_participant() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/test-id/participant/add")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting__list_participants() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/test-id/participant/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__meeting__get_meeting() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_message_assemble_communicate → message_assemble_communicate (89 routes) ──
    #[tokio::test]
    async fn parity__message_assemble_communicate__connector_create() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/connector")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_list_consume_count_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/list/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_list_consume_currentperson_count_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/list/test-id/currentperson/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_list_consume_person_person_count_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/list/test-id/person/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_type_type() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_type_type_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/type/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_type_type_mockputtopost_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/type/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_id_type_type() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/test-id/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_id_type_type_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/test-id/type/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_business_businessId() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/business/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_list_my() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/list/my")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_list_with_person() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/list/with/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_list_with_person_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/list/with/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_update() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_group() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/group")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_group_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/group")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_group_mockdeletetoget() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/group/mockdeletetoget")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_group_mockdeletetoget_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/group/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_group_quit_self() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/group/quit/self")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_group_quit_self_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/group/quit/self")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_icon() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/icon")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_read() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/read")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_read_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/read")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_read_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/read/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_single_delete_virtual() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/single")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_single() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/single")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_single_delete_virtual_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/single/mockdeletetoget")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_single_delete_virtual_2() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/single/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_top_cancel() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/top/cancel")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_top_cancel_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/top/cancel")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_top_cancel_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/top/cancel/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_top_cancel_mockputtopost_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/top/cancel/mockputtopost")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_top_set() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/top/set")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_top_set_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/top/set")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_top_set_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/top/set/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_manager_config() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/manager/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_manager_config_post() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/manager/config")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_clear() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/clear")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_collection() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/collection")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_collection_list_page_size_size() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/collection/list/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_collection_list_page_size_size_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/collection/list/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_collection_remove() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/collection/remove")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_collection_remove_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/collection/remove")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_download_id() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_download_id_image_width_width_height_height() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/download/test-id/image/width/test-id/height/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_list_object() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/list/object")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_list_object_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/list/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_list_page_size_size() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/list/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_list_page_size_size_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/list/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_revoke_id() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/revoke/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_revoke_id_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/revoke/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_upload_conversationId_type_type() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/upload/test-id/type/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_currentperson_consumed() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/currentperson/consumed")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_currentperson_consumed_put() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/currentperson/consumed")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_currentperson_consumed_all() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/currentperson/consumed/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_currentperson_consumed_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/currentperson/consumed/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_consumed_count_count_asc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/consumed/count/test-id/asc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_consumed_count_count_desc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/consumed/count/test-id/desc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_count_count_asc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/count/test-id/asc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_count_count_desc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/count/test-id/desc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_noim_count_count_desc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/noim/count/test-id/desc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_not_consumed_count_count_asc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/not/consumed/count/test-id/asc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_not_consumed_count_count_desc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/not/consumed/count/test-id/desc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_id_next_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_id_prev_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mark_read() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mark_read/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_create() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_enable_type_get() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/enable/type")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_enable_type() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/enable/type")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_list_id_next_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_list_id_prev_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_id_mockdeletetoget() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_id() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_id_mockdeletetoget_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/test-id/mockdeletetoget")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_id_mockdeletetoget_2() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__message_custom_create() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/message/custom/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__message_list_paging_page_size_size() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/message/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__message_list_paging_page_size_size_1() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/message/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__receive_list() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/receive/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__send_message() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/send")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__ws_create() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/ws")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__ws_count_person() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/ws/count/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__ws_list_person() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/ws/list/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__ws_list_person_current_node() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/ws/list/person/current/node")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_message_core_entity → message (5 routes) ──
    #[tokio::test]
    async fn parity__message__consume_list() {
        let router = oa4rust::message::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/consume/list/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message__update_single() {
        let router = oa4rust::message::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/consume/test-id/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message__custom_create() {
        let router = oa4rust::message::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/custom/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message__mark_read() {
        let router = oa4rust::message::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/mark_read/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__message__unread_count() {
        let router = oa4rust::message::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/unread/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_mind_assemble_control → mind_assemble_control (27 routes) ──
    #[tokio::test]
    async fn parity__mind_assemble_control__get_control_config() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__update_control_config() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/config/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__folder_move_folderId() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/move/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__save_folder() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/save")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__list_folders() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/tree/my")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__get_folder() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__folder_id_force() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/test-id/force")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__update_folder() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/test-id/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_1() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_2() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/filter/recived/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_3() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/filter/recycle/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_4() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/filter/shared/test-id/next/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_5() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/list/test-id/shareRecords")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_6() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/list/test-id/version")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_7() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/recycle/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_8() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/restore/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_9() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/save")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_10() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/share/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_11() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/share/test-id/cancel")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_12() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/version/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_13() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_14() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_15() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/test-id/destorymind")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_16() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/test-id/destoryrecycle")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_17() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/test-id/icon")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__crate_18() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/mind/test-id/icon/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_mind_core_entity → mind (10 routes) ──
    #[tokio::test]
    async fn parity__mind__crate() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/folder")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_1() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/folder/tree/my")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_2() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/folder/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_3() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/folder/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_4() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/mind")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_5() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/mind/list/test-id/version")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_6() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/mind/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_7() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/mind/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_8() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/mind/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_9() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/version")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_organization_assemble_authentication → auth (47 routes) ──
    #[tokio::test]
    async fn parity__auth__andfx() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/andfx/moa/sso/token/test-id/enter/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__logout() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__whoami() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__login() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__bind() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/bind")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__bind_poll() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/bind/meta/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__captcha_default() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/captcha")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__captcha_with_size() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/captcha/width/test-id/height/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__check_token() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/check/token")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__code() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/code")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__code_send() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/code/credential/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__group_list() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/group/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__login_1() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/login")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__logout_1() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/logout")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/bind/name/test-id/code/test-id/redirecturi/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_1() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/dingding/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_2() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_3() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/login/dingding/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_4() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/login/name/test-id/code/test-id/redirecturi/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_5() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/login/qywx/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_6() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/name/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_7() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/qywx/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__oidc_authorize() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oidc/authorize")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__oidc_callback() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oidc/callback")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__refresh() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/refresh")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__role_list() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/role/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__safe_logout() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/safe/logout")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__sms_send_handler() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/sms/send")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__sms_verify_handler() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/sms/verify")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__sso() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/sso")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__sso_1() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/sso/client/test-id/token/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__sso_2() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/sso/encrypt")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__switch_user() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/switchuser")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__two_factor() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/two_factor")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__unit_list() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/unit/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__whoami_1() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/who")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__mpweixin_bind_code() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mpweixin/bind/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__mpweixin_bind_openid() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mpweixin/bind/openid/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__mpweixin_login() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mpweixin/login/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__mpweixin_test_send() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mpweixin/menu/test/send/to/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__qiyeweixin_login() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/qiyeweixin/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__qiyeweixin_jssdk_sign() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/qiyeweixin/jssdk/sign/info")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__qiyeweixin_update_person_detail() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/qiyeweixin/update/person/detail/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__verify() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/secret/captcha/verify")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__welink_login() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/welink/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__zwdingding_login() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/zhengwudingding/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__auth__zwdingding_info() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/zhengwudingding/info")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_organization_assemble_control → control (25 routes) ──
    #[tokio::test]
    async fn parity__control__group() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__group_1() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__group_2() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__group_3() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__group_4() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__group_5() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__person() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__person_1() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__person_2() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__person_3() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__person_4() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__person_5() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__role() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__role_1() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__role_2() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__role_3() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__role_4() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__role_5() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__unit() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_1() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_2() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_3() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_4() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_5() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_6() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_organization_assemble_express → organization_assemble_express (135 routes) ──
    #[tokio::test]
    async fn parity__organization_assemble_express__distinguishedname_list() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/distinguishedname/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__empower_list_identity_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/empower/list/identity/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__empowerlog_create() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/empowerlog")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_has_role() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/has/role")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_group_sub_direct() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/group/sub/direct")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_group_sub_direct_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/group/sub/direct/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_group_sub_nested() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/group/sub/nested")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_group_sub_nested_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/group/sub/nested/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_group_sup_direct() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/group/sup/direct")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_group_sup_direct_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/group/sup/direct/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_group_sup_nested() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/group/sup/nested")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_group_sup_nested_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/group/sup/nested/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_group_tree() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/group/tree")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_identity() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/identity")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_identity_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/identity/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_person() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__group_list_person_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/person/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_group() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/group")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_group_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/group/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_major_person() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/major/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_major_person_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/major/person/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_person() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_person_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/person/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_unit_person() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/unit/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_unit_person_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/unit/person/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_unit_sub_direct() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/unit/sub/direct")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_unit_sub_direct_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/unit/sub/direct/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_unit_sub_nested() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/unit/sub/nested")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__identity_list_unit_sub_nested_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/identity/list/unit/sub/nested/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__get_express_config() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/assemble/express/config/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__sync_organization_data() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/assemble/express/data/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__get_express_status() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/assemble/express/status/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__list_organization_units() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/assemble/express/units/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_auth_info_flag() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/auth/info/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_detail_flag() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/detail/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_has_role() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/has/role")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_all() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_all_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/all/object")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_filter_page_size() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/filter/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_group() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/group")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_group_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/group/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_identity() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/identity")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_identity_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/identity/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_login_after() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/login/after")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_login_after_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/login/after/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_login_recent() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/login/recent")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_login_recent_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/login/recent/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_pair_identity() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/pair/identity")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_person_sub_direct() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/person/sub/direct")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_person_sub_direct_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/person/sub/direct/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_person_sub_nested() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/person/sub/nested")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_person_sub_nested_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/person/sub/nested/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_person_sup_direct() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/person/sup/direct")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_person_sup_direct_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/person/sup/direct/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_person_sup_nested() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/person/sup/nested")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_person_sup_nested_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/person/sup/nested/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_personattribute() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/personattribute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_personattribute_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/personattribute/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_role() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/role")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_role_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/role/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_unit_sub_direct() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/unit/sub/direct")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_unit_sub_direct_like() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/unit/sub/direct/like")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_unit_sub_direct_like_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/unit/sub/direct/like/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_unit_sub_direct_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/unit/sub/direct/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_unit_sub_nested() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/unit/sub/nested")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_unit_sub_nested_like() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/unit/sub/nested/like")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_unit_sub_nested_like_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/unit/sub/nested/like/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_list_unit_sub_nested_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/unit/sub/nested/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_mobile_flag() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__person_nick_name_flag() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/nick/name/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__personattr_append_person_name() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/personattribute/append/person/name")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__personattr_list_attribute_person_name() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/personattribute/list/attribute/person/name")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__personattr_list_name_person() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/personattribute/list/name/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__personattr_list_person_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/personattribute/list/person/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__personattr_set_person_name() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/personattribute/set/person/name")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__role_list() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__role_list_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/list/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__role_list_person() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/list/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__role_list_person_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/list/person/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_check_unit_has_identity() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/check/unit/has/identity")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_check_unit_has_person() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/check/unit/has/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_check_unit_has_unit() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/check/unit/has/unit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_get_with_identity_with_level() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/identity/level")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_get_with_identity_with_level_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/identity/level/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_get_with_identity_with_type() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/identity/type")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_get_with_identity_with_type_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/identity/type/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_all() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_all_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/all/object")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_identity() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/identity")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_identity_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/identity/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_identity_sup_nested() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/identity/sup/nested")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_identity_sup_nested_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/identity/sup/nested/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_level() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/level")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_level_name_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/level/name/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_level_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/level/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_person() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_person_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/person/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_person_sup_nested() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/person/sup/nested")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_person_sup_nested_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/person/sup/nested/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_type_type_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/type/test-id/object")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_types() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/types")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_types_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/types/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unit_sub_direct() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unit/sub/direct")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unit_sub_direct_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unit/sub/direct/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unit_sub_nested() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unit/sub/nested")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unit_sub_nested_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unit/sub/nested/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unit_sup_direct() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unit/sup/direct")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unit_sup_direct_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unit/sup/direct/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unit_sup_nested() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unit/sup/nested")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unit_sup_nested_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unit/sup/nested/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unit_tree() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unit/tree")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unitattribute() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unitattribute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unitattribute_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unitattribute/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unitduty() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unitduty")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unit_list_unitduty_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/unitduty/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitattr_append_unit_name() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitattribute/append/unit/name")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitattr_list_attribute_unit_name() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitattribute/list/attribute/unit/name")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitattr_list_name_unit() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitattribute/list/name/unit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitattr_list_unit_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitattribute/list/unit/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitattr_set_unit_name() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitattribute/set/unit/name")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitduty_find_by_unit_name() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitduty/find/by/unit/name")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitduty_list_identity_unit_name() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitduty/list/identity/unit/name")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitduty_list_identity_unit_name_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitduty/list/identity/unit/name/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitduty_list_name() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitduty/list/name")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitduty_list_name_identity() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitduty/list/name/identity")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitduty_list_name_unit() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitduty/list/name/unit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__unitduty_list_unit_object() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unitduty/list/unit/object")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_organization_assemble_personal → personal (62 routes) ──
    #[tokio::test]
    async fn parity__personal__get_person() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__edit_person() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/custom/manager/person/test-id/name/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/custom/manager/person/test-id/name/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_2() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/custom/manager/person/test-id/name/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_3() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/custom/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_4() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/custom/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_5() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/custom/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_6() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/custom/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_7() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/custom/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_8() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/definition/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_9() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/definition/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_10() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/definition/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_11() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/definition/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_12() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/definition/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_13() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/definition/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_14() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empower/list/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_15() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empower/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_16() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empower/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__empower() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empower/manager/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__empower_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empower/manager/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__empower_2() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empower/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__empower_3() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empower/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_17() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empowerlog/list/currentperson/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_18() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empowerlog/list/to/currentperson/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_19() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empowerlog/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_20() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empowerlog/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_21() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empowerlog/manager/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_22() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/empowerlog/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_23() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/exmail")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_24() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/exmail")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_25() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/exmail/list/title/passive")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_26() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/exmail/new/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_27() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/exmail/new/count/passive")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_28() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/exmail/sso")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_29() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/icon")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_30() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/icon/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__icon() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/icon/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__icon_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/icon/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__edit_person_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__password() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/password")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__password_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/password/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__regist() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__auth() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/captcha/width/test-id/height/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__regist_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/check/email/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__regist_2() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/check/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__regist_3() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/check/name/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_31() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/check/password/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__regist_4() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/code")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_32() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/code/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_33() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/mode")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__signature() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/signature/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__signature_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/signature/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__u2_34() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/signature/list/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__signature_2() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/signature/manager/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__signature_3() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/signature/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__reset() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/reset")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__reset_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/reset/check/credential/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__reset_2() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/reset/check/password/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__reset_3() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/reset/code/credential/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__reset_4() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/reset/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__personal__reset_5() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/reset/password/anonymous")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_organization_core_entity → organization_core_entity (18 routes) ──
    #[tokio::test]
    async fn parity__organization_core_entity__bind_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/bind")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__bind_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/bind/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__bind_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/bind/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__custom_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/custom")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__custom_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/custom/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__custom_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/custom/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__definition_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/definition")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__definition_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/definition/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__definition_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/definition/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__group_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/group")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__group_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/group/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__group_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/group/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__identity_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/identity")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__identity_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/identity/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__identity_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/identity/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__person_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__person_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/person/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__person_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/person/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_organization_core_express → organization_core_express (3 routes) ──
    #[tokio::test]
    async fn parity__organization_core_express__get_config() {
        let router = oa4rust::organization_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/core/express/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_express__get_status() {
        let router = oa4rust::organization_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/core/express/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_express__sync_organization() {
        let router = oa4rust::organization_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/core/express/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_portal_assemble_designer → portal_assemble_designer (63 routes) ──
    #[tokio::test]
    async fn parity__portal_assemble_designer__create_design() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/dict/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_1() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/dict/list/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_2() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/dict/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_3() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/file/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_4() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/file/list/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_5() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/file/list/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_6() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/file/upload/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_7() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__get_design() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__list_designs() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_8() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/list/portal/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_9() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/output/select/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_10() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/output/select/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_11() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__create_page() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__delete_page() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__delete_page_1() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_12() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/list/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__list_pages_by_category() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__save_page() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__save_page_1() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/save/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_13() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__get_page() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_14() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_15() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/pageversion/list/page/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_16() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/pageversion/list/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_17() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/pageversion/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_18() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_19() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal/icon/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_20() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal/list/portalcategory/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_21() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal/list/summary")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_22() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal/list/summary/portalcategory/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_23() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal/list/summary/v2")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_24() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal/permission/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_25() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_26() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_27() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_28() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal/test-id/icon")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_29() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/portal/test-id/permission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__save_design() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__save_design_1() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/save/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_30() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/script/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_31() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/script/list/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_32() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/script/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_33() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/scriptversion/list/script/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_34() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/scriptversion/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_35() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/templatepage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_36() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/templatepage/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_37() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/templatepage/list/category")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_38() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/templatepage/list/category")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_39() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/templatepage/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_40() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/templatepage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_41() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/widget")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_42() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/widget/list/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_43() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/widget/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_44() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/widget/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_45() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/widget/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__crate_46() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__design_list() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/design/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__design_save() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/design/save")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__design_save_1() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/design/save")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__design_get() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/design/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_portal_assemble_surface → portal_assemble_surface (75 routes) ──
    #[tokio::test]
    async fn parity__portal_assemble_surface__create_surface() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__delete_layout() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/delete/layout")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/delete/layout")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_1() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/list/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_2() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/portal/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__dict_dictFlag_portal_portalFlag_path_data_mockdeletetoget() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/portal/path/data/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_3() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/portal/path/data/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_4() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/portal/path/data/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_5() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/portal/path/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_6() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/portal/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_7() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/test-id/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_8() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/test-id/portal/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_9() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/test-id/portal/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_10() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/test-id/portal/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_11() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/test-id/portal/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_12() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/test-id/portal/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_13() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/test-id/portal/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_14() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/dict/test-id/portal/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_15() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/file/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_16() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/file/list/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_17() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/file/portal/content/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_18() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/file/portal/download/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_19() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_20() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/file/test-id/portal/test-id/content")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_21() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/file/test-id/portal/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_22() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/get/layout")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__get_surface() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_23() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/list/layouts")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_24() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/list/portal/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__list_surfaces() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_25() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/mobile/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_26() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/page/list/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_27() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/page/v2/test-id/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_28() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/page/v2/test-id/portal/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_29() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/page/v2/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_30() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/page/v2/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_31() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/page/test-id/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_32() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/page/test-id/portal/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_33() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/page/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_34() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/portal/corner/mark/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_35() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/portal/icon/base64/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_36() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/portal/icon/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_37() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/portal/list/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_38() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/portal/mobile/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_39() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_40() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/portal/test-id/corner/mark")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_41() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/portal/test-id/icon")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_42() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/portal/test-id/icon/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_43() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/portal/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__preview_surface() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/preview/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__publish_surface() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/publish/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_44() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/save/layout")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__save_layout() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/save/layout")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_45() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/script/list/portal/portal")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_46() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/script/portal/portal/imported/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_47() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/script/portal/portal/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_48() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/script/portal/test-id/name/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_49() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/script/portal/test-id/name/test-id/imported")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_50() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/script/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_51() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/v2/mobile/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_52() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/v2/portal/mobile/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_53() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/v2/portal/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_54() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/v2/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_55() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/widget/list/portal/portal")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_56() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/widget/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_57() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/widget/portal/mobile/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_58() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/widget/portal/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_59() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/widget/test-id/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_60() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/widget/test-id/portal/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_61() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/widget/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_62() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/widget/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__crate_63() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__surface_list() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/surface/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__surface_publish() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/surface/publish")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__surface_preview() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/surface/test-id/preview")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_portal_core_entity → portal (7 routes) ──
    #[tokio::test]
    async fn parity__portal__dict_list() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/dict/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal__portal_list() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal__delete_page() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/page/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal__save_page() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/page/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal__get_page() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/page/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal__portal_id() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__portal__list_portal_category() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portalcategory/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_processplatform_assemble_bam → processplatform_assemble_bam (91 routes) ──
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__create_bam() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__delete_bam() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__delete_bam_1() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__get_bam_config() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__list_bams() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/application/by/process/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_1() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/application/process/by/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_2() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/application/process/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_3() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/application/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_4() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/by/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_5() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/application/by/process/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_6() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/application/process/by/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_7() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/application/process/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_8() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_9() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/by/application/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_10() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/task/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_11() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/task/application/by/process/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_12() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/task/application/process/activity/by/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_13() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/task/application/process/activity/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_14() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/task/application/process/by/activity/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_completed_task_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/task/applicationstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_15() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/task/by/application/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_completed_task_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/task/unitstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_16() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/task/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_completed_work_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/work/applicationstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_completed_work_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/work/unitstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_17() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/completed/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_completed_task_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/completed/task/application/test-id/process/test-id/activity/test-id/by/unit")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_completed_task_total() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/completed/task/application/test-id/process/test-id/activity/test-id/unit/test-id/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_completed_task_by_activity() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/completed/task/application/test-id/process/test-id/unit/test-id/person/test-id/by/activity")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_completed_task_by_process() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/completed/task/application/test-id/unit/test-id/person/test-id/by/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_completed_task_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/completed/task/unit/test-id/person/test-id/by/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_completed_work_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/completed/work/application/test-id/process/test-id/by/unit")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_completed_work_total() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/completed/work/application/test-id/process/test-id/unit/test-id/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_completed_work_by_process() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/completed/work/application/test-id/unit/test-id/person/test-id/by/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_completed_work_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/completed/work/unit/test-id/person/test-id/by/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_expired_task_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/expired/task/application/test-id/process/test-id/activity/test-id/by/unit")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_expired_task_total() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/expired/task/application/test-id/process/test-id/activity/test-id/unit/test-id/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_expired_task_by_activity() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/expired/task/application/test-id/process/test-id/unit/test-id/person/test-id/by/activity")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_expired_task_by_process() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/expired/task/application/test-id/unit/test-id/person/test-id/by/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_expired_task_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/expired/task/unit/test-id/person/test-id/by/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_expired_work_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/expired/work/application/test-id/process/test-id/by/unit")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_expired_work_total() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/expired/work/application/test-id/process/test-id/unit/test-id/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_expired_work_by_process() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/expired/work/application/test-id/unit/test-id/person/test-id/by/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_expired_work_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/expired/work/unit/test-id/person/test-id/by/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_start_task_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/start/task/application/test-id/process/test-id/activity/test-id/by/unit")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_start_task_total() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/start/task/application/test-id/process/test-id/activity/test-id/unit/test-id/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_start_task_by_activity() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/start/task/application/test-id/process/test-id/unit/test-id/person/test-id/by/activity")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_start_task_by_process() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/start/task/application/test-id/unit/test-id/person/test-id/by/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_start_task_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/start/task/unit/test-id/person/test-id/by/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_start_work_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/start/work/application/test-id/process/test-id/by/unit")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_start_work_total() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/start/work/application/test-id/process/test-id/unit/test-id/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_start_work_by_process() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/start/work/application/test-id/unit/test-id/person/test-id/by/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_count_start_work_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/count/start/work/unit/test-id/person/test-id/by/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_18() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/application/by/process/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_19() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/application/process/by/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_20() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/application/process/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_21() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_22() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/by/application/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_23() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/task/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_24() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/task/application/by/process/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_25() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/task/application/process/activity/by/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_26() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/task/application/process/activity/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_27() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/task/application/process/by/activity/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_expired_task_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/task/applicationstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_28() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/task/by/application/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_expired_task_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/task/unitstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_29() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/task/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_expired_work_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/work/applicationstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_expired_work_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/work/unitstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_30() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/expired/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_start_task_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/start/task/applicationstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_start_task_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/start/task/unitstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_start_work_by_application() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/start/work/applicationstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__bam_stubs_start_work_by_unit() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/start/work/unitstubs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_31() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/task/application/by/process/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_32() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/task/application/process/activity/by/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_33() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/task/application/process/activity/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_34() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/task/application/process/by/activity/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_35() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/task/application/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_36() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/task/by/application/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_37() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/task/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_38() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/period/list/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__state_applicationtstubs_trigger() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/state/applicationtstubs/trigger")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__state_category() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/state/category")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__state_category_trigger_all() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/state/category/trigger")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__state_organization() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/state/organization")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__state_running() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/state/running")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__state_summary() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/state/summary")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__crate_39() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/state/trigger/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__get_bam_status() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/status/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_processplatform_assemble_designer → processplatform_assemble_designer (114 routes) ──
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/application/icon/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_1() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/application/list/applicationcategory/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_2() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/application/list/summary/applicationcategory/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_3() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/application/permission/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_4() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_5() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/application/test-id/icon")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_6() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/application/test-id/permission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_7() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/application/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_8() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/applicationcategory/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_9() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/applicationdict")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_10() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/applicationdict/list/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_11() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/applicationdict/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_12() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/applicationdict/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_13() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/applicationdict/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__create_flow() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__delete_flow() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_14() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/elementtool/applicationdict/orphan")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_15() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/elementtool/form/orphan")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_16() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/elementtool/process/orphan")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_17() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/elementtool/script/orphan")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_18() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/file/application/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_19() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/file/content/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_20() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/file/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_21() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/file/list/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_22() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/file/list/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_23() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/file/upload/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_24() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_25() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/file/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_26() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/form/list/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_27() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/form/list/formfield/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_28() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/form/list/formfield/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_29() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/form/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_30() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/form/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_31() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/form/list/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_32() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/form/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_33() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/formversion/list/form/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_34() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/formversion/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__get_flow() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_35() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/item-access")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_36() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/item-access/bach/save")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_37() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/item-access/delete/process/test-id/path/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_38() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/item-access/path/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_39() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/item-access/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_40() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/item-access/process/test-id/path/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_41() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/item-access/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_42() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/item/access/delete/process/path/path/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_43() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/item/access/path/path")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_44() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/item/access/process/path/path/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__list_flows() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_45() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mapping")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_46() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mapping/execute/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_47() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mapping/list/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_48() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mapping/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_49() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mapping/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_50() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mapping/list/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_51() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mapping/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_52() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mapping/test-id/execute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_53() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mergeitemplan")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_54() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mergeitemplan/estimate")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_55() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mergeitemplan/list/application/paging/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_56() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mergeitemplan/list/application/test-id/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_57() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mergeitemplan/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_58() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mergeitemplan/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_59() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/mergeitemplan/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_60() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/output/select/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__preview_flow() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/preview/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_61() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/activity/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_62() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/application/disable/edition/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_63() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/application/edition/edition/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_64() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_65() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/application/test-id/disable/edition")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_66() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/application/test-id/edition/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_67() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/disable/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_68() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/edition/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_69() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/enable/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_70() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/enabled/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_71() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/execute/projection/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_72() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/form/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_73() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/lead/out/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_74() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/list/element/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_75() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/permission/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_76() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_77() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/upgrade/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_78() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/upgrade/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_79() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_80() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id/disable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_81() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id/enable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_82() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id/enabled")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_83() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id/execute/projection")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_84() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id/lead/out")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_85() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id/list/element")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_86() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id/permission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_87() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_88() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id/upgrade")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_89() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_90() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/process/test-id/test-id/edition")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_91() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/processversion/list/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_92() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/processversion/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__save_flow() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_93() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/script/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_94() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/script/application/test-id/name/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_95() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/script/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_96() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/script/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_97() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/script/list/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_98() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/script/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_99() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/scriptversion/list/script/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_100() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/scriptversion/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_101() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/templateform/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_102() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/templateform/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_103() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/workcompleted/application/merge/data/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_104() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/workcompleted/application/test-id/merge/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_105() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/workcompleted/process/merge/data/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_106() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/workcompleted/process/test-id/merge/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__crate_107() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_processplatform_assemble_surface → processplatform_assemble_surface (1066 routes) ──
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__anonymous_read_count_credential() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/anonymous/read/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__anonymous_read_count_credential_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/anonymous/read/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__anonymous_task_count_credential() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/anonymous/task/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__anonymous_task_count_credential_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/anonymous/task/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_flag_icon() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/icon/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_flag_is_manager() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/is/manager/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_list() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_list_complex() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/list/complex")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_list_complex_manage_person() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/list/complex/manage/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_list_complex_manage_person_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/list/complex/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_list_key_key() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/list/key/key")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_list_key_key_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/list/key/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_list_range() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/list/range")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_list_terminal_terminal() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/list/terminal/terminal")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_list_terminal_terminal_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/list/terminal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_flag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_flag_icon_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/test-id/icon")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_flag_is_manager_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/test-id/is/manager")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_flag_onlyRemoveNotCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__application_flag_onlyRemoveNotCompleted_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/application/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_data() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_data_mockd() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/data/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_data_mockp() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/data/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_data() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_data() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/data/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_data_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/data/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_data_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/data/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/data/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/data/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_4() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/data/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_5() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_6() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/data/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_7() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/data/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_8() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_9() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/path5/data/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_10() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/path5/data/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_11() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/path5/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_12() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/path5/path6/data/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_13() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/path5/path6/data/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_14() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/path5/path6/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_15() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/path5/path6/path7/data/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_16() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/path5/path6/path7/data/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_17() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/path0/path1/path2/path3/path4/path5/path6/path7/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/application/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_list_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/list/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_data_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_data_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_data_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_data_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_data_4() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_data_mockd_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_data_mockp_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_data_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_data_4() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_data_5() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_data_6() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_data_7() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_data_8() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_18() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_19() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_20() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_21() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_22() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_23() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_24() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_25() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_26() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_27() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_28() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_29() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_30() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_31() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_32() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_33() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_34() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_35() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_36() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_37() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_38() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_39() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_40() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_41() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_42() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_43() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_44() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_45() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_46() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_47() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_48() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_49() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_50() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_51() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_52() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__applicationdict_applicationDictFlag_application_applicationFlag_path0_path1_path_53() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/applicationdict/test-id/application/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_batch_delete_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/batch/delete/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_batch_download_zip() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/batch/download/job/test-id/site/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_batch_download_zip_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/batch/download/work/test-id/site/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_batch_download_zip_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/batch/download/work/test-id/site/test-id/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_batch_update_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/batch/update/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_batch_upload_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/batch/upload/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_copy_to_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/copy/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_copy_to_work_soft() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/copy/work/test-id/soft")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_copy_to_workcompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/copy/workcompleted/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_copy_to_workcompleted_soft() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/copy/workcompleted/test-id/soft")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_doc_to_word() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/doc/to/word/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_doc_to_word_wowc() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/doc/to/word/workorworkcompleted/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_invoice_download() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/invoice/test-id/joborworkorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_download_transfer() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/transfer/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_download_work_att() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/work/test-id/att/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_download_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_download_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_download_manage_stream() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id/manage/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_download_stream() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_download_by_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_download_by_work_stream() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id/work/test-id/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2c_download_work_stream_ext() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id/work/test-id/stream/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2c_download_work_ext() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id/work/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_download_by_workcompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id/workcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_download_by_workcompleted_stream() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id/workcompleted/test-id/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2c_download_wc_stream_ext() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id/workcompleted/test-id/stream/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2c_download_wc_ext() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/download/test-id/workcompleted/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_edit_by_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/edit/test-id/work/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_edit_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/edit/test-id/work/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_edit_text() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/edit/test-id/work/test-id/text")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_edit_text_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/edit/test-id/work/test-id/text/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_html_to_image() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/html/to/image")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_html_to_pdf() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/html/to/pdf")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_invoice_info() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/invoice/test-id/joborworkorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2_list_work_work_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/list/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2_list_workcompleted_work_completed_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/list/workcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2_list_workorworkcompleted_flag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/list/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_preview_image_result() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/preview/image/test-id/result")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_preview_pdf_result() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/preview/pdf/test-id/result")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_update_content() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/update/content/test-id/work/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_update_content_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/update/content/test-id/work/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_update_post() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/update/test-id/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_update_by_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/update/test-id/work/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_update_callback() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/update/test-id/work/test-id/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_update_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/update/test-id/work/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_upload_with_url() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/upload/with/url")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_upload_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/upload/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_upload_work_callback() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/upload/work/test-id/callback/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_upload_save_as() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/upload/work/test-id/save/as/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_upload_save_as_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/upload/work/test-id/save/as/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_upload_workcompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/upload/workcompleted/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_v2_upload_wowc() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/v2/upload/workorworkcompleted/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_v2_upload_base64() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/v2/upload/workorworkcompleted/test-id/base64")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2_id_available() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/available")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_id_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_online_info() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/online/info")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_preview_image_page() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/preview/image/page/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_preview_pdf() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/preview/pdf")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2_delete_by_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/work/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2_get_by_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_change_order_number() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/work/test-id/change/ordernumber/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_change_site() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/work/test-id/change/site/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_get_by_work_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/work/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2_text_by_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/work/test-id/text")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_delete_by_workcompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/workcompleted/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2_get_by_workcompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/workcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_u2b_get_by_wc_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/workcompleted/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__attachment_id_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/attachment/test-id/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__control_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/control/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__correlation_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/correlation/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__correlation_job_job_delete() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/correlation/job/job/delete")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__correlation_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/correlation/job/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__correlation_job_job_delete_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/correlation/job/test-id/delete")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__correlation_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/correlation/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__correlation_list_job_job_site_site() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/correlation/list/job/job/site/site")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__correlation_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/correlation/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__correlation_list_job_job_site_site_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/correlation/list/job/test-id/site/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__correlation_update_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/correlation/update/job/job")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__correlation_update_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/correlation/update/job/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__create_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_fetch_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/fetch/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_fetch_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/fetch/job/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_array_data() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/array/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2/path3")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2/path3/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2/path3/path4")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2/path3/path4/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2/path3/path4/path5")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2/path3/path4/path5/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_path6() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2/path3/path4/path5/path6")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_path6_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2/path3/path4/path5/path6/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_path6_path7() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2/path3/path4/path5/path6/path7")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_path6_path7_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/job/path0/path1/path2/path3/path4/path5/path6/path7/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_array_data_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/array/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_path6_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_path6_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_path6_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_path6_path7_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_path6_path7_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_job_job_path0_path1_path2_path3_path4_path5_path6_path7_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/job/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/path5/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/path5/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/path5/path6/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/path5/path6/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_path7_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/path5/path6/path7/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_path7_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/path5/path6/path7/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_path7() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/path5/path6/path7/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/path5/path6/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/path5/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/path4/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/path3/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/path2/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/path1/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/path0/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_mockdeletetoget_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_mockputtopost_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_mockdeletetoget_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_mockputtopost_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_mockputtopost_4() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_mockdeletetoget_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_mockputtopost_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_mockdeletetoget_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_mockputtopost_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_mockdeletetoget_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_mockputtopost_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_mockdeletetoget_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_mockputtopost_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_mockdeletetoget_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_mockputtopost_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_path7_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_path7_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_path7_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_path7_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_path7_mockdeletetoget_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_work_id_path0_path1_path2_path3_path4_path5_path6_path7_mockputtopost_3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/work/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_from_data() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/from/data/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_from_item() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/from/item/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/path3/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/path3/path4/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/path3/path4/path5/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_path6_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/path3/path4/path5/path6/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_path6_path7_mockputtop() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/path3/path4/path5/path6/path7/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_path6_path7() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/path3/path4/path5/path6/path7/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_path6() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/path3/path4/path5/path6/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/path3/path4/path5/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/path3/path4/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/path3/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/path2/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/path1/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/path0/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_from_data_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/from/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_from_item_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/from/item")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_path6_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_path6_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_path6_mockputtopost_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_path6_path7_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_path6_path7_mockputtop_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__data_workcompleted_id_path0_path1_path2_path3_path4_path5_path6_path7_mockputtop_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/data/workcompleted/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__datarecord_get_job_job_path_path() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/datarecord/get/job/job/path/path")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__datarecord_get_job_job_path_path_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/datarecord/get/job/test-id/path/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__datarecord_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/datarecord/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__datarecord_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/datarecord/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__delete_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__documentversion_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/documentversion/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__documentversion_list_job_job_category_category() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/documentversion/list/job/job/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__documentversion_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/documentversion/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__documentversion_list_job_job_category_category_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/documentversion/list/job/test-id/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__documentversion_list_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/documentversion/list/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__documentversion_list_workorworkcompleted_workOrWorkCompleted_category_category() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/documentversion/list/workorworkcompleted/test-id/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__documentversion_list_workorworkcompleted_workOrWorkCompleted_category_category_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/documentversion/list/workorworkcompleted/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__documentversion_work_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/documentversion/work/work")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__documentversion_work_work_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/documentversion/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__documentversion_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/documentversion/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_u2_save() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_list_my_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/list/my/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_list_my_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/list/my/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_list_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/list/next/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_list_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/list/prev/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_list_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_list_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_id_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_u2_save_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/process/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_id_start() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/start/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_id_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_id_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__draft_id_start_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/draft/test-id/start")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__file_flag_application_applicationFlag_content() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/file/application/content/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__file_flag_application_applicationFlag_download() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/file/application/download/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__file_list_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/file/list/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__file_flag_application_applicationFlag_content_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/file/test-id/application/test-id/content")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__file_flag_application_applicationFlag_download_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/file/test-id/application/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_flag_application_applicationFlag_mobile() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/application/mobile/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_flag_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/application/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_flag_mobile() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_v2_lookup_taskcompleted_taskcompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/v2/lookup/taskcompleted/taskcompleted")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_v2_lookup_taskcompleted_taskcompleted_mobile() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/v2/lookup/taskcompleted/taskcompleted/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_v2_lookup_taskcompleted_taskcompleted_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/v2/lookup/taskcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_v2_lookup_taskcompleted_taskcompleted_mobile_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/v2/lookup/taskcompleted/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_v2_lookup_workorworkcompleted_workOrWorkCompleted_mobile() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/v2/lookup/workorworkcompleted/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_v2_lookup_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/v2/lookup/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_v2_lookup_workorworkcompleted_workOrWorkCompleted_mobile_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/v2/lookup/workorworkcompleted/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_v2_id_mobile() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/v2/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_v2_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/v2/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_v2_id_mobile_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/v2/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_flag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_flag_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_flag_application_applicationFlag_mobile_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/test-id/application/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__form_flag_mobile_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/form/test-id/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__get_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__handover_u2_create() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/handover")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__handover_id_cancel() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/handover/cancel/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__handover_list_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/handover/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__handover_list_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/handover/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__handover_id_process() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/handover/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__handover_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/handover/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__handover_id_cancel_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/handover/test-id/cancel")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__handover_id_process_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/handover/test-id/process")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__job_job_allow_visit_person_person() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/job/job/allow/visit/person/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__job_job_find_work_workcompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/job/job/find/work/workcompleted")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__job_latest_work_workcompleted_serial_serial() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/job/latest/work/workcompleted/serial/serial")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__job_latest_work_workcompleted_serial_serial_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/job/latest/work/workcompleted/serial/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__job_v2_job_projection() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/job/v2/job/projection")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__job_v2_job_projection_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/job/v2/test-id/projection")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__job_job_allow_visit_person_person_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/job/test-id/allow/visit/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__job_job_find_work_workcompleted_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/job/test-id/find/work/workcompleted")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__keylock_lock() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/keylock/lock")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__keylock_u2_lock() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/keylock/lock")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__keylock_lock_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/keylock/lock/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__keylock_u2_lock_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/keylock/lock/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__list_surfaces() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__mode_clear_person_person_manager() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/mode/clear/person/person/manager")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__mode_clear_person_person_manager_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/mode/clear/person/test-id/manager")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__mode_id_delete() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/mode/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__mode_list() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/mode/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__mode_save() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/mode/save")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__mode_id_delete_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/mode/test-id/delete")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__openapi_get() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/openapi")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__preview_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/preview/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_activity_activity_activityType_activityType() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/activity/activity/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_activity_activity_activityType_activityType_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/activity/test-id/activityType/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_flag_allowrerouteto() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/allowrerouteto/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_flag_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/application/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_flag_complex() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/complex/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_list_application_applicationFlag_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/list/application/filter/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_list_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/list/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_list_application_applicationFlag_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/list/application/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_list_available_identity_process_flag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/list/available/identity/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_list_controllable_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/list/controllable/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_list_ids() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/list/ids")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_flag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_flag_allowrerouteto_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/test-id/allowrerouteto")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_flag_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_flag_complex_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/test-id/complex")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_flag_onlyRemoveNotCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__process_flag_onlyRemoveNotCompleted_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/process/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__publish_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/publish/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_count_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/count/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_count_credential() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_filter_attribute() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/filter/attribute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_filter_attribute_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/filter/attribute/filter")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_u2_filter_attribute_post() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/filter/attribute/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_count_application_applicationFlag_process() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/application/process/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_count_application() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/count/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_count_application_applicationFlag_process_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/count/application/test-id/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_date_date_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/date/date/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_date_date_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/date/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_filter_page_size_size_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/filter/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_filter_page_size_size_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/filter/test-id/size/test-id/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_my_filter_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/my/filter/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_my_filter_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/my/filter/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_my_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/my/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_my_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/my/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_next_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/next/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_next_count_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/next/filter/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_next_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/next/process/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/next/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_person_person_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/person/person/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_person_person_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/person/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_prev_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/prev/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_prev_count_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/prev/filter/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_prev_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/prev/process/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/prev/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_work_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/work/work")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_work_work_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_next_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/test-id/next/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_next_count_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/test-id/next/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_next_count_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/test-id/next/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_prev_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/test-id/prev/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_prev_count_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/test-id/prev/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_list_id_prev_count_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/list/test-id/prev/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_manage_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/manage/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_opinion_manage_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/opinion/manage/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_opinion_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/opinion/manage/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_processing_manage_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/processing/manage/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_processing_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/processing/manage/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_processing() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/processing/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_reference() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/reference/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_reset_manage_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/reset/manage/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_reset_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/reset/manage/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/count")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_create_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/create/next/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_create_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/create/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_create_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/create/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_create_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/create/prev/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_create_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/create/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_create_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/create/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/next/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/prev/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_v2_list_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/v2/list/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_work_workId() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_work_workId_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_workcompleted_workCompletedId() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/workcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_workcompleted_workCompletedId_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/workcompleted/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_count_credential_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/manage")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_manage_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/manage/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_opinion_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/opinion/manage")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_opinion_manage_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/opinion/manage/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_processing_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/processing")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_processing_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/processing/manage")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_processing_manage_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/processing/manage/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_reference_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/reference")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_reset_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/reset/manage")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__read_id_reset_manage_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/read/test-id/reset/manage/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_count_credential() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_filter_attribute() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/filter/attribute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_filter_attribute_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/filter/attribute/filter")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_u2_filter_attribute_post() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/filter/attribute/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_count_application_applicationFlag_process() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/application/process/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_count_application() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/count/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_count_application_applicationFlag_process_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/count/application/test-id/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_date_date_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/date/date/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_date_date_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/date/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_filter_page_size_size_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/filter/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_filter_page_size_size_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/filter/test-id/size/test-id/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_my_filter_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/my/filter/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_my_filter_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/my/filter/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_my_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/my/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_my_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/my/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_next_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/next/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_next_count_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/next/filter/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_next_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/next/process/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/next/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_prev_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/prev/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_prev_count_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/prev/filter/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_prev_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/prev/process/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/prev/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_work_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/work/work")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_work_work_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_next_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/test-id/next/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_next_count_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/test-id/next/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_next_count_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/test-id/next/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_prev_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/test-id/prev/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_prev_count_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/test-id/prev/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_list_id_prev_count_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/list/test-id/prev/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_id_manage_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/manage/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_id_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_id_opinion_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/opinion/manage/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_id_reference() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/reference/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/count")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_create_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/create/next/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_create_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/create/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_create_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/create/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_create_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/create/prev/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_create_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/create/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_create_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/create/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/next/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/prev/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_v2_list_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/v2/list/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_count_credential_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_id_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/test-id/manage")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_id_manage_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/test-id/manage/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_id_opinion_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/test-id/opinion/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readcompleted_id_reference_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readcompleted/test-id/reference")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readrecord_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readrecord/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readrecord_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readrecord/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__readrecord_list_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/readrecord/list/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_job_job_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/job/job/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_job_job_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/job/test-id/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_list_job_job_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/list/job/job/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_list_job_job_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/list/job/test-id/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_list_workorworkcompleted_workOrWorkCompleted_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/list/workorworkcompleted/paging/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_list_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/list/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_list_workorworkcompleted_workOrWorkCompleted_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/list/workorworkcompleted/test-id/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_id_manage_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/manage/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_id_manage_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/manage/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_id_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_id_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/test-id/manage")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_id_manage_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/test-id/manage")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_id_manage_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/test-id/manage/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__record_id_manage_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/record/test-id/manage/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_id_application_applicationFlag_manage_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/application/manage/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_id_application_applicationFlag_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/application/manage/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_count_application() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/count/application")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_count_person_credential() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/count/person/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_create_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/create/work")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_create_workcompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/create/workcompleted")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_filter_attribute() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/filter/attribute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_u2_filter_attribute_post() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/filter/attribute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_u2_filter_create_entry() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/filter/create/entry")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_filter_create_entry() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/filter/create/entry")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_filter_entry() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/filter/entry")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_count_person_credential_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/person/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/count")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_create_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/create/next/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_create_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/create/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_create_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/create/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_create_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/create/prev/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_create_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/create/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_create_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/create/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/next/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_paging_page_size_size_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/paging/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_paging_page_size_size_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/paging/test-id/size/test-id/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/prev/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_list_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/list/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_v2_search() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/search")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_u2_v2_search() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/v2/search")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_id_application_applicationFlag_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/test-id/application/test-id/manage")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__review_id_application_applicationFlag_manage_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/review/test-id/application/test-id/manage/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__route_list() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/route/list")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__route_list_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/route/list/mockputtopost")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__route_u2_list_by_ids() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/route/list/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__route_id_selectconfig() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/route/selectconfig/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__route_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/route/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__route_id_selectconfig_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/route/test-id/selectconfig")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__save_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__script_flag_application_applicationFlag_imported() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/script/application/imported/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__script_flag_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/script/application/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__script_flag_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/script/test-id/application/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__script_flag_application_applicationFlag_imported_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/script/test-id/application/test-id/imported")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_u2_create() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_generate_process_processId_name_name_serial() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/generate/process/name/name/serial/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_u2_generate() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/generate/process/test-id/name/test-id/serial")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_list_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/list/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_list_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_list_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_id_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_id_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_id_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_id_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_id_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__serialnumber_id_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/serialnumber/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__service_work_id_touch_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/service/work/touch/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__service_work_id_touch() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/service/work/touch/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__service_work_id_touch_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/service/work/test-id/touch")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__service_work_id_touch_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/service/work/test-id/touch/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_download_scrawlId() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_id_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_save_task_taskId() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/save/task/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_task_taskId_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/task/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_task_taskId() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/task/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_task_taskId_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/task/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_task_taskId_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/task/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_id_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__sign_id_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/sign/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_manage_filter_paging() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/filter/test-id/size/test-id/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_list_my_filter_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/my/filter/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_list_my_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/my/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_manage_app_paging_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/paging/test-id/size/test-id/application/test-id/filter/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_list_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_list_id_next_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/test-id/next/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_manage_next_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/test-id/next/test-id/filter/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_list_next_count_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/test-id/next/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_list_id_next_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/test-id/next/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_list_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_list_id_prev_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/test-id/prev/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_manage_prev_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/test-id/prev/test-id/filter/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_list_prev_count_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/test-id/prev/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_list_id_prev_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/list/test-id/prev/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_upload() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_work_type_abandoned() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/work/test-id/type/abandoned")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_work_type_snap() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/work/test-id/type/snap")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_work_type_suspend() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/work/test-id/type/suspend")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_workcompleted_type_abandonedworkcompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/workcompleted/test-id/type/abandonedworkcompleted")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_workcompleted_type_snapworkcompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/workcompleted/test-id/type/snapworkcompleted")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_delete() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_get() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_download() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_id_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__snap_u2_restore() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/snap/test-id/restore")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_count_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/count/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_count_credential() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_filter_attribute() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/filter/attribute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_filter_attribute_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/filter/attribute/filter")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_u2_filter_attribute_post() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/filter/attribute/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_count_application_applicationFlag_process() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/application/process/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_count_application() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/count/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_count_application_applicationFlag_process_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/count/application/test-id/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_date_date_hour_hour_exclude_draft_isExcludeDraft_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/date/date/hour/hour/exclude/draft/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_date_hour_exclude_draft_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/date/test-id/hour/test-id/exclude/draft/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_filter_page_size_size_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/filter/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_filter_page_size_size_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/filter/test-id/size/test-id/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_my_filter_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/my/filter/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_my_filter_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/my/filter/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_my_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/my/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_my_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/my/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/next/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count_filter_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/next/filter/manage/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/next/filter/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/next/manage/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/next/process/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/next/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_person_person_exclude_draft_isExcludeDraft_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/person/person/exclude/draft/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_person_exclude_draft_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/person/test-id/exclude/draft/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/prev/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count_filter_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/prev/filter/manage/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/prev/filter/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/prev/manage/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/prev/process/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/prev/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_work_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/work/work")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_work_work_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/next/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/next/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count_filter_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/next/test-id/filter/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/next/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_next_count_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/next/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/prev/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/prev/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count_filter_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/prev/test-id/filter/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/prev/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_list_id_prev_count_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/list/test-id/prev/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_manage_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/manage/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_opinion_manage_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/opinion/manage/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_opinion_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/opinion/manage/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_press_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/press/manage/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_processing_manage_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/processing/manage/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_processing_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/processing/manage/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_processing_neural() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/processing/neural/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_processing() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/processing/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_reference() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/reference/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_reset_manage_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/reset/manage/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_reset_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/reset/manage/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/count")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_create_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/create/next/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_create_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/create/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_create_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/create/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_create_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/create/prev/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_create_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/create/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_create_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/create/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/next/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/prev/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_list_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/list/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_id_pause() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/pause/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_id_reset_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/reset/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_id_reset() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/reset/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_id_resume() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/resume/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_id_trigger_processing() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/trigger/processing/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_id_pause_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/test-id/pause")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_id_reset_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/test-id/reset")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_id_reset_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/test-id/reset/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_id_resume_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/test-id/resume")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v2_id_trigger_processing_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v2/test-id/trigger/processing")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v3_id_add() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v3/add/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v3_id_pin() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v3/pin/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v3_id_add_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v3/test-id/add")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_v3_id_pin_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/v3/test-id/pin")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_will() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/will/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_count_credential_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/manage")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_manage_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/manage/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_opinion_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/opinion/manage")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_opinion_manage_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/opinion/manage/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_press_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/press/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_processing_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/processing")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_processing_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/processing/manage")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_processing_manage_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/processing/manage/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_processing_neural_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/processing/neural")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_reference_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/reference")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_reset_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/reset/manage")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_reset_manage_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/reset/manage/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__task_id_will_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/task/test-id/will")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_count_credential() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_filter_attribute() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/filter/attribute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_filter_attribute_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/filter/attribute/filter")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_u2_filter_attribute_post() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/filter/attribute/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_count_application_applicationFlag_process() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/application/process/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_count_application() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/count/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_count_application_applicationFlag_process_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/count/application/test-id/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_date_date_hour_hour_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/date/date/hour/hour/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_date_date_hour_hour_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/date/test-id/hour/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_filter_page_size_size_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/filter/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_filter_page_size_size_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/filter/test-id/size/test-id/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_my_filter_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/my/filter/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_my_filter_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/my/filter/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_my_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/my/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_my_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/my/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_next_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/next/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_next_count_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/next/filter/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_next_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/next/process/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/next/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_prev_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/prev/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_prev_count_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/prev/filter/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_prev_manual_flag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/prev/manual/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_prev_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/prev/process/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/prev/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_work_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/work/work")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_work_work_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_next_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/test-id/next/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_next_count_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/test-id/next/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_next_count_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/test-id/next/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_prev_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/test-id/prev/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_prev_count_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/test-id/prev/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_list_id_prev_count_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/list/test-id/prev/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_manage_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/manage/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_opinion_manage_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/opinion/manage/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_opinion_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/opinion/manage/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_press_work_work() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/press/work/work")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_press_work_work_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/press/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_reference_control() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/reference/control/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_reference() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/reference/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/count")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_create_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/create/next/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_create_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/create/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_create_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/create/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_create_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/create/prev/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_create_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/create/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_create_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/create/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/next/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/prev/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_v2_list_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/v2/list/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_count_credential_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/test-id/manage")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_manage_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/test-id/manage/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_opinion_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/test-id/opinion/manage")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_opinion_manage_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/test-id/opinion/manage/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_reference_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/test-id/reference")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__taskcompleted_id_reference_control_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/taskcompleted/test-id/reference/control")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__touch_expire() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/touch/expire")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__touch_passexpired() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/touch/passexpired")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__touch_touchdetained() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/touch/touchdetained")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_application_applicationFlag_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/application/process/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_application_applicationFlag_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/application/test-id/process/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_count_credential_application_appId() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_assignment_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/assignment/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_close_check() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/close/check/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_count_credential() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_count_credential_application_appId_u2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/count/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_filter_attribute_application_applicationFlag_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/filter/attribute/application/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_filter_attribute_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/filter/attribute/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_filter_attribute_application_applicationFlag_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/filter/attribute/application/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_count_application_applicationFlag_process_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/application/process/manage/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_count_application_applicationFlag_process() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/application/process/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_count_application() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/count/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_count_application_applicationFlag_process_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/count/application/test-id/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_count_application_applicationFlag_process_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/count/application/test-id/process/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_filter_page_size_size_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/filter/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_filter_page_size_size_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/filter/test-id/size/test-id/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_my_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/my/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_my_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/my/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_application_applicationFlag_filter_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/next/application/filter/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_application_applicationFlag_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/next/application/filter/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_application_applicationFlag_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/next/application/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/next/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_creator_current_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/next/creator/current/filter/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_creator_current() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/next/creator/current/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/next/process/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_paging_page_size_size_application_applicationFlag_filter_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/paging/application/filter/manage/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_paging_page_size_size_application_applicationFlag_filter_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/paging/test-id/size/test-id/application/test-id/filter/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_application_applicationFlag_filter_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/prev/application/filter/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_application_applicationFlag_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/prev/application/filter/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_application_applicationFlag_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/prev/application/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/prev/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_creator_current_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/prev/creator/current/filter/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_creator_current() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/prev/creator/current/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/prev/process/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/next/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_application_applicationFlag_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/next/test-id/application/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_application_applicationFlag_filter_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/next/test-id/application/test-id/filter/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_application_applicationFlag_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/next/test-id/application/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_creator_current_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/next/test-id/creator/current")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_creator_current_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/next/test-id/creator/current/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_next_count_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/next/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/prev/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_application_applicationFlag_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/prev/test-id/application/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_application_applicationFlag_filter_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/prev/test-id/application/test-id/filter/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_application_applicationFlag_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/prev/test-id/application/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_creator_current_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/prev/test-id/creator/current")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_creator_current_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/prev/test-id/creator/current/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_list_id_prev_count_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/list/test-id/prev/test-id/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_process_processFlag_force() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/process/force/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/process/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_process_processFlag_force_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/process/test-id/force")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_processing_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/processing/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_processing() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/processing/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_projection() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/projection/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_refer() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/refer/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_relative_manage_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/relative/manage/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_relative_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/relative/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_single_manage_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/single/manage/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_single_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/single/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_add_split_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/add/split/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_add_split() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/add/split/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_list() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_list_id_activity_goback() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/list/activity/goback/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_list_id_next_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/list/next/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_list_paging_page_size_size() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_list_paging_page_size_size_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_list_id_prev_count() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/list/prev/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_list_id_activity_goback_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/list/test-id/activity/goback")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_list_id_next_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/list/test-id/next/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_list_id_prev_count_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/list/test-id/prev/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_reroute_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/reroute/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_reroute() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/reroute/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_retract_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/retract/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_retract() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/retract/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_rollback_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/rollback/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_rollback() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/rollback/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_terminate_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/terminate/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_terminate() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/terminate/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_trigger_processing() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/trigger/processing/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_add_split_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/test-id/add/split")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_add_split_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/test-id/add/split/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_reroute_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/test-id/reroute")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_reroute_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/test-id/reroute/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_retract_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/test-id/retract")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_retract_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/test-id/retract/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_rollback_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/test-id/rollback")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_rollback_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/test-id/rollback/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_terminate_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/test-id/terminate")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_terminate_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/test-id/terminate/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v2_id_trigger_processing_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v2/test-id/trigger/processing")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v3_retract() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v3/retract")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_u2_v3_retract() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v3/retract")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v3_retract_stage_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v3/retract/stage/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v3_retract_stage_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v3/retract/stage/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v3_workorworkcompleted_workOrWorkCompleted_permission() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v3/workorworkcompleted/permission/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_v3_workorworkcompleted_workOrWorkCompleted_permission_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/v3/workorworkcompleted/test-id/permission")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_count_credential_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_assignment_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/assignment/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_close_check_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/close/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_close_check_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/close/check")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_processing_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/processing")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_processing_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/processing/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_projection_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/projection")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_refer_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/refer")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_relative_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/relative/manage")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_relative_manage_2() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/relative/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_relative_manage_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/relative/manage/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_single_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/single/manage")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__work_id_single_manage_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/work/test-id/single/manage/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_id_assignment_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/assignment/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_id_delete_manage_mockdeletetoget() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/delete/manage/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_id_delete_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/delete/manage/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_filter_attribute_application_applicationFlag_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/filter/attribute/application/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_filter_attribute_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/filter/attribute/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_filter_attribute_application_applicationFlag_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/filter/attribute/application/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_filter_list_id_prev_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/filter/list/prev/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_filter_list_id_prev_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/filter/list/test-id/prev/test-id/application/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_count_application_applicationFlag_process_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/application/process/manage/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_count_application_applicationFlag_process() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/application/process/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_count_application() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/count/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_count_application_applicationFlag_process_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/count/application/test-id/process")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_count_application_applicationFlag_process_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/count/application/test-id/process/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_filter_page_size_size_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/filter/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_filter_page_size_size_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/filter/test-id/size/test-id/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_next_count_application_applicationFlag_filter_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/next/application/filter/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_next_count_application_applicationFlag_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/next/application/filter/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_next_count_application_applicationFlag_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/next/application/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_next_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/next/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_paging_page_size_size_application_applicationFlag_filter_mana() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/paging/application/filter/manage/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_paging_page_size_size_application_applicationFlag_filter_mana_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/paging/test-id/size/test-id/application/test-id/filter/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_prev_count_application_applicationFlag_filter() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/prev/application/filter/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_prev_count_application_applicationFlag_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/prev/application/manage/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_prev_count_application_applicationFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/prev/application/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_next_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/test-id/next/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_next_count_application_applicationFlag_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/test-id/next/test-id/application/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_next_count_application_applicationFlag_filter_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/test-id/next/test-id/application/test-id/filter/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_next_count_application_applicationFlag_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/test-id/next/test-id/application/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_prev_count_application_applicationFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/test-id/prev/test-id/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_prev_count_application_applicationFlag_filter_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/test-id/prev/test-id/application/test-id/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_list_id_prev_count_application_applicationFlag_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/list/test-id/prev/test-id/application/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_id_manage() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/manage/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_process_processFlag() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_process_processFlag_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/process/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_flag_rollback_mockputtopost() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/rollback/mockputtopost/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_flag_rollback() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/rollback/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_shift_time() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/shift/time")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_u2_shift_time() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/shift/time")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_flag_rollback_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/test-id/rollback")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_flag_rollback_mockputtopost_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/test-id/rollback/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_id() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_id_assignment_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/test-id/assignment/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_id_delete_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/test-id/delete/manage")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_id_delete_manage_mockdeletetoget_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/test-id/delete/manage/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__workcompleted_id_manage_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/workcompleted/test-id/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__worklog_list_add_split_work_workId() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/worklog/list/add/split/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__worklog_list_add_split_work_workId_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/worklog/list/add/split/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__worklog_list_job_job() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/worklog/list/job/job")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__worklog_list_job_job_1() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/worklog/list/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__worklog_list_rollback_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/worklog/list/rollback/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__worklog_list_workorworkcompleted_workOrWorkCompleted() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/worklog/list/workorworkcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_processplatform_core_entity → processplatform_core_entity (6 routes) ──
    #[tokio::test]
    async fn parity__processplatform_core_entity__task_list() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/task/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_entity__task_get() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/task/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_entity__ticket_list() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/ticket/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_entity__work_list() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/work/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_entity__work_get() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_entity__workcompleted_list() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/workcompleted/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_processplatform_core_express → processplatform_core_express (6 routes) ──
    #[tokio::test]
    async fn parity__processplatform_core_express__task_count_with_person() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/task/count/with/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_express__task_processing() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/task/processing/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_express__work_count_with_person() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/work/count/with/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_express__work_processing() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/work/processing/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_express__work_retract() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/work/retract/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_express__work_terminate() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/work/terminate/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_processplatform_service_processing → processplatform_service_processing (199 routes) ──
    #[tokio::test]
    async fn parity__processplatform_service_processing__gateway_join() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/gateway/test-id/test-id/join")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/path0/data/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_1() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/path0/path1/data/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_2() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/path0/path1/path2/data/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_3() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/path0/path1/path2/path3/data/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_4() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/path0/path1/path2/path3/path4/data/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_5() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/path0/path1/path2/path3/path4/path5/data/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_6() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/path0/path1/path2/path3/path4/path5/path6/data/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_7() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/path0/path1/path2/path3/path4/path5/path6/path7/data/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_8() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_1() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_2() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_3() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_4() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_5() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_6() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_7() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_8() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/applicationdict/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/test-id/data")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_9() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/attachment/copy/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_9() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/attachment/copy/workcompleted/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_10() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/attachment/copy/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_11() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/attachment/edit/text/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_10() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/attachment/edit/test-id/text")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_12() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/attachment/workcompleted/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_11() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/attachment/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_13() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/attachment/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_12() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/attachment/test-id/work/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_13() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/attachment/test-id/workcompleted/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_14() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/attachment/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__cancel_process_instance() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/cancel/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__create_process() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_15() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/delete/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_14() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/job/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_15() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/job/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_16() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/path/delete/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_17() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/path/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_16() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_17() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/work/test-id/delete")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_18() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/work/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_19() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/work/test-id/test-id/delete")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_18() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/workcompleted/path/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_19() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/workcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_20() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/workcompleted/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_21() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/workcompleted/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_20() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/data/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_22() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/documentversion/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_21() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/documentversion/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_22() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/draft/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_23() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/event/add/update/table")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__execute_process() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/execute/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_24() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/form/suitable/activity/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__gateway_fork() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/gateway/fork/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__get_process() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__get_process_instance() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/instance/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_23() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/job/v2/test-id/person/test-id/view")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_25() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/job/v2/test-id/projection")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_24() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/job/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__list_processes() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_26() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/manual/after/processing/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_27() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/manual/append/identity/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__process_id_complex() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/process/test-id/complex")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_28() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/process/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_29() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/process/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_30() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/projection/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_25() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/read/work/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_26() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/read/workcompleted/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_27() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/read/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_28() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/read/test-id/processing")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_29() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/read/test-id/replace")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_30() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/read/test-id/reset")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_31() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/readcompleted/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_31() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/readcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_32() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/record/job/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_32() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/record/processing/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_33() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/record/task/processing")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_34() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/record/terminate/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_33() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/record/work/processing")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_34() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/record/work/terminate")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_35() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/record/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_35() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/record/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_36() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/record/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_36() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/review/create/work")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_37() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/review/create/workcompleted")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_37() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/review/init/review")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_38() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/review/init/review")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_39() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/review/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_38() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/review/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_39() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/series/series/activitytoken/processing/signal/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_40() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/service/touch/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_41() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/service/work/test-id/touch")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_42() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/abandoned/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_43() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/restore/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_44() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/snap/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_45() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/suspend/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_46() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_40() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/work/test-id/type/abandoned")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_41() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/work/test-id/type/snap")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_42() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/work/test-id/type/suspend")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_47() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/workcompleted/abandonedworkcompleted/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_48() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/workcompleted/snapworkcompleted/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_43() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/workcompleted/test-id/type/abandonedworkcompleted")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_44() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/workcompleted/test-id/type/snapworkcompleted")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_45() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_49() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_50() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/snap/test-id/restore")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_51() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/expire/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_52() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/pass/expired/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_53() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/press/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_54() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/processing/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_55() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/replace/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_56() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/urge/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_57() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/v2/pause/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_58() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/v2/reset/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_59() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/v2/resume/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_60() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/v2/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_61() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/v2/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_62() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/v2/test-id/pause")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_63() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/v2/test-id/reset")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_64() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/v2/test-id/resume")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_65() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/v3/add/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_66() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/v3/test-id/add")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_67() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/will/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_46() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_68() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_69() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/test-id/expire")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_70() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/test-id/pass/expired")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_71() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/test-id/press")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_72() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/test-id/processing")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_73() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/test-id/replace")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_74() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/test-id/urge")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_75() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/task/test-id/will")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_47() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/taskcompleted/next/task/identity")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_76() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/taskcompleted/press/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_77() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/taskcompleted/task/identity/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_48() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/taskcompleted/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_78() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/taskcompleted/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_49() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/taskcompleted/test-id/press/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__start_timer() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/timer/start")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__cancel_timer() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/timer/test-id/cancel")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_50() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/cleanevent")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_51() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/deletedraft")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_79() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/deletedraft")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_52() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/handoverjob")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_80() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/handoverjob")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_53() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/loglongdetained")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_54() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/merge")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_81() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/merge")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_55() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/mergeitem")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_82() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/mergeitem")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_56() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/touchdelay")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_83() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/touchdelay")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_57() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/urge")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_84() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/touch/urge")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_85() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/v2/add/split/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_86() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/v2/goback/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_87() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/v2/projection/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_88() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/v2/reroute/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_89() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/v2/rollback/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_90() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/v2/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_91() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/v3/retract/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_58() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_list() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_59() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/manual/after/processing")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_60() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/process/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_61() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/process/test-id/name/test-id/serial")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_62() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/v2/test-id/add/manual/task/identity/matrix")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_92() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/v2/test-id/add/split")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_93() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/v2/test-id/goback")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_94() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/v2/test-id/reroute")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_95() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/v2/test-id/retract")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_96() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/v2/test-id/rollback")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_97() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/v2/test-id/terminate")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_63() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/v3/retract")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_64() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_98() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/test-id/add/split")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_65() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/test-id/draft")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_99() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/test-id/manual/append/identity")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_100() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/test-id/processing")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_101() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/test-id/projection")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_66() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/test-id/series/test-id/activitytoken/test-id/processing/signal")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_102() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/workcompleted/merge/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_103() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/workcompleted/process/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_67() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/workcompleted/process/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_104() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/workcompleted/rollback/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_105() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/workcompleted/shift/time")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_68() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/workcompleted/shift/time")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_69() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/workcompleted/test-id/merge")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__u2_70() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/workcompleted/test-id/rollback")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__crate_106() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__task_claim() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/task/test-id/claim")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__task_complete() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/task/test-id/complete")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__task_reject() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/task/test-id/reject")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__task_transfer() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/task/test-id/transfer/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_complete() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/work/test-id/complete")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_id_processing() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/work/test-id/processing")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_v2_id_retract() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/work/test-id/retract")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_start() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/work/test-id/start")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_v2_id_terminate() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/work/test-id/terminate")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_program_center → program_center (392 routes) ──
    #[tokio::test]
    async fn parity__program_center__applications() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program/applications")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__current_style() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program/appstyle/current/style")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__modules_all() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program/datastructure/modules/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__adminlogin_logout() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/adminlogin")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_list_all() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_create_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_flag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_flag_disable() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/flag/disable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_flag_enable() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/flag/enable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_flag_execute() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/flag/execute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_flag_file() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/flag/file")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_save() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_save_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/save/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_delete_flag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_agent_flag_disable() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/test-id/disable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_agent_flag_enable() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/test-id/enable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_agent_flag_execute() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/test-id/execute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_agent_file_flag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/test-id/file")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__andfx_pull_sync() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/andfx/pull/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__application_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/application/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__application_save() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/application/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__application_save_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/application/save/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__applications_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/applications")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__apppack_info_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/apppack/pack/info")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__apppack_android_repack() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/apppack/pack/info/android/repack")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__apppack_android_start() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/apppack/pack/info/android/start")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__apppack_file_download() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/apppack/pack/info/file/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__apppack_file_last() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/apppack/pack/info/file/last")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__apppack_publish() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/apppack/pack/info/file/publish")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__apppack_logo_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/apppack/pack/info/logo")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__apppack_server_connect() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/apppack/server/connect")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__apppack_file_download_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/apppackanony/pack/info/file/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__apppack_file_last_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/apppackanony/pack/info/file/last")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_current_style() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/current/style")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_current_update_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/current/update")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_current_update() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/current/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_current_update_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/current/update")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_application_top() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/application/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_image_application_top_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/application/top")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_application_top_erase() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/application/top/erase")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_application_top_erase_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/application/top/erase")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_launch_logo() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/launch/logo")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_image_launch_logo_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/launch/logo")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_launch_logo_erase() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/launch/logo/erase")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__crate() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/launch/logo/erase")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_login_avatar() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/login/avatar")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_image_login_avatar_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/login/avatar")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_login_avatar_erase() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/login/avatar/erase")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__crate_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/login/avatar/erase")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_menu_logo_blur() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/menu/logo/blur")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_image_menu_logo_blur_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/menu/logo/blur")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_menu_logo_blur_erase() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/menu/logo/blur/erase")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_menu_logo_blur_erase_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/menu/logo/blur/erase")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_menu_logo_focus() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/menu/logo/focus")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_image_menu_logo_focus_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/menu/logo/focus")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_menu_logo_focus_erase() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/menu/logo/focus/erase")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_menu_logo_focus_erase_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/menu/logo/focus/erase")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_process_default() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/process/default")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_image_process_default_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/process/default")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_process_default_erase() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/process/default/erase")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_process_default_erase_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/process/default/erase")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_setup_about_logo() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/setup/about/logo")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_image_setup_about_logo_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/setup/about/logo")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_image_setup_about_logo_erase() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/setup/about/logo/erase")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_appstyle_setup_about_logo_erase_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/image/setup/about/logo/erase")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__appstyle_index_portal() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/appstyle/index/portal")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__authentication_who() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/authentication")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__bar_create_mass_from_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/bar/create/mass/from/count")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_bar_create_mass_from_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/bar/create/mass/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__bar_select1_field_field_value_value_count_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/bar/select1/field/field/value/value/count/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_bar_select1_field_value_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/bar/select1/field/test-id/value/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__bar_select2_count_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/bar/select2/count/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_bar_select2_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/bar/select2/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__bar_select3_field_field_value_value_count_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/bar/select3/field/field/value/value/count/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_bar_select3_field_value_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/bar/select3/field/test-id/value/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__bar_select4_field_field_value_value_count_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/bar/select4/field/field/value/value/count/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_bar_select4_field_value_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/bar/select4/field/test-id/value/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__cachedispatch_dispatch() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/cachedispatch")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__captcha_id_validate_answer_answer() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/captcha/id/validate/answer/answer")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__captcha_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/captcha/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__captcha_v2_create_width_width_height_height() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/captcha/v2/create/width/width/height/height")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__captcha_v2_create_width_width_height_height_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/captcha/v2/create/width/test-id/height/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__captcha_id_validate_answer_answer_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/captcha/test-id/validate/answer/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__center_applications() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/center/applications")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__center_regist_applications() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/center/regist/applications")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__center_regist_applications_update() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/center/regist/applications")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__center_version() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/center/version")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__code_create_mobile_mobile() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/code/create/mobile/mobile")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__code_create_mobile_mobile_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/code/create/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__code_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/code/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__code_list_paging_page_size_size() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/code/list/paging/page/size/size")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__code_list_paging_page_size_size_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/code/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__code_validate_mobile_mobile_answer_answer() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/code/validate/mobile/mobile/answer/answer")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__code_validate_mobile_mobile_answer_answer_cascade() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/code/validate/mobile/mobile/answer/answer/cascade")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__code_validate_mobile_mobile_answer_answer_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/code/validate/mobile/test-id/answer/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__code_validate_mobile_mobile_answer_answer_cascade_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/code/validate/mobile/test-id/answer/test-id/cascade")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_add() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_collect_update() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_add_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/add")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_code_mobile_mobile() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/code/mobile/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_code_mobile_mobile_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/code/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_connect() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/connect")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_controllebbs() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/controllebbs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_controllermobile_name_name_mobile_mobile() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/controllermobile/name/name/mobile/mobile")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_collect_controllermobile_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/controllermobile/name/test-id/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_disconnect() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/disconnect")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_list_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_login() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/login")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_mobile_check_connect() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/mobile/check/connect")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_name_name_exist() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/name/name/exist")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_name_name_mobile_mobile_code_code() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/name/name/mobile/mobile/code/code")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_name_name_exist_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/name/test-id/exist")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_collect_delete_name_mobile_code() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/name/test-id/mobile/test-id/code/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_person() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_remove() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/remove")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_remove_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/remove")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_resetpassword() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/resetpassword")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_collect_resetpassword_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/resetpassword")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_sync_area() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/sync/area")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_updateUnit() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/updateUnit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_updateUnit_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/updateUnit")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_urlMapping() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/urlMapping")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_collect_urlmapping_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/urlMapping")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_validate() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/validate")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_validate_codeanswer() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/validate/codeanswer")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_validate_codeanswer_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/validate/codeanswer")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_validate_direct() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/validate/direct")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_validate_direct_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/validate/direct")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_validate_password() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/validate/password")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__collect_validate_password_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/collect/validate/password")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__command_execute() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/command/execute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__command_execute_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/command/execute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__command_list_node() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/command/list/node")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_config_dump() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_open_get_disable_export_enable() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config-open/get/disable/export/enable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_centerserver() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/centerserver")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__crate_2() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/centerserver")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_change_password() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/change/password")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_config_change_password_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/change/password")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_collect() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/collect")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_config_collect_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/collect")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_license() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/license")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_list_application() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/list/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_list_dump_data() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/list/dump/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_list_dump_data_current_node() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/list/dump/data/current/node")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_list_entity() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/list/entity")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_open() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/open")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_config_open_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/open")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_open_get_disable_export_enable_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/open/get/disable/export/enable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_open_run_time_config() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/open/run/time/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_config_open_run_time_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/open/run/time/config")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_person() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__crate_3() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/person")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_portal() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/portal")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_config_portal_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/portal")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_proxy() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/proxy")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_config_proxy_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/proxy")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_save() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/save")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_save_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/save")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_ternary_management() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/ternary/management")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_config_ternary_management_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/ternary/management")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__config_token() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/token")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__crate_4() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/config/token")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__datastructure_fileds_all() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/datastructure/fileds/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__datastructure_modules_all() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/datastructure/modules/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__datastructure_tables_all() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/datastructure/tables/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__deploy_id() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/deploy/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__deploy_list_paging_page_size_size() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/deploy/list/paging/page/size/size")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__deploy_list_paging_page_size_size_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/deploy/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__deploy_server_o2() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/deploy/server/o2")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__deploy_server_o2_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/deploy/server/o2")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__deploy_server_resource() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/deploy/server/resource")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_deploy_server_resource_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/deploy/server/resource")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__deploy_web_resource_as_new_asNew() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/deploy/web/resource/as/new/asNew")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__deploy_web_resource_as_new_asNew_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/deploy/web/resource/as/new/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__deploy_id_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/deploy/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__designer_search() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/designer/search")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__designer_search_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/designer/search")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_dictFlag_data() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/dictFlag/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_dictFlag_path_data() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/dictFlag/path/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_dictFlag_path_data_mockdeletetoget() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/dictFlag/path/data/mockdeletetoget")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_dictFlag_path_data_mockputtopost() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/dictFlag/path/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_id() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_list_paging_page_size_size() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/list/paging/page/size/size")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_list_paging_page_size_size_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_dictFlag_data_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/test-id/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_data_save_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/test-id/test-id/data")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_dictFlag_path_data_mockdeletetoget_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/test-id/test-id/data/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_dictFlag_path_data_mockputtopost_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/test-id/test-id/data/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dict_delete_id() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dict/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dingding_get_callback_aes() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dingding/get/callback/aes")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dingding_pull_sync() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dingding/pull/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dingding_request_pull_sync() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dingding/request/pull/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dingding_request_pull_sync_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dingding/request/pull/sync")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dingding_sync_organization_callback() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dingding/sync/organization/callback")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dingding_sync_organization_callback_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dingding/sync/organization/callback")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dingding_sync_organization_register_callback_enable() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dingding/sync/organization/register/callback/enable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__dingding_sync_organization_register_callback_enable_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/dingding/sync/organization/register/callback/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__distribute_assemble_source_source() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/distribute/assemble/source/source")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_distribute_source_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/distribute/assemble/source/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__distribute_webserver_assemble_source_source() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/distribute/webserver/assemble/source/source")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_distribute_webserver_source_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/distribute/webserver/assemble/source/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__foo_create_mass_from_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/foo/create/mass/from/count")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_foo_create_mass_from_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/foo/create/mass/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__input_compare() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/input/compare")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__input_compare_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/input/compare")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__input_cover() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/input/cover")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__input_cover_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/input/cover")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__input_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/input/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__input_create_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/input/create")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__input_prepare_cover() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/input/prepare/cover")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__input_prepare_cover_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/input/prepare/cover")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__input_prepare_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/input/prepare/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__input_prepare_create_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/input/prepare/create")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_invoke_list_all() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__crate_5() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__invoke_flag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__invoke_flag_client_client_token_token_execute() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/flag/client/client/token/token/execute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__invoke_flag_execute() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/flag/execute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__invoke_flag_execute_get() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/flag/execute/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__invoke_flag_file() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/flag/file")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__invoke_list_category() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/list/category")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__invoke_list_with_category_category() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/list/with/category/category")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_invoke_list_by_category() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/list/with/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__invoke_token() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/token")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__invoke_token_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/token")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__crate_6() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_invoke_execute_with_token() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/test-id/client/test-id/token/test-id/execute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_invoke_execute() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/test-id/execute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__invoke_flag_execute_get_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/test-id/execute/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_invoke_file_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/test-id/file")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__jest_center_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/jest/center/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__jest_clear_cache_source() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/jest/clear/cache/source")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_jest_clear_cache_source() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/jest/clear/cache/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__jest_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/jest/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__jest_version() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/jest/version")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_cloud_unit_is_vip() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/cloud/unit/is/vip")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_flag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_flag_cover_pic() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/flag/cover/pic")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_flag_install_log() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/flag/install/log")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_flag_install_or_update() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/flag/install/or/update")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_flag_install_or_update_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/flag/install/or/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_flag_install_or_update_2() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/flag/install/or/update")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_flag_installed_version() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/flag/installed/version")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_flag_uninstall() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/flag/uninstall")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_id_download() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_install_offline() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/install/offline")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_install_offline_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/install/offline")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_list_category() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/list/category")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_list_install_log_paging_page_size_size() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/list/install/log/paging/page/size/size")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_market_install_log_paging_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/list/install/log/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_list_paging_page_size_size() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/list/paging/page/size/size")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_list_paging_page_size_size_category_category() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/list/paging/page/size/size/category/category")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_market_list_paging_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_market_list_paging_category() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/list/paging/test-id/size/test-id/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_list_top_three() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/list/top/three")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_market_get_flag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_market_cover_pic_flag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/test-id/cover/pic")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_id_download_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_market_install_log_flag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/test-id/install/log")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_flag_install_or_update_3() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/test-id/install/or/update")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_flag_installed_version_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/test-id/installed/version")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__market_flag_uninstall_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/market/test-id/uninstall")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_compare_upload() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/compare/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_module_compare_upload_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/compare/upload")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_id_compare() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/id/compare")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_module_list_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/list")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_list_category() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/list/category")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_output() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/output")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_module_output_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/output")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_output_flag_file() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/output/flag/file")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_output_list_structure() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/output/list/structure")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_module_output_structure_latest() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/output/structure")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_output_structure() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/output/structure")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_output_flag_file_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/output/test-id/file")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_remove_structure_id() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/remove/structure/id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_remove_structure_id_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/remove/structure/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_remove_structure_id_2() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/remove/structure/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_write_flag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/write/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_write_flag_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/write/flag")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_write_flag_2() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/write/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__module_id_compare_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/module/test-id/compare")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_check() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_mpweixin_check_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/check")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_media_add_forever() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/media/add/forever")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_menu_add() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/menu/add")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_mpweixin_menu_latest() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/menu/create/to/weixin")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_menu_create_to_weixin() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/menu/create/to/weixin")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_menu_delete_id() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/menu/delete/id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_menu_delete_id_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/menu/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_menu_list_weixin() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/menu/list/weixin")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_menu_subscribe() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/menu/subscribe")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_menu_update_id() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/menu/update/id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_menu_update_id_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/menu/update/id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_menu_update_id_2() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/menu/update/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__mpweixin_message_template_send() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/message/template/send")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_mpweixin_template_send_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/mpweixin/message/template/send")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__output_appInfoFlag_select() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/output/appInfoFlag/select")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__output_flag_select_file() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/output/flag/select/file")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__output_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/output/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__output_list_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/output/list")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_output_select_put() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/output/test-id/select")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__output_flag_select_file_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/output/test-id/select/file")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_prompterrorlog_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_count_exceptionclass() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/count/exceptionclass")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_count_loggername() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/count/loggername")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_id() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_next_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/id/next/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_next_count_date_date() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/id/next/count/date/date")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_next_count_exceptionclass_exceptionClass() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/id/next/count/exceptionclass/exceptionClass")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_next_count_loggername_loggerName() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/id/next/count/loggername/loggerName")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_prev_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/id/prev/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_prev_count_date_date() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/id/prev/count/date/date")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_prev_count_exceptionclass_exceptionClass() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/id/prev/count/exceptionclass/exceptionClass")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_prev_count_loggername_loggerName() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/id/prev/count/loggername/loggerName")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_next_count_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_next_count_date_date_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/test-id/next/test-id/date/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_next_count_exceptionclass_exceptionClass_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/test-id/next/test-id/exceptionclass/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_next_count_loggername_loggerName_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/test-id/next/test-id/loggername/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_prev_count_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_prev_count_date_date_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/test-id/prev/test-id/date/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_prev_count_exceptionclass_exceptionClass_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/test-id/prev/test-id/exceptionclass/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_list_id_prev_count_loggername_loggerName_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/list/test-id/prev/test-id/loggername/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__prompterrorlog_id_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/prompterrorlog/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__qiyeweixin_get_callback_aes() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/qiyeweixin")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_qiyeweixin_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/qiyeweixin")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__qiyeweixin_get_callback_aes_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/qiyeweixin/get/callback/aes")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__qiyeweixin_pull_sync() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/qiyeweixin/pull/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__qiyeweixin_request_pull_sync() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/qiyeweixin/request/pull/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_qiyeweixin_request_pull_sync_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/qiyeweixin/request/pull/sync")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__qiyeweixin_send_getprivateinfo_message() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/qiyeweixin/send/getprivateinfo/message")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__qiyeweixin_send_getprivateinfo_message_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/qiyeweixin/send/getprivateinfo/message")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__schedule_list_schedule() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/schedule/list/schedule")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__schedule_list_schedulelocal() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/schedule/list/schedulelocal")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__schedule_list_schedulelog_application_application() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/schedule/list/schedulelog/application/application")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__schedule_list_schedulelog_application_application_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/schedule/list/schedulelog/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__schedule_report() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/schedule/report")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__schedule_report_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/schedule/report")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__schedule_schedule_fire() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/schedule/schedule/fire")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_schedule_fire_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/schedule/schedule/fire")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__script_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__script_flag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__script_id() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__script_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__script_list_paging_page_size_size() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/list/paging/page/size/size")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__script_list_paging_page_size_size_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__script_name_name() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/name/name")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__script_name_name_imported() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/name/name/imported")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_script_save_name() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/name/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_script_name_imported() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/name/test-id/imported")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__script_save_flag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__storagemappings_list() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/storagemappings")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__test_test1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/test/test1")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__test_test2() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/test/test2")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__tokenthreshold_update() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/tokenthreshold/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__tokenthreshold_update_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/tokenthreshold/update")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_unexpectederrorlog_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/unexpectederrorlog")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__unexpectederrorlog_id() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/unexpectederrorlog/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__unexpectederrorlog_list_id_next_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/unexpectederrorlog/list/id/next/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__unexpectederrorlog_list_id_next_count_date_date() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/unexpectederrorlog/list/id/next/count/date/date")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__unexpectederrorlog_list_id_prev_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/unexpectederrorlog/list/id/prev/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__unexpectederrorlog_list_id_prev_count_date_date() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/unexpectederrorlog/list/id/prev/count/date/date")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__unexpectederrorlog_list_id_next_count_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/unexpectederrorlog/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__unexpectederrorlog_list_id_next_count_date_date_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/unexpectederrorlog/list/test-id/next/test-id/date/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__unexpectederrorlog_list_id_prev_count_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/unexpectederrorlog/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__unexpectederrorlog_list_id_prev_count_date_date_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/unexpectederrorlog/list/test-id/prev/test-id/date/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__unexpectederrorlog_id_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/unexpectederrorlog/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__validation_meta() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/validation/meta")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__validation_scripting_benchmark() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/validation/scripting/benchmark")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__validation_timeout_timeout() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/validation/timeout/timeout")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__validation_timeout_timeout_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/validation/timeout/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__warnlog_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/warnlog")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__warnlog_list_next_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/warnlog/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__warnlog_list_next_count_date_date() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/warnlog/list/test-id/next/test-id/date/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__warnlog_list_prev_count() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/warnlog/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__warnlog_list_prev_count_date_date() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/warnlog/list/test-id/prev/test-id/date/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__warnlog_view_system_log_tag_tag() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/warnlog/view/system/log/tag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__warnlog_id() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/warnlog/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_welink_pull_sync() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/welink/pull/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__u3_welink_request_pull_sync_post() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/welink/request/pull/sync")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__zhengwudingding_pull_sync() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/zhengwudingding/pull/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__zhengwudingding_regist_callback() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/zhengwudingding/regist/callback")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__zhengwudingding_sync_organization_callback() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/zhengwudingding/sync/organization/callback")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_center__zhengwudingding_sync_organization_callback_1() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/zhengwudingding/sync/organization/callback")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_program_init → program_init (15 routes) ──
    #[tokio::test]
    async fn parity__program_init__external_datasources_check() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/externaldatasources/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__external_datasources_list() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/externaldatasources/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__external_datasources_set() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/externaldatasources/set")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__external_datasources_set_cancel() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/externaldatasources/set/cancel")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__external_datasources_validate() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/externaldatasources/validate")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__h2_check() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/h2/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__restore_upload() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/restore/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__restore_upload_cancel() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/restore/upload/cancel")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__check() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/secret/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__set() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/secret/set")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__set_cancel() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/secret/set/cancel")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__server_execute() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/server/execute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__server_execute_status() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/server/execute/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__server_license() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/server/license")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__program_init__server_stop() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/server/stop")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_query_assemble_designer → query_assemble_designer (146 routes) ──
    #[tokio::test]
    async fn parity__query_assemble_designer__crate() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/bundle/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__create_designer() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__delete_designer() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__delete_designer_1() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/designer/search")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_1() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/entity/entity/properties/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__get_designer() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_1() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/icon/set/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_2() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/icon/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_2() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_3() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/importmodel")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_4() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/importmodel/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_5() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/importmodel/edit/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_3() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/importmodel/list/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_4() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/importmodel/list/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_5() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/importmodel/permission/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_6() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/importmodel/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_6() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/importmodel/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_7() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/importmodel/test-id/permission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__input_compare() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/input/compare")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__input_cover() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/input/cover")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__input_create() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/input/create")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__input_prepare_cover() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/input/prepare/cover")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__input_prepare_create() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/input/prepare/create")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__query_list_all() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_7() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/list/querycategory/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__query_list_summary() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/list/summary")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_8() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/list/summary/querycategory/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__list_designers() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_9() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/list/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_10() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/list/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_8() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/delete/model/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_11() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/generate/model/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_12() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/learn/model/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__neural_list_model() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/list/model")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__neural_model() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/model")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_13() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/model/reset/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_14() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/model/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_9() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/model/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_15() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/model/test-id/reset/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_16() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/stop/generating/model/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_17() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/stop/learn/model/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_10() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/neural/update/model/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__output_list() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/output/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_18() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/output/select/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_19() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/output/select/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_11() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/output/test-id/select")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_20() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/output/test-id/select/file")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_21() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/permission/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_12() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/query")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_22() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/query/entity/test-id/category/test-id/properties")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_23() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/query/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_24() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/query/list/querycategory/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_25() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/query/list/summary")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_26() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/query/list/summary/querycategory/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_27() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/query/querycategory/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__query_flag() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_13() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/query/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_14() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/query/test-id/icon")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_15() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/query/test-id/permission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__query_querycategory_list() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/querycategory/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__save_designer() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__save_designer_1() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/save/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__designer_search() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/search")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_28() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/simulate/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_16() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_17() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_18() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/edit/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_29() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/list/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_30() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__stat_list_id_prev_count() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_31() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/list/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_32() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/list/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_33() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/permission/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_34() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/simulate/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_35() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_19() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_20() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/test-id/permission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_21() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/stat/test-id/simulate")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_22() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/statement")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_23() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/statement/execute/test-id/mode/test-id/page/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_24() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/statement/execute/test-id/page/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_25() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/statement/list/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_26() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/statement/list/query/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_27() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/statement/permission/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_28() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/statement/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_29() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/statement/test-id/execute/mode/test-id/page/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_30() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/statement/test-id/execute/page/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_31() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/statement/test-id/permission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_32() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_36() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/build/dispatch/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__table_query_query_build() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/build/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_37() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/build/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_33() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_38() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/draft/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_34() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/edit/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_39() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/execute/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_40() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/export/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_41() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/export/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__table_list_manage() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/list/manage")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_42() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/list/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_43() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/list/row/select/where/where/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__table_list_tableFlag_row_id_prev_count() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/list/row/test-id/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_44() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/list/row/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_45() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/list/test-id/row/select/where/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_46() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/list/test-id/row/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_47() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/list/test-id/row/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_48() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/list/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_49() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/permission/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_50() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/query/test-id/build")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__table_reload_dynamic() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/reload/dynamic")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__table_tableFlag_row_delete_all() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/row/delete/all/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_51() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/row/delete/all/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_35() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/row/delete/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_36() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/row/insert/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_52() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/row/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__table_tableFlag_row_save() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/row/save/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_37() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/row/update/test-id/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_53() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/row/where/where/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_54() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/row/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_55() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/row/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_56() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_38() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_39() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id/build/dispatch")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_57() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id/execute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_40() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id/permission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_41() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id/row")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_58() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id/row/count/where/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_59() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id/row/delete/all")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_60() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id/row/save")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_61() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id/row/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_62() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id/status/build")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_63() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/table/test-id/status/draft")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_42() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_43() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_44() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view/edit/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_64() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view/list/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_65() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__view_list_id_prev_count() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__view_id_permission() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view/permission/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__view_id() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_45() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_46() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view/test-id/bundle")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_47() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view/test-id/permission")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__u2_48() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/view/test-id/simulate")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__crate_66() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_query_assemble_surface → query_assemble_surface (129 routes) ──
    #[tokio::test]
    async fn parity__query_assemble_surface__importmodel_id_execute() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/importmodel/id/test-id/execute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__create_surface() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__delete_surface() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__get_surface() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__list_surfaces() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__preview_surface() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/preview/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__save_surface() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/bundle/mockputtopost/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_1() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/bundle/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/bundle/v2/post/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_2() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/bundle/v2/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_3() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/bundle/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_4() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/bundle/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_5() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/excel/mockputtopost/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_6() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/excel/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_7() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/excel/result/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_8() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/excel/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_9() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/excel/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_10() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/execute/mockputtopost/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_11() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/execute/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_12() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/execute/v2/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_13() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/execute/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_1() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/execute/record/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_14() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/execute/record/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_15() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/flag/test-id/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_16() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/list/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_17() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/list/record/item/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_18() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/list/record/item/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_19() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/list/record/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_20() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/list/record/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_21() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/list/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_2() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/record/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_22() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/record/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_3() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/record/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_23() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/record/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_24() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/record/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_25() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/record/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_26() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/record/test-id/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_27() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/record/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__importmodel_uuid() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/uuid")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_28() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_29() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_30() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_31() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/importmodel/test-id/execute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__query_list() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_32() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/list/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_4() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/morelikethis")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_33() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/neural/list/calculate/model/test-id/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_34() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/neural/list/calculate/model/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__query_list_1() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/query/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_35() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/query/list/key/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_36() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_5() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/search")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_6() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/stat/execute/mockputtopost/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_7() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/stat/flag/test-id/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_8() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/stat/flag/test-id/query/test-id/execute")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_9() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/stat/flag/test-id/query/test-id/execute/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_10() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/stat/list/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_11() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/stat/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_12() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/stat/test-id/execute")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_13() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/stat/test-id/execute/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_14() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/statement/execute/test-id/mode/test-id/page/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_15() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/statement/execute/test-id/page/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_16() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/statement/list/query/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_17() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/statement/test-id/execute/mode/test-id/page/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_18() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/statement/test-id/execute/page/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_19() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/statement/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_20() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/statement/test-id/format")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_37() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_38() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/paging/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_39() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/row/select/where/where/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_40() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/row/select/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__table_list_tableFlag_row_id_prev_count() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/row/test-id/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_41() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/row/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_42() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/table/row/paging/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_43() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/table/test-id/row/paging/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_44() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__table_list_id_prev_count() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_21() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/test-id/row/select")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_45() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/test-id/row/select/where/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_46() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/test-id/row/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_47() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/test-id/row/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_48() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/list/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__table_reload_dynamic() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/reload/dynamic")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_49() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/delete/all/mockdeletetoget/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_50() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/delete/all/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_22() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/delete/test-id/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_23() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/insert/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_51() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/mockdeletetoget/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_52() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/mockputtopost/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_24() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/one/insert/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_53() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/one/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_54() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/part/update/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_55() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/where/where/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_56() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_57() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/row/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_58() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_25() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/test-id/row")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_59() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/test-id/row/count/where/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_60() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/test-id/row/delete/all")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_61() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/test-id/row/delete/all/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_26() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/test-id/row/one")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_62() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/test-id/row/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_63() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/test-id/row/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_64() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/test-id/row/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_65() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/table/test-id/row/test-id/part/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_66() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/excel/result/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_67() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/flag/test-id/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_27() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/flag/test-id/query/test-id/bundle")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_28() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/flag/test-id/query/test-id/bundle/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_29() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/flag/test-id/query/test-id/excel")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_30() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/flag/test-id/query/test-id/excel/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_31() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/flag/test-id/query/test-id/execute")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_32() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/flag/test-id/query/test-id/execute/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_33() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/flag/test-id/query/test-id/execute/v2/page/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__view_list_query_queryFlag() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/list/query/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__view_id() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_34() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/test-id/bundle")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_35() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/test-id/bundle/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_36() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/test-id/bundle/v2")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_37() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/test-id/excel")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_68() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/test-id/excel/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_38() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/test-id/execute")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_69() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/test-id/execute/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__u2_39() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/view/test-id/execute/v2/page/test-id/size/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_70() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__view_flag_flag_query_queryFlag_execute() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/test-id/application/test-id/execute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__view_flag_flag_query_queryFlag_execute_v2_page_page_size_size() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/test-id/application/test-id/execute/page/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__crate_71() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/test-id/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_query_core_entity → query_core_entity (5 routes) ──
    #[tokio::test]
    async fn parity__query_core_entity__import_list() {
        let router = oa4rust::query_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/import/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_core_entity__item_list() {
        let router = oa4rust::query_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/item/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_core_entity__view_create() {
        let router = oa4rust::query_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/view/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_core_entity__view_list() {
        let router = oa4rust::query_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/view/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_core_entity__view_get() {
        let router = oa4rust::query_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

    // ── x_query_core_express → query_core_express (4 routes) ──
    #[tokio::test]
    async fn parity__query_core_express__get_cache_status() {
        let router = oa4rust::query_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/core/express/cache/status/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_core_express__cache_query_result() {
        let router = oa4rust::query_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/core/express/cache/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_core_express__execute_query() {
        let router = oa4rust::query_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/core/express/execute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }
    #[tokio::test]
    async fn parity__query_core_express__get_query_history() {
        let router = oa4rust::query_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/core/express/history/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        let (parts, body) = response.into_parts();
        let bytes = axum::body::to_bytes(body, usize::MAX)
            .await
            .expect("read parity body");
        assert!(
            parts.status != StatusCode::NOT_FOUND || !bytes.is_empty(),
            "route not registered"
        );
    }

// ──────────────────────────────────────────────────────────────────────────────
// Parity count sanity check
// ──────────────────────────────────────────────────────────────────────────────
#[test]
fn parity_generated_test_count() {
    assert!(4117 > 0, "no parity tests generated — check the Python script output");
}
