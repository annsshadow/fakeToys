// AUTO-GENERATED by scripts/generate_parity_tests.py 鈥?DO NOT EDIT.
//
// Parity regression suite (Phase 4 U4.1).
//
// For each o2server @Path mapped in docs/audits/o2server-parity-report.json,
// this file contains a test that verifies the the corresponding Rust axum route
// is registered on the crate's Router.  A NOT_FOUND (404) response means the
// route is missing from the Rust implementation 鈥?a parity gap.
//
// Generated: 2026-08-19 23:51:57
// Crates: 55   Routes: 824   Tests: 824

#[allow(unreachable_code, unused_variables, non_snake_case)]

use axum::body::Body;
use axum::http::{Request, Method, StatusCode};
use tower::util::ServiceExt;
use shared::testing::{test_pool, mock_pool};

    // 鈹€鈹€ x_ai_assemble_control 鈫?ai_assemble_control (37 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__ai_assemble_control__config_create_mcp() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/assemble/control/config/create/mcp")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai/assemble/control/config/create/mcp (POST)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_delete_mcp_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/assemble/control/config/delete/mcp/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai/assemble/control/config/delete/mcp/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_get_mcp_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/assemble/control/config/get/mcp/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai/assemble/control/config/get/mcp/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_list_mcp_paging_page_size_size() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/assemble/control/config/list/mcp/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai/assemble/control/config/list/mcp/paging/{{page}}/size/{{size}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_update_mcp_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/assemble/control/config/update/mcp/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai/assemble/control/config/update/mcp/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__chat_completion() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/chat/completion")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/chat/completion (POST)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_base_config() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/base/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/base/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_create_mcp_1() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/create/mcp")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/create/mcp (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_create_model() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/create/model")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/create/model (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_delete_mcp_flag_1() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/delete/mcp/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/delete/mcp/flag (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_delete_model_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/delete/model/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/delete/model/flag (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_get_mcp_ext_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/get/mcp/ext/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/get/mcp/ext/flag (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_get_mcp_flag_1() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/get/mcp/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/get/mcp/flag (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_get_model_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/get/model/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/get/model/flag (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_list_enable_model() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/list/enable/model")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/list/enable/model (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_list_mcp_paging_page_size_size_1() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/list/mcp/paging/page/size/size")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/list/mcp/paging/page/size/size (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_list_model_paging_page_size_size() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/list/model/paging/page/size/size")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/list/model/paging/page/size/size (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_save() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/save")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/save (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_update_mcp_flag_1() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/update/mcp/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/update/mcp/flag (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__config_update_model_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/config/update/model/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/config/update/model/flag (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_copy_file() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/copy/file")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/file/copy/file (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_delete_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/delete/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/file/delete/flag (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/file/flag (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_id_download() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/file/id/download (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_id_download_scale() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/id/download/scale")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/file/id/download/scale (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_list() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/file/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_list_paging_page_size_size() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/list/paging/page/size/size")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/file/list/paging/page/size/size (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__file_upload() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/file/upload")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/file/upload (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__get_ai_control_config() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/get/ai/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/get/ai/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__get_usage_stats() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/get/usage/stats")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/get/usage/stats (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__index_cms_doc_docId() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/index/cms/doc/docId")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/index/cms/doc/docId (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__index_cms_doc_with_app_appId() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/index/cms/doc/with/app/appId")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/index/cms/doc/with/app/appId (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__index_delete_flag() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/index/delete/flag")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/index/delete/flag (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__index_list_paging_page_size_size() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/index/list/paging/page/size/size")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/index/list/paging/page/size/size (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__index_sync_to_knowledge() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/index/sync/to/knowledge")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/index/sync/to/knowledge (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__list_ai_models() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/list/ai/models")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/list/ai/models (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai_assemble_control__update_ai_control_config() {
        let router = oa4rust::ai_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai_assemble_control/update/ai/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai_assemble_control: /jaxrs/ai_assemble_control/update/ai/control/config (GET)"
        );
    }

    // 鈹€鈹€ x_ai_core_entity 鈫?ai (21 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__ai__app_list() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/app/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/app/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__chat_delete() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/chat/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/chat/delete/{{clue_id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__chat_list_completion_paging() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/chat/list/completion/test-id/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/chat/list/completion/{{clue_id}}/paging/{{page}}/size/{{size}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__chat_list_paging() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/chat/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/chat/list/paging/{{page}}/size/{{size}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_base_config() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/base/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/config/base/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_get() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/config/get (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_get_mcp() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/get/mcp/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/config/get/mcp/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_get_model() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/get/model/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/config/get/model/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__list_enable_model() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/list/enable/model")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/config/list/enable/model (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_list_mcp_paging() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/list/mcp/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/config/list/mcp/paging/{{page}}/size/{{size}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__config_list_model_paging() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/config/list/model/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/config/list/model/paging/{{page}}/size/{{size}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__conversation_list() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/conversation/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/conversation/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__file_delete() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/file/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/file/delete/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__file_get() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/file/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__file_download() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/file/test-id/download")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/file/{{id}}/download (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__file_download_scale() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/file/test-id/download/scale")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/file/{{id}}/download/scale (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__index_cms_doc_with_app() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/index/cms/doc/with/app/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/index/cms/doc/with/app/{{appId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__index_cms_doc() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/index/cms/doc/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/index/cms/doc/{{docId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__index_delete() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/index/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/index/delete/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__sync_to_knowledge() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/index/sync/to/knowledge")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/index/sync/to/knowledge (GET)"
        );
    }
    #[tokio::test]
    async fn parity__ai__model_list() {
        let router = oa4rust::ai::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/ai/model/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on ai: /jaxrs/ai/model/list (GET)"
        );
    }

    // 鈹€鈹€ x_attendance_assemble_control 鈫?attendance_assemble_control (87 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceadmin/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceadmin/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_1() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceadmin/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceadmin/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_2() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/appeal/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceappealInfo/appeal/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_3() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/archive/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceappealInfo/archive/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_4() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/audit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceappealInfo/audit (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_5() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/check")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceappealInfo/check (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_6() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceappealInfo/filter/list/{{id}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_7() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceappealInfo/filter/list/{{id}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_8() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/manager/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceappealInfo/manager/list/{{id}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_9() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/workflow/appeal/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceappealInfo/workflow/appeal/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_10() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/workflow/sync")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceappealInfo/workflow/sync (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_11() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceappealInfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceappealInfo/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_12() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/analyse")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/analyse (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_13() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/analyse/id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/analyse/id/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_14() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/analyse/redo")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/analyse/redo (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_15() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/analyse/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/analyse/{{startDate}}/{{endDate}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_16() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/archive/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/archive/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_17() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/checkDetailWithPersonByCycle/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/checkDetailWithPersonByCycle/{{cycleYear}}/{{cycleMonth}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_18() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/filter/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_19() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/topUnit")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/filter/list/topUnit (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_20() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/unit")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/filter/list/unit (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_21() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/user")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/filter/list/user (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_22() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/filter/list/{{id}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_23() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/filter/list/{{id}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_24() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/list/persons/nonesign")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/list/persons/nonesign (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_25() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/list/{{file_id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_26() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/filter/list/page/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/mobile/filter/list/page/{{page}}/count/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_27() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/mobilepreview")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/mobile/mobilepreview (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_28() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/my")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/mobile/my (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_29() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/recive")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/mobile/recive (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_30() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/mobile/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_31() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/recive")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/recive (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_32() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/reciveSingle")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/reciveSingle (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_33() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancedetail/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancedetail/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_34() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceemployeeconfig/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceemployeeconfig/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_35() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceemployeeconfig/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceemployeeconfig/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_36() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceimportfileinfo/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceimportfileinfo/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_37() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceimportfileinfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceimportfileinfo/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_38() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceschedulesetting/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceschedulesetting/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_39() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceschedulesetting/list/topUnit/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceschedulesetting/list/topUnit/{{name}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_40() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceschedulesetting/list/unit/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceschedulesetting/list/unit/{{name}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_41() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceschedulesetting/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceschedulesetting/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_42() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday/filter/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceselfholiday/filter/list/{{id}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_43() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday/filter/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceselfholiday/filter/list/{{id}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_44() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceselfholiday/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_45() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceselfholiday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceselfholiday/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_46() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancesetting/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancesetting/code/{{code}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_47() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancesetting/enable/type")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancesetting/enable/type (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_48() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancesetting/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancesetting/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_49() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancesetting/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancesetting/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_50() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticalcycle/cycleDetail/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancestatisticalcycle/cycleDetail/{{year}}/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_51() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticalcycle/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancestatisticalcycle/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_52() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticalcycle/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancestatisticalcycle/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_53() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticrequirelog/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancestatisticrequirelog/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_54() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendancestatisticrequirelog/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendancestatisticrequirelog/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_55() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceworkdayconfig/filter")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceworkdayconfig/filter (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_56() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceworkdayconfig/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceworkdayconfig/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_57() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/attendanceworkdayconfig/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/attendanceworkdayconfig/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_58() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/rule/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/rule/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_59() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/rule/test-id/toggle")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/rule/{{id}}/toggle (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_60() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/selfholidaysimple/docId/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/selfholidaysimple/docId/{{docId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_61() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statistic/do")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statistic/do (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_62() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/personMonth/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/filter/personMonth/list/{{id}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_63() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/personMonth/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/filter/personMonth/list/{{id}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_64() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitDay/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/filter/topUnitDay/list/{{id}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_65() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitDay/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/filter/topUnitDay/list/{{id}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_66() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitMonth/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/filter/topUnitMonth/list/{{id}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_67() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/topUnitMonth/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/filter/topUnitMonth/list/{{id}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_68() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitDay/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/filter/unitDay/list/{{id}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_69() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitDay/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/filter/unitDay/list/{{id}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_70() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitMonth/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/filter/unitMonth/list/{{id}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_71() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/filter/unitMonth/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/filter/unitMonth/list/{{id}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_72() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/person/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/person/{{name}}/{{year}}/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_73() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/persons/unit/subnested/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/persons/unit/subnested/{{name}}/{{year}}/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_74() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/persons/unit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/persons/unit/{{name}}/{{year}}/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_75() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/topUnit/day/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/topUnit/day/{{name}}/{{year}}/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_76() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/topUnit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/topUnit/{{name}}/{{year}}/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_77() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/day/topUnit/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/unit/day/topUnit/{{name}}/{{date}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_78() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/day/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/unit/day/{{name}}/{{date}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_79() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/day/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/unit/day/{{name}}/{{year}}/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_80() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/subnested/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/unit/subnested/{{name}}/{{year}}/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_81() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/sum/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/unit/sum/{{name}}/{{year}}/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_82() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/topUnit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/unit/topUnit/{{name}}/{{year}}/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_83() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/statisticshow/unit/test-id/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/statisticshow/unit/{{name}}/{{year}}/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_84() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/uuid/random")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/uuid/random (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_85() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/workplace/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/workplace/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance_assemble_control__crate_86() {
        let router = oa4rust::attendance_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/assemble/control/workplace/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance_assemble_control: /jaxrs/attendance/assemble/control/workplace/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_attendance_core_entity 鈫?attendance (9 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__attendance__crate() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/admin/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance: /jaxrs/attendance/admin/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_1() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/appeal/archive/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance: /jaxrs/attendance/appeal/archive/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_2() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/appeal/audit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance: /jaxrs/attendance/appeal/audit (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_3() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/appeal/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance: /jaxrs/attendance/appeal/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_4() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/appeal/submit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance: /jaxrs/attendance/appeal/submit (POST)"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_5() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/employee/config/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance: /jaxrs/attendance/employee/config/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_6() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/record/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance: /jaxrs/attendance/record/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_7() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/rule/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance: /jaxrs/attendance/rule/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__attendance__crate_8() {
        let router = oa4rust::attendance::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attendance/statistical/cycle/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on attendance: /jaxrs/attendance/statistical/cycle/list/all (GET)"
        );
    }

    // 鈹€鈹€ x_base_core_project 鈫?base (3 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__base__cache_detail() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/cache/detail")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on base: /jaxrs/base/cache/detail (GET)"
        );
    }
    #[tokio::test]
    async fn parity__base__echo_get() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/echo/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on base: /jaxrs/base/echo/get (GET)"
        );
    }
    #[tokio::test]
    async fn parity__base__openapi_info() {
        let router = oa4rust::base::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/base/openapi/info")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on base: /jaxrs/base/openapi/info (GET)"
        );
    }

    // 鈹€鈹€ x_bbs_assemble_control 鈫?bbs_assemble_control (17 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__bbs_assemble_control__get_control_config() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__list_forums() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/forum/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/forum/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__forum_view_all() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/forum/view/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/forum/view/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__get_forum() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/forum/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/forum/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__permission_section_sectionId() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/permission/section/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/permission/section/{{sectionId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__permission_subject_subjectId() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/permission/subject/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/permission/subject/{{subjectId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__create_reply() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/reply/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/reply/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__reply_list_sub_id() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/reply/list/sub/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/reply/list/sub/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__list_control_sections() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/section/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/section/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__section_viewforum_forumId() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/section/viewforum/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/section/viewforum/{{forumId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__shutup_create() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/shutup/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/shutup/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__subject_top_sectionId() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/subject/top/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/subject/top/{{sectionId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__subject_view_id() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/subject/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/subject/view/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__create_topic() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/topic/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/topic/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__list_topics_by_forum() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/topic/list/forum/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/topic/list/forum/{{forumId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__update_control_config() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/update/control/config")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/update/control/config (POST)"
        );
    }
    #[tokio::test]
    async fn parity__bbs_assemble_control__uuid_generate() {
        let router = oa4rust::bbs_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/assemble/control/uuid")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs_assemble_control: /jaxrs/bbs/assemble/control/uuid (GET)"
        );
    }

    // 鈹€鈹€ x_bbs_core_entity 鈫?bbs (9 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__bbs__forum() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/forum/view/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs: /jaxrs/bbs/forum/view/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs__forum_1() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/forum/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs: /jaxrs/bbs/forum/view/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs__section() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/section/view/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs: /jaxrs/bbs/section/view/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs__section_1() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/section/viewforum/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs: /jaxrs/bbs/section/viewforum/{{forumId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs__subject() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/subject/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs: /jaxrs/bbs/subject/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__bbs__subject_1() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/subject/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs: /jaxrs/bbs/subject/list/{{sectionId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs__subject_2() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/subject/search")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs: /jaxrs/bbs/subject/search (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs__subject_3() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/subject/top/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs: /jaxrs/bbs/subject/top/{{sectionId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__bbs__subject_4() {
        let router = oa4rust::bbs::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/bbs/subject/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on bbs: /jaxrs/bbs/subject/view/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_calendar_assemble_control 鈫?calendar_assemble_control (4 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__calendar_assemble_control__get_calendar_detail() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/assemble/control/calendar/detail/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar_assemble_control: /jaxrs/calendar/assemble/control/calendar/detail/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__get_control_config() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/get/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar_assemble_control: /jaxrs/calendar_assemble_control/get/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__list_control_calendars() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/list/control/calendars")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar_assemble_control: /jaxrs/calendar_assemble_control/list/control/calendars (GET)"
        );
    }
    #[tokio::test]
    async fn parity__calendar_assemble_control__update_control_config() {
        let router = oa4rust::calendar_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar_assemble_control/update/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar_assemble_control: /jaxrs/calendar_assemble_control/update/control/config (GET)"
        );
    }

    // 鈹€鈹€ x_calendar_core_entity 鈫?calendar (10 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__calendar__calendar_create() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar: /jaxrs/calendar/calendar/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__calendar__calendar_list_my() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/list/my")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar: /jaxrs/calendar/calendar/list/my (GET)"
        );
    }
    #[tokio::test]
    async fn parity__calendar__calendar_list_public() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/list/public")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar: /jaxrs/calendar/calendar/list/public (GET)"
        );
    }
    #[tokio::test]
    async fn parity__calendar__calendar_remove() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/remove")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar: /jaxrs/calendar/calendar/remove (POST)"
        );
    }
    #[tokio::test]
    async fn parity__calendar__calendar_update() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar: /jaxrs/calendar/calendar/update (POST)"
        );
    }
    #[tokio::test]
    async fn parity__calendar__calendar_get() {
        let router = oa4rust::calendar::router(shared::testing::mock_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/calendar/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar: /jaxrs/calendar/calendar/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__calendar__event_create() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/event/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar: /jaxrs/calendar/event/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__calendar__event_list() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/event/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar: /jaxrs/calendar/event/list/{{calendarId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__calendar__event_remove() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/event/remove")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar: /jaxrs/calendar/event/remove (POST)"
        );
    }
    #[tokio::test]
    async fn parity__calendar__event_update() {
        let router = oa4rust::calendar::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/calendar/event/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on calendar: /jaxrs/calendar/event/update (POST)"
        );
    }

    // 鈹€鈹€ x_cms_assemble_control 鈫?cms_assemble_control (42 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_document_id_view() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/document/test-id/view")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/anonymous/document/{{id}}/view (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__anonymous_fileinfo_download_document_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/fileinfo/download/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/anonymous/fileinfo/download/document/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__application_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/application/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_search() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms_assemble_control/document/search")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/cms_assemble_control/document/search (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__get_control_config() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms_assemble_control/get/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/cms_assemble_control/get/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__list_control_sections() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms_assemble_control/list/control/sections")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/cms_assemble_control/list/control/sections (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__update_control_config() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms_assemble_control/update/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/cms_assemble_control/update/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__commend_list_paging() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/commend/list/paging/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/commend/list/paging/{{docId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_array_data() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/array/data")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/array/data (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/mockdeletetoget (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0 (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/mockdeletetoget (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1 (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/mockdeletetoget (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2 (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/mockdeletetoget (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3 (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/mockdeletetoget (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4 (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4/mockdeletetoget (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4/path5")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4/path5 (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4/path5/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4/path5/mockdeletetoget (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4/path5/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4/path5/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4/path5/path6")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4/path5/path6 (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4/path5/path6/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4/path5/path6/mockdeletetoget (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4/path5/path6/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4/path5/path6/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_path7() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4/path5/path6/path7")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4/path5/path6/path7 (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_path7_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4/path5/path6/path7/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4/path5/path6/path7/mockdeletetoget (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__data_document_id_path0_path1_path2_path3_path4_path5_path6_path7_mockputtopost() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/data/document/test-id/path0/path1/path2/path3/path4/path5/path6/path7/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/data/document/{{id}}/path0/path1/path2/path3/path4/path5/path6/path7/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__document_id_view_count() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/document/test-id/view/count")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/document/{{id}}/view/count (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_download_document_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/download/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/fileinfo/download/document/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_upload_document_docId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/upload/document/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/fileinfo/upload/document/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_id() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/fileinfo/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_id_document_documentId() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/test-id/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/fileinfo/{{id}}/document/{{docId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__fileinfo_id_mockdeletetoget() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/fileinfo/test-id/mockdeletetoget")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/fileinfo/{{id}}/mockdeletetoget (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_assemble_control__queryview_flag_definition() {
        let router = oa4rust::cms_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/flag/test-id/definition/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_assemble_control: /jaxrs/queryview/flag/{{view_flag}}/definition/{{query_flag}} (GET)"
        );
    }

    // 鈹€鈹€ x_cms_core_entity 鈫?cms_core_entity (6 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__cms_core_entity__article_create() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/article/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_core_entity: /jaxrs/cms/article/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_entity__article_list() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/article/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_core_entity: /jaxrs/cms/article/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_entity__article_get() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/article/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_core_entity: /jaxrs/cms/article/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_entity__category_create() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/category/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_core_entity: /jaxrs/cms/category/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_entity__category_list() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/category/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_core_entity: /jaxrs/cms/category/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_entity__category_get() {
        let router = oa4rust::cms_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/category/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_core_entity: /jaxrs/cms/category/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_cms_core_express 鈫?cms_core_express (2 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__cms_core_express__content_detail() {
        let router = oa4rust::cms_core_express::router(shared::testing::mock_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/core/express/content/detail/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_core_express: /jaxrs/cms/core/express/content/detail/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__cms_core_express__content_list() {
        let router = oa4rust::cms_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/cms/core/express/content/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on cms_core_express: /jaxrs/cms/core/express/content/list (GET)"
        );
    }

    // 鈹€鈹€ x_component_assemble_control 鈫?component_assemble_control (10 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__component_assemble_control__component_delete_all() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component/assemble/control/component/delete/all")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component_assemble_control: /jaxrs/component/assemble/control/component/delete/all (POST)"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__status_list() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component/assemble/control/status/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component_assemble_control: /jaxrs/component/assemble/control/status/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__create_component() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/create/component")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component_assemble_control: /jaxrs/component_assemble_control/create/component (GET)"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__delete_component() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/delete/component")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component_assemble_control: /jaxrs/component_assemble_control/delete/component (GET)"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__get_component() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/get/component")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component_assemble_control: /jaxrs/component_assemble_control/get/component (GET)"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__get_control_config() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/get/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component_assemble_control: /jaxrs/component_assemble_control/get/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__list_components() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/list/components")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component_assemble_control: /jaxrs/component_assemble_control/list/components (GET)"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__list_control_categories() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/list/control/categories")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component_assemble_control: /jaxrs/component_assemble_control/list/control/categories (GET)"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__save_component() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/save/component")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component_assemble_control: /jaxrs/component_assemble_control/save/component (GET)"
        );
    }
    #[tokio::test]
    async fn parity__component_assemble_control__update_control_config() {
        let router = oa4rust::component_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component_assemble_control/update/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component_assemble_control: /jaxrs/component_assemble_control/update/control/config (GET)"
        );
    }

    // 鈹€鈹€ x_component_core_entity 鈫?component (3 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__component__count() {
        let router = oa4rust::component::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component: /jaxrs/component/count (GET)"
        );
    }
    #[tokio::test]
    async fn parity__component__list_all() {
        let router = oa4rust::component::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component/list/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component: /jaxrs/component/list/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__component__get_component() {
        let router = oa4rust::component::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/component/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on component: /jaxrs/component/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_console 鈫?console (7 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__console__clear_cache() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/cache/clear/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on console: /jaxrs/console/cache/clear/{{type}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__console__execute_command() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/command/execute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on console: /jaxrs/console/command/execute (POST)"
        );
    }
    #[tokio::test]
    async fn parity__console__get_logs() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/logs/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on console: /jaxrs/console/logs/{{type}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__console__get_metric() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/metric/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on console: /jaxrs/console/metric/{{name}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__console__send_message() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/send/message")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on console: /jaxrs/console/send/message (POST)"
        );
    }
    #[tokio::test]
    async fn parity__console__get_status() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on console: /jaxrs/console/status (GET)"
        );
    }
    #[tokio::test]
    async fn parity__console__get_system_info() {
        let router = oa4rust::console::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/console/system/info")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on console: /jaxrs/console/system/info (GET)"
        );
    }

    // 鈹€鈹€ x_correlation_core_entity 鈫?correlation (3 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__correlation__list_cms_correlations() {
        let router = oa4rust::correlation::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/type/cms/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation: /jaxrs/correlation/type/cms/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation__check_cms_readable() {
        let router = oa4rust::correlation::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/type/cms/readable")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation: /jaxrs/correlation/type/cms/readable (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation__list_process_platform_correlations() {
        let router = oa4rust::correlation::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/type/processplatform/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation: /jaxrs/correlation/type/processplatform/list (GET)"
        );
    }

    // 鈹€鈹€ x_correlation_core_express 鈫?correlation_core_express (2 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__correlation_core_express__get_status() {
        let router = oa4rust::correlation_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/core/express/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_core_express: /jaxrs/correlation/core/express/status (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_core_express__sync_correlation() {
        let router = oa4rust::correlation_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/core/express/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_core_express: /jaxrs/correlation/core/express/sync (GET)"
        );
    }

    // 鈹€鈹€ x_correlation_service_processing 鈫?correlation_service_processing (20 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_delete_type_cms_document_document() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/delete/type/cms/document/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/delete/type/cms/document/{{document}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_delete_type_processplatform_job_job() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/delete/type/processplatform/job/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/delete/type/processplatform/job/{{job}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_list_type_cms_document_document() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/list/type/cms/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/list/type/cms/document/{{document}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_list_type_cms_document_document_site_site() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/list/type/cms/document/test-id/site/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/list/type/cms/document/{{document}}/site/{{site}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_list_type_processplatform_job_job() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/list/type/processplatform/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/list/type/processplatform/job/{{job}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_list_type_processplatform_job_job_site_site() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/list/type/processplatform/job/test-id/site/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/list/type/processplatform/job/{{job}}/site/{{site}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_readable_type_cms() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/readable/type/cms")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/readable/type/cms (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_readable_type_processplatform() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/readable/type/processplatform")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/readable/type/processplatform (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_type_cms_document_document() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/type/cms/document/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/type/cms/document/{{document}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_type_processplatform_job_job() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/type/processplatform/job/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/type/processplatform/job/{{job}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_update_type_cms_document_document() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/update/type/cms/document/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/update/type/cms/document/{{document}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__correlation_update_type_processplatform_job_job() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/correlation/update/type/processplatform/job/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/correlation/update/type/processplatform/job/{{job}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__create_correlation() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__delete_correlation() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__link_service() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/link")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/link (POST)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__get_link() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/link/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/link/{{sourceType}}/{{sourceId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__list_correlations() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/list/{{personId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__save_correlation() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/save/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__unlink_service() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/unlink/test-id/test-id/test-id/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/unlink/{{sourceType}}/{{sourceId}}/{{targetType}}/{{targetId}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__correlation_service_processing__get_correlation() {
        let router = oa4rust::correlation_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/correlation/service/processing/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on correlation_service_processing: /jaxrs/correlation/service/processing/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_file_assemble_control 鈫?file_assemble_control (12 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__file_assemble_control__anonymous_file_id_download_stream() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/anonymous/file/test-id/download/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/anonymous/file/{{id}}/download/stream (GET)"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment_id_download_stream() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/attachment/download/test-id/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/attachment/download/{{attid}}/stream (GET)"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__attachment2_id_office_preview_type_type() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/attachment2/test-id/office/preview/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/file/assemble/control/attachment2/{{id}}/office/preview/type/{{type}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__create_file() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/file/assemble/control/file/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__delete_file() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/file/assemble/control/file/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__list_files() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/file/assemble/control/file/list/{{folderId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__upload_file() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/file/assemble/control/file/upload (POST)"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__get_file() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/assemble/control/file/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/file/assemble/control/file/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__create_file_entity() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/core/entity/file/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/file/core/entity/file/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__delete_file_entity() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/core/entity/file/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/file/core/entity/file/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__update_file_entity() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/core/entity/file/update/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/file/core/entity/file/update/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__file_assemble_control__file_id_download_stream() {
        let router = oa4rust::file_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/test-id/download/stream")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file_assemble_control: /jaxrs/file/{{id}}/download/stream (GET)"
        );
    }

    // 鈹€鈹€ x_file_core_entity 鈫?file (9 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__file__complex_top() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/complex/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file: /jaxrs/file/complex/top (GET)"
        );
    }
    #[tokio::test]
    async fn parity__file__file_download() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file: /jaxrs/file/download/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__file__folder_create() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file: /jaxrs/file/folder/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__file__folder_list_top() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/list/top")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file: /jaxrs/file/folder/list/top (GET)"
        );
    }
    #[tokio::test]
    async fn parity__file__folder_list_with_folder() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file: /jaxrs/file/folder/list/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__file__folder_remove() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/remove")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file: /jaxrs/file/folder/remove (POST)"
        );
    }
    #[tokio::test]
    async fn parity__file__folder_update() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/folder/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file: /jaxrs/file/folder/update (POST)"
        );
    }
    #[tokio::test]
    async fn parity__file__permission_set() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/permission/set")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file: /jaxrs/file/permission/set (POST)"
        );
    }
    #[tokio::test]
    async fn parity__file__file_upload() {
        let router = oa4rust::file::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/file/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on file: /jaxrs/file/upload (POST)"
        );
    }

    // 鈹€鈹€ x_general_assemble_control 鈫?general_assemble_control (61 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__general_assemble_control__crate() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/area/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_1() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/area/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_2() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/area/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_3() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/list/province/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/area/list/province/{{province}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_4() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/list/province/test-id/city/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/area/list/province/{{province}}/city/{{city}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_5() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/list/province/test-id/city/test-id/district/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/area/list/province/{{province}}/city/{{city}}/district/{{district}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_6() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/update/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/area/update/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_7() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/area/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/area/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_8() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/attendscope/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_9() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/attendscope/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_10() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/attendscope/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_11() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/attendscope/save/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_12() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/attendscope/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/attendscope/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_13() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/ecnet/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/ecnet/check (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_14() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/result/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/excel/result/flag/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_15() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/excel/upload (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_16() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/upload/with/url")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/excel/upload/with/url (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_17() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/excel/{{\\\"excelName\\\"}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_18() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/excel/test-id/sheetList")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/excel/{{\\\"excelName\\\"}}/sheetList (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_19() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/generalfile/download/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/generalfile/download/flag/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_20() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/generalfile/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/generalfile/flag/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_21() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/generalfile/flag/test-id/binary/base64")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/generalfile/flag/{{flag}}/binary/base64 (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_22() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/invoice/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_23() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/invoice/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_24() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/download/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/invoice/download/flag/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_25() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/invoice/get/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_26() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/invoice/list/paging/{{page}}/size/{{size}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_27() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/update/apply/status/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/invoice/update/apply/status/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_28() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/update/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/invoice/update/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_29() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/invoice/upload (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_30() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/upload/for/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/invoice/upload/for/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_31() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/invoice/upload/with/url")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/invoice/upload/with/url (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_32() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/office/html/to/word")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/office/html/to/word (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_33() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/office/html/to/word/result/flag/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/office/html/to/word/result/flag/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_34() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/permissions/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/permissions/{{module}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_35() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/qrcode/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/qrcode/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_36() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/qrcode/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/qrcode/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_37() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/qrcode/width/test-id/height/test-id/text/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/qrcode/width/{{width}}/height/{{height}}/text/{{text}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_38() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/qrcode/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/qrcode/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_39() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/securityclearance/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_40() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/securityclearance/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_41() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/enable")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/securityclearance/enable (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_42() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/object")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/securityclearance/object (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_43() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/subject")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/securityclearance/subject (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_44() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/system")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/securityclearance/system (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_45() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/update/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/securityclearance/update/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_46() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/securityclearance/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/securityclearance/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_47() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/status (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_48() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/status/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/status/update (POST)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_49() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/upgrade/2021090901")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/upgrade/2021090901 (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_50() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/upgrade/2021090902")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/upgrade/2021090902 (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_51() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/between/holiday/count/start/test-id/end/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/worktime/between/holiday/count/start/{{startDate}}/end/{{endDate}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_52() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/between/minutes/start/test-id/end/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/worktime/between/minutes/start/{{start}}/end/{{end}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_53() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/forward/days/start/test-id/days/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/worktime/forward/days/start/{{start}}/days/{{days}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_54() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/forward/minutes/start/test-id/minutes/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/worktime/forward/minutes/start/{{start}}/minutes/{{minutes}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_55() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/indefined/holiday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/worktime/indefined/holiday/{{date}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_56() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/indefined/workday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/worktime/indefined/workday/{{date}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_57() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/is/holiday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/worktime/is/holiday/{{date}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_58() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/is/workday/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/worktime/is/workday/{{date}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__general_assemble_control__crate_59() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/is/worktime/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/worktime/is/worktime/{{date}} (GET)"
        );
    }
    #[ignore]
    #[tokio::test]
    async fn parity__general_assemble_control__crate_60() {
        let router = oa4rust::general_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/general/assemble/control/worktime/minutes/of/workday")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on general_assemble_control: /jaxrs/general/assemble/control/worktime/minutes/of/workday (GET)"
        );
    }

    // 鈹€鈹€ x_hotpic_assemble_control 鈫?hotpic_assemble_control (36 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_bbs_id() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/cipher/hotpic/bbs/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/cipher/hotpic/bbs/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_cms_id() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/cipher/hotpic/cms/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/cipher/hotpic/cms/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_filter_list_page_page_count_count() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/cipher/hotpic/filter/list/page/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/cipher/hotpic/filter/list/page/{{page}}/count/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_id() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/cipher/hotpic/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/cipher/hotpic/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__get_control_config() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_control_applications() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/list/control/applications")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/list/control/applications (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_control_panels() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/list/control/panels")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/list/control/panels (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__update_control_config() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/update/control/config")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/update/control/config (POST)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_application_infoId() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/application/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/user/hotpic/application/{{infoId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_changeTitle() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/changeTitle")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/user/hotpic/changeTitle (POST)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_exists_check() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/exists/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/user/hotpic/exists/check (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_filter_list_page_page_count_count() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/filter/list/page/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/user/hotpic/filter/list/page/{{page}}/count/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_id() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/assemble/control/user/hotpic/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/assemble/control/user/hotpic/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__create_hotpic() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/create/hotpic")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/create/hotpic (POST)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__delete_hotpic() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/delete/hotpic")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/delete/hotpic (POST)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__get_hotpic() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/get/hotpic/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/get/hotpic/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_hotpics() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/list/hotpics")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/list/hotpics (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__save_hotpic() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/save/hotpic")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic/save/hotpic (POST)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_bbs_id_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/cipher/hotpic/bbs/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/cipher/hotpic/bbs/id (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_cms_id_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/cipher/hotpic/cms/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/cipher/hotpic/cms/id (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_filter_list_page_page_count_count_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/cipher/hotpic/filter/list/page/page/count/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/cipher/hotpic/filter/list/page/page/count/count (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__cipher_hotpic_id_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/cipher/hotpic/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/cipher/hotpic/id (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__create_hotpic_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/create/hotpic")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/create/hotpic (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__delete_hotpic_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/delete/hotpic")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/delete/hotpic (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__get_control_config_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/get/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/get/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__get_hotpic_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/get/hotpic")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/get/hotpic (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_control_applications_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/list/control/applications")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/list/control/applications (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_control_panels_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/list/control/panels")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/list/control/panels (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__list_hotpics_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/list/hotpics")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/list/hotpics (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__save_hotpic_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/save/hotpic")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/save/hotpic (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__update_control_config_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/update/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/update/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_application_infoId_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/user/hotpic/application/infoId")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/user/hotpic/application/infoId (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_changeTitle_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/user/hotpic/changeTitle")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/user/hotpic/changeTitle (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_exists_check_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/user/hotpic/exists/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/user/hotpic/exists/check (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_filter_list_page_page_count_count_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/user/hotpic/filter/list/page/page/count/count")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/user/hotpic/filter/list/page/page/count/count (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic_assemble_control__user_hotpic_id_1() {
        let router = oa4rust::hotpic_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic_assemble_control/user/hotpic/id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic_assemble_control: /jaxrs/hotpic_assemble_control/user/hotpic/id (GET)"
        );
    }

    // 鈹€鈹€ x_hotpic_core_entity 鈫?hotpic (3 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__hotpic__exists_check() {
        let router = oa4rust::hotpic::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/user/hotpic/exists/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic: /jaxrs/hotpic/user/hotpic/exists/check (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic__list_by_application_and_info_id() {
        let router = oa4rust::hotpic::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/user/hotpic/test-id/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic: /jaxrs/hotpic/user/hotpic/{{application}}/{{infoId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__hotpic__get_by_id() {
        let router = oa4rust::hotpic::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/hotpic/user/hotpic/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on hotpic: /jaxrs/hotpic/user/hotpic/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_jpush_assemble_control 鈫?jpush_assemble_control (32 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__jpush_assemble_control__get_control_config() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/assemble/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_admin_unbind_all_person() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/admin/unbind/all/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/assemble/control/device/admin/unbind/all/person (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_bind() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/bind")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/assemble/control/device/bind (POST)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_check_deviceName_deviceType_pushType() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/check/deviceName/deviceType/pushType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/assemble/control/device/check/deviceName/deviceType/pushType (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_config_push_type() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/config/push/type")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/assemble/control/device/config/push/type (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_list_pushType() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/list/pushType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/assemble/control/device/list/pushType (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_unbind_deviceName_deviceType() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/unbind/deviceName/deviceType")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/assemble/control/device/unbind/deviceName/deviceType (POST)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_unbind_new_deviceName_deviceType_pushType() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/device/unbind/new/deviceName/deviceType/pushType")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/assemble/control/device/unbind/new/deviceName/deviceType/pushType (POST)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__list_control_apps() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/list/control/apps")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/assemble/control/list/control/apps (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__message_test_send() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/message/test/send")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/assemble/control/message/test/send (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__update_control_config() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/assemble/control/update/control/config")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/assemble/control/update/control/config (POST)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__create_jpush() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__delete_jpush() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__get_jpush() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/get/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__list_jpushs() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__save_jpush() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush/save/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__create_jpush_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/create/jpush")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/create/jpush (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__delete_jpush_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/delete/jpush")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/delete/jpush (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_admin_unbind_all_person_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/admin/unbind/all/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/device/admin/unbind/all/person (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_bind_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/bind")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/device/bind (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_check_deviceName_deviceType_pushType_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/check/deviceName/deviceType/pushType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/device/check/deviceName/deviceType/pushType (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_config_push_type_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/config/push/type")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/device/config/push/type (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_list_pushType_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/list/pushType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/device/list/pushType (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_unbind_deviceName_deviceType_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/unbind/deviceName/deviceType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/device/unbind/deviceName/deviceType (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__device_unbind_new_deviceName_deviceType_pushType_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/device/unbind/new/deviceName/deviceType/pushType")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/device/unbind/new/deviceName/deviceType/pushType (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__get_control_config_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/get/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/get/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__get_jpush_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/get/jpush")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/get/jpush (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__list_control_apps_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/list/control/apps")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/list/control/apps (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__list_jpushs_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/list/jpushs")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/list/jpushs (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__message_test_send_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/message/test/send")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/message/test/send (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__save_jpush_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/save/jpush")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/save/jpush (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush_assemble_control__update_control_config_1() {
        let router = oa4rust::jpush_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush_assemble_control/update/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush_assemble_control: /jaxrs/jpush_assemble_control/update/control/config (GET)"
        );
    }

    // 鈹€鈹€ x_jpush_core_entity 鈫?jpush (6 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__jpush__hello() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/hello/world")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush: /hello/world (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush__device_create() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/device/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush: /jaxrs/jpush/device/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__jpush__device_list() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/device/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush: /jaxrs/jpush/device/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush__device_get() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/device/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush: /jaxrs/jpush/device/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush__template_list() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/template/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush: /jaxrs/jpush/template/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__jpush__template_get() {
        let router = oa4rust::jpush::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/jpush/template/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on jpush: /jaxrs/jpush/template/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_meeting_assemble_control 鈫?meeting_assemble_control (42 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__meeting_assemble_control__building_list() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/building/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__building_list_like_pinyin_key() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/like/pinyin/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/building/list/like/pinyin/{{key}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__building_list_like_key() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/like/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/building/list/like/{{key}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__building_list_pinyininitial_key() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/list/pinyininitial/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/building/list/pinyininitial/{{key}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__building_id() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/building/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/building/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__config_system_config() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/config/system/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/config/system/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__config_system_config_manage() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/config/system/config/manage")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/config/system/config/manage (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__create_meeting_control() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__delete_meeting_control() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/delete/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/delete/{{id}} (DELETE)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__list_meeting_controls() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/list/{{\\\"meetingId\\\"}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__create_meeting() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__delete_meeting() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_applied_completed() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/applied/completed")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/applied/completed (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_applied_processing() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/applied/processing")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/applied/processing (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_applied_wait() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/applied/wait")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/applied/wait (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_apply_page_size_size() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/apply/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/apply/{{page}}/size/{{size}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_coming_day_count() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/coming/day/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/coming/day/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_invited_completed() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/invited/completed")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/invited/completed (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_invited_processing() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/invited/processing")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/invited/processing (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_invited_rejected() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/invited/rejected")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/invited/rejected (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_invited_wait() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/invited/wait")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/invited/wait (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_wait_accept() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/wait/accept")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/wait/accept (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_wait_confirm() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/wait/confirm")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/wait/confirm (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_year_year_month_month() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/year/test-id/month/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/year/{{year}}/month/{{month}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_year_year_month_month_all() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/year/test-id/month/test-id/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/year/{{year}}/month/{{month}}/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_year_year_month_month_day_day() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/year/test-id/month/test-id/day/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/year/{{year}}/month/{{month}}/day/{{day}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_list_year_year_month_month_day_day_all() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/list/year/test-id/month/test-id/day/test-id/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/list/year/{{year}}/month/{{month}}/day/{{day}}/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__save_meeting() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/save/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_accept() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/accept")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/{{id}}/accept (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_add_invite() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/add/invite")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/{{id}}/add/invite (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_checkin() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/checkin")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/{{id}}/checkin (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_confirm_allow() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/confirm/allow")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/{{id}}/confirm/allow (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_confirm_deny() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/confirm/deny")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/{{id}}/confirm/deny (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_delete_invite() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/delete/invite")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/{{id}}/delete/invite (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_manual_completed() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/manual/completed")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/{{id}}/manual/completed (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_modify_completedtime() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/modify/completedtime")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/{{id}}/modify/completedtime (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_modify_starttime() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/modify/starttime")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/{{id}}/modify/starttime (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__meeting_id_reject() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/meeting/test-id/reject")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/meeting/{{id}}/reject (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__openmeeting_list_room() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/openmeeting/list/room")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/openmeeting/list/room (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__room_list() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/room/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/room/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting_assemble_control__room_id() {
        let router = oa4rust::meeting_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/assemble/control/room/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting_assemble_control: /jaxrs/meeting/assemble/control/room/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_meeting_core_entity 鈫?meeting (9 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__meeting__building_list() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/building/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting: /jaxrs/meeting/building/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting__create_meeting() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting: /jaxrs/meeting/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting__list_meetings() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting: /jaxrs/meeting/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting__openmeeting_list_room() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/openmeeting/list/room")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting: /jaxrs/meeting/openmeeting/list/room (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting__room_list() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/room/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting: /jaxrs/meeting/room/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting__list_schedule() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/schedule/days/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting: /jaxrs/meeting/schedule/days/{{days}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting__add_participant() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/test-id/participant/add")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting: /jaxrs/meeting/{{\\\"meetingId\\\"}}/participant/add (POST)"
        );
    }
    #[tokio::test]
    async fn parity__meeting__list_participants() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/test-id/participant/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting: /jaxrs/meeting/{{\\\"meetingId\\\"}}/participant/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__meeting__get_meeting() {
        let router = oa4rust::meeting::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/meeting/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on meeting: /jaxrs/meeting/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_message_assemble_communicate 鈫?message_assemble_communicate (58 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_list_consume_count_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/list/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/consume/list/{{consume}}/count/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_list_consume_currentperson_count_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/list/test-id/currentperson/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/consume/list/{{consume}}/currentperson/count/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_list_consume_person_person_count_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/list/test-id/person/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/consume/list/{{consume}}/person/{{person}}/count/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_type_type() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/consume/type/{{type}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_type_type_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/type/test-id/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/consume/type/{{type}}/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__consume_id_type_type() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/consume/test-id/type/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/consume/{{id}}/type/{{type}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_business_businessId() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/business/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/business/{{businessId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_list_my() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/list/my")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/list/my (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_list_with_person() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/list/with/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/list/with/person (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_group() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/group")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/group (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_group_mockdeletetoget() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/group/mockdeletetoget")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/group/mockdeletetoget (DELETE)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_group_quit_self() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/group/quit/self")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/group/quit/self (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_icon() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/icon")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/icon (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_read() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/read")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/read (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_read_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/read/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/read/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_single() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/single")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/single (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_single_mockdeletetoget() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/single/mockdeletetoget")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/single/mockdeletetoget (DELETE)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_top_cancel() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/top/cancel")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/top/cancel (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_top_cancel_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/top/cancel/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/top/cancel/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_top_set() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/top/set")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/top/set (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_conversation_id_top_set_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/conversation/test-id/top/set/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/conversation/{{id}}/top/set/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_manager_config() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/manager/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/manager/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/msg (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_clear() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/clear")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/msg/clear (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_collection() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/collection")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/msg/collection (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_collection_list_page_size_size() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/collection/list/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/msg/collection/list/{{page}}/size/{{size}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_collection_remove() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/collection/remove")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/msg/collection/remove (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_download_id() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/download/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/msg/download/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_download_id_image_width_width_height_height() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/download/test-id/image/width/test-id/height/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/msg/download/{{id}}/image/width/{{width}}/height/{{height}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_list_object() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/list/object")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/msg/list/object (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_list_page_size_size() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/list/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/msg/list/{{page}}/size/{{size}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_revoke_id() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/revoke/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/msg/revoke/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__im_msg_upload_conversationId_type_type() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/im/msg/upload/test-id/type/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/im/msg/upload/{{\\\"conversationId\\\"}}/type/{{type}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_currentperson_consumed() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/currentperson/consumed")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/currentperson/consumed (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_currentperson_consumed_all() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/currentperson/consumed/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/currentperson/consumed/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_currentperson_consumed_mockputtopost() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/currentperson/consumed/mockputtopost")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/currentperson/consumed/mockputtopost (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_consumed_count_count_asc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/consumed/count/test-id/asc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/list/currentperson/consumed/count/{{count}}/asc (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_consumed_count_count_desc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/consumed/count/test-id/desc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/list/currentperson/consumed/count/{{count}}/desc (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_count_count_asc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/count/test-id/asc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/list/currentperson/count/{{count}}/asc (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_count_count_desc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/count/test-id/desc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/list/currentperson/count/{{count}}/desc (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_noim_count_count_desc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/noim/count/test-id/desc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/list/currentperson/noim/count/{{count}}/desc (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_not_consumed_count_count_asc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/not/consumed/count/test-id/asc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/list/currentperson/not/consumed/count/{{count}}/asc (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_currentperson_not_consumed_count_count_desc() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/currentperson/not/consumed/count/test-id/desc")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/list/currentperson/not/consumed/count/{{count}}/desc (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_id_next_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/list/{{id}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__instant_list_id_prev_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/instant/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/instant/list/{{id}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mark_read() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mark_read/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/mark_read/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_enable_type() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/enable/type")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/mass/enable/type (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_list_id_next_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/mass/list/{{id}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_list_id_prev_count() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/mass/list/{{id}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_id() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/mass/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__mass_id_mockdeletetoget() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/mass/test-id/mockdeletetoget")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/mass/{{id}}/mockdeletetoget (DELETE)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__message_custom_create() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/message/custom/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/message/custom/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__message_list_paging_page_size_size() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/message/list/paging/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/message/list/paging/{{page}}/size/{{size}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__receive_list() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/receive/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/receive/{{consume}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message_assemble_communicate__send_message() {
        let router = oa4rust::message_assemble_communicate::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/assemble/communicate/send")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message_assemble_communicate: /jaxrs/message/assemble/communicate/send (POST)"
        );
    }

    // 鈹€鈹€ x_message_core_entity 鈫?message (5 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__message__consume_list() {
        let router = oa4rust::message::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/consume/list/test-id/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message: /jaxrs/message/consume/list/{{consume}}/count/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message__update_single() {
        let router = oa4rust::message::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/consume/test-id/type/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message: /jaxrs/message/consume/{{id}}/type/{{type}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__message__custom_create() {
        let router = oa4rust::message::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/custom/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message: /jaxrs/message/custom/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message__mark_read() {
        let router = oa4rust::message::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/mark_read/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message: /jaxrs/message/mark_read/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__message__unread_count() {
        let router = oa4rust::message::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/message/unread/count/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on message: /jaxrs/message/unread/count/{{consume}} (GET)"
        );
    }

    // 鈹€鈹€ x_mind_assemble_control 鈫?mind_assemble_control (8 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__mind_assemble_control__get_control_config() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind_assemble_control: /jaxrs/mind/assemble/control/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__update_control_config() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/config/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind_assemble_control: /jaxrs/mind/assemble/control/config/update (POST)"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__folder_move_folderId() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/move/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind_assemble_control: /jaxrs/mind/assemble/control/folder/move/{{folderId}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__save_folder() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/save")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind_assemble_control: /jaxrs/mind/assemble/control/folder/save (POST)"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__list_folders() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/tree/my")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind_assemble_control: /jaxrs/mind/assemble/control/folder/tree/my (GET)"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__get_folder() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind_assemble_control: /jaxrs/mind/assemble/control/folder/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__folder_id_force() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/test-id/force")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind_assemble_control: /jaxrs/mind/assemble/control/folder/{{id}}/force (POST)"
        );
    }
    #[tokio::test]
    async fn parity__mind_assemble_control__update_folder() {
        let router = oa4rust::mind_assemble_control::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/assemble/control/folder/test-id/update")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind_assemble_control: /jaxrs/mind/assemble/control/folder/{{id}}/update (POST)"
        );
    }

    // 鈹€鈹€ x_mind_core_entity 鈫?mind (10 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__mind__crate() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/folder")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind: /jaxrs/mind/folder (POST)"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_1() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/folder/tree/my")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind: /jaxrs/mind/folder/tree/my (GET)"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_2() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/folder/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind: /jaxrs/mind/folder/{{id}} (DELETE)"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_3() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/folder/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind: /jaxrs/mind/folder/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_4() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/mind")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind: /jaxrs/mind/mind (POST)"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_5() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/mind/list/test-id/version")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind: /jaxrs/mind/mind/list/{{id}}/version (GET)"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_6() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/mind/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind: /jaxrs/mind/mind/{{id}} (DELETE)"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_7() {
        let router = oa4rust::mind::router(shared::testing::mock_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/mind/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind: /jaxrs/mind/mind/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_8() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/mind/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind: /jaxrs/mind/mind/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__mind__crate_9() {
        let router = oa4rust::mind::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mind/version")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on mind: /jaxrs/mind/version (POST)"
        );
    }

    // 鈹€鈹€ x_organization_assemble_authentication 鈫?auth (46 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__auth__andfx() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/andfx/moa/sso/token/test-id/enter/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/andfx/moa/sso/token/{{token}}/enter/{{enterId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__logout() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication (DELETE)"
        );
    }
    #[tokio::test]
    async fn parity__auth__whoami() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__login() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__bind() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/bind")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/bind (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__bind_poll() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/bind/meta/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/bind/meta/{{meta}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__captcha_default() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/captcha")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/captcha (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__captcha_with_size() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/captcha/width/test-id/height/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/captcha/width/{{width}}/height/{{height}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__check_token() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/check/token")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/check/token (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__code() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/code")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/code (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__code_send() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/code/credential/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/code/credential/{{credential}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__group_list() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/group/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/group/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__login_1() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/login")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/login (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__logout_1() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/logout")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/logout (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/bind/name/test-id/code/test-id/redirecturi/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/oauth/bind/name/{{name}}/code/{{code}}/redirecturi/{{redirectUri}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_1() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/dingding/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/oauth/dingding/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_2() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/oauth/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_3() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/login/dingding/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/oauth/login/dingding/code/{{code}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_4() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/login/name/test-id/code/test-id/redirecturi/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/oauth/login/name/{{name}}/code/{{code}}/redirecturi/{{redirectUri}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_5() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/login/qywx/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/oauth/login/qywx/code/{{code}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_6() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/name/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/oauth/name/{{name}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__oauth_7() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oauth/qywx/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/oauth/qywx/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__oidc_authorize() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oidc/authorize")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/oidc/authorize (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__oidc_callback() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/oidc/callback")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/oidc/callback (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__refresh() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/refresh")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/refresh (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__role_list() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/role/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/role/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__safe_logout() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/safe/logout")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/safe/logout (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__sms_send_handler() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/sms/send")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/sms/send (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__sso() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/sso")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/sso (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__sso_1() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/sso/client/test-id/token/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/sso/client/{{client}}/token/{{token}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__sso_2() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/sso/encrypt")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/sso/encrypt (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__switch_user() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/switchuser")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/switchuser (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__two_factor() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/two_factor")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/two_factor (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__unit_list() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/unit/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/unit/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__whoami_1() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/authentication/who")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/authentication/who (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__mpweixin_bind_code() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mpweixin/bind/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/mpweixin/bind/code/{{code}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__mpweixin_bind_openid() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mpweixin/bind/openid/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/mpweixin/bind/openid/{{openid}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__mpweixin_login() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mpweixin/login/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/mpweixin/login/code/{{code}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__mpweixin_test_send() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/mpweixin/menu/test/send/to/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/mpweixin/menu/test/send/to/{{person}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__qiyeweixin_login() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/qiyeweixin/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/qiyeweixin/code/{{code}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__qiyeweixin_jssdk_sign() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/qiyeweixin/jssdk/sign/info")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/qiyeweixin/jssdk/sign/info (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__qiyeweixin_update_person_detail() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/qiyeweixin/update/person/detail/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/qiyeweixin/update/person/detail/{{code}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__verify() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/secret/captcha/verify")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/secret/captcha/verify (POST)"
        );
    }
    #[tokio::test]
    async fn parity__auth__welink_login() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/welink/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/welink/code/{{code}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__zwdingding_login() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/zhengwudingding/code/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/zhengwudingding/code/{{code}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__auth__zwdingding_info() {
        let router = oa4rust::auth::router(shared::testing::test_pool(), shared::RateLimiter::new(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/zhengwudingding/info")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on auth: /jaxrs/zhengwudingding/info (GET)"
        );
    }

    // 鈹€鈹€ x_organization_assemble_control 鈫?control (25 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__control__group() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/group (POST)"
        );
    }
    #[tokio::test]
    async fn parity__control__group_1() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/group/list/{{flag}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__group_2() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/group/list/{{flag}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__group_3() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/group/{{flag}} (DELETE)"
        );
    }
    #[tokio::test]
    async fn parity__control__group_4() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/group/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__group_5() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/group/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/group/{{flag}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__control__person() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/person (POST)"
        );
    }
    #[tokio::test]
    async fn parity__control__person_1() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/person/list/{{flag}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__person_2() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/person/list/{{flag}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__person_3() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/person/{{flag}} (DELETE)"
        );
    }
    #[tokio::test]
    async fn parity__control__person_4() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/person/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__person_5() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/person/{{flag}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__control__role() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/role (POST)"
        );
    }
    #[tokio::test]
    async fn parity__control__role_1() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/role/list/{{flag}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__role_2() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/role/list/{{flag}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__role_3() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/role/{{flag}} (DELETE)"
        );
    }
    #[tokio::test]
    async fn parity__control__role_4() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/role/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__role_5() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/role/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/role/{{flag}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__control__unit() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/unit (POST)"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_1() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/unit/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_2() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/test-id/next/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/unit/list/{{flag}}/next/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_3() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/list/test-id/prev/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/unit/list/{{flag}}/prev/{{count}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_4() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/test-id")
                    .method(axum::http::Method::DELETE)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/unit/{{flag}} (DELETE)"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_5() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/unit/{{flag}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__control__unit_6() {
        let router = oa4rust::control::control_router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/unit/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on control: /jaxrs/unit/{{flag}} (PUT)"
        );
    }

    // 鈹€鈹€ x_organization_assemble_express 鈫?organization_assemble_express (4 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__organization_assemble_express__get_express_config() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/assemble/express/config/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_assemble_express: /jaxrs/organization/assemble/express/config/get (GET)"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__sync_organization_data() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/assemble/express/data/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_assemble_express: /jaxrs/organization/assemble/express/data/sync (GET)"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__get_express_status() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/assemble/express/status/get")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_assemble_express: /jaxrs/organization/assemble/express/status/get (GET)"
        );
    }
    #[tokio::test]
    async fn parity__organization_assemble_express__list_organization_units() {
        let router = oa4rust::organization_assemble_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/assemble/express/units/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_assemble_express: /jaxrs/organization/assemble/express/units/list (GET)"
        );
    }

    // 鈹€鈹€ x_organization_assemble_personal 鈫?personal (19 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__personal__get_person() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person (GET)"
        );
    }
    #[tokio::test]
    async fn parity__personal__edit_person() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__personal__icon() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/icon/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/icon/upload (POST)"
        );
    }
    #[tokio::test]
    async fn parity__personal__icon_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/icon/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/icon/{{person}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__personal__password() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/password")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/password (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__personal__regist() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/regist (POST)"
        );
    }
    #[tokio::test]
    async fn parity__personal__regist_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/check/email/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/regist/check/email/{{email}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__personal__regist_2() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/check/mobile/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/regist/check/mobile/{{mobile}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__personal__regist_3() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/check/name/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/regist/check/name/{{name}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__personal__regist_4() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/regist/code")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/regist/code (POST)"
        );
    }
    #[tokio::test]
    async fn parity__personal__signature() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/signature/delete/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/signature/delete/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__personal__signature_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/signature/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/signature/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__personal__signature_2() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/signature/manager/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/signature/manager/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__personal__signature_3() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/person/signature/upload")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/person/signature/upload (POST)"
        );
    }
    #[tokio::test]
    async fn parity__personal__reset() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/reset")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/reset (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__personal__reset_1() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/reset/check/credential/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/reset/check/credential/{{credential}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__personal__reset_2() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/reset/check/password/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/reset/check/password/{{password}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__personal__reset_3() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/reset/code/credential/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/reset/code/credential/{{credential}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__personal__reset_4() {
        let router = oa4rust::personal::router(shared::testing::test_pool(), shared::SessionManager::new());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/reset/password/anonymous")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on personal: /jaxrs/reset/password/anonymous (POST)"
        );
    }

    // 鈹€鈹€ x_organization_core_entity 鈫?organization_core_entity (18 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__organization_core_entity__bind_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/bind")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/bind (POST)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__bind_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/bind/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/bind/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__bind_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/bind/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/bind/{{id}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__custom_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/custom")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/custom (POST)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__custom_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/custom/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/custom/list/{{identityId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__custom_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/custom/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/custom/{{id}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__definition_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/definition")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/definition (POST)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__definition_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/definition/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/definition/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__definition_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/definition/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/definition/{{id}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__group_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/group")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/group (POST)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__group_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/group/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/group/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__group_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/group/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/group/{{id}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__identity_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/identity")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/identity (POST)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__identity_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/identity/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/identity/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__identity_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/identity/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/identity/{{id}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__person_create() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/person")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/person (POST)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__person_list() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/person/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/person/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_entity__person_update() {
        let router = oa4rust::organization_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/person/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_entity: /jaxrs/organization/person/{{id}} (PUT)"
        );
    }

    // 鈹€鈹€ x_organization_core_express 鈫?organization_core_express (3 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__organization_core_express__get_config() {
        let router = oa4rust::organization_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/core/express/config")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_express: /jaxrs/organization/core/express/config (GET)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_express__get_status() {
        let router = oa4rust::organization_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/core/express/status")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_express: /jaxrs/organization/core/express/status (GET)"
        );
    }
    #[tokio::test]
    async fn parity__organization_core_express__sync_organization() {
        let router = oa4rust::organization_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/organization/core/express/sync")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on organization_core_express: /jaxrs/organization/core/express/sync (GET)"
        );
    }

    // 鈹€鈹€ x_portal_assemble_designer 鈫?portal_assemble_designer (12 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__portal_assemble_designer__create_design() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/assemble/designer/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__get_design() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/assemble/designer/get/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__list_designs() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/assemble/designer/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__create_page() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/assemble/designer/page/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__delete_page() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/assemble/designer/page/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__list_pages_by_category() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/assemble/designer/page/list/{{category}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__save_page() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/assemble/designer/page/save/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__get_page() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/page/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/assemble/designer/page/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__save_design() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/designer/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/assemble/designer/save/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__design_list() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/design/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/design/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__design_save() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/design/save")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/design/save (POST)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_designer__design_get() {
        let router = oa4rust::portal_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/design/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_designer: /jaxrs/portal/design/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_portal_assemble_surface 鈫?portal_assemble_surface (8 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__portal_assemble_surface__create_surface() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_surface: /jaxrs/portal/assemble/surface/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__get_surface() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_surface: /jaxrs/portal/assemble/surface/get/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__list_surfaces() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_surface: /jaxrs/portal/assemble/surface/list/{{category}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__preview_surface() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/preview/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_surface: /jaxrs/portal/assemble/surface/preview/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__publish_surface() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/assemble/surface/publish/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_surface: /jaxrs/portal/assemble/surface/publish/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__surface_list() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/surface/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_surface: /jaxrs/portal/surface/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__surface_publish() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/surface/publish")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_surface: /jaxrs/portal/surface/publish (POST)"
        );
    }
    #[tokio::test]
    async fn parity__portal_assemble_surface__surface_preview() {
        let router = oa4rust::portal_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/surface/test-id/preview")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal_assemble_surface: /jaxrs/portal/surface/{{id}}/preview (GET)"
        );
    }

    // 鈹€鈹€ x_portal_core_entity 鈫?portal (7 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__portal__dict_list() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/dict/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal: /jaxrs/portal/dict/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal__portal_list() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal: /jaxrs/portal/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal__delete_page() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/page/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal: /jaxrs/portal/page/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__portal__save_page() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/page/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal: /jaxrs/portal/page/save/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__portal__get_page() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/page/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal: /jaxrs/portal/page/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal__portal_id() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portal/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal: /jaxrs/portal/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__portal__list_portal_category() {
        let router = oa4rust::portal::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/portalcategory/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on portal: /jaxrs/portalcategory/list (GET)"
        );
    }

    // 鈹€鈹€ x_processplatform_assemble_bam 鈫?processplatform_assemble_bam (5 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__create_bam() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_bam: /jaxrs/processplatform/assemble/bam/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__delete_bam() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_bam: /jaxrs/processplatform/assemble/bam/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__get_bam_config() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_bam: /jaxrs/processplatform/assemble/bam/get/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__list_bams() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_bam: /jaxrs/processplatform/assemble/bam/list/{{category}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_bam__get_bam_status() {
        let router = oa4rust::processplatform_assemble_bam::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/bam/status/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_bam: /jaxrs/processplatform/assemble/bam/status/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_processplatform_assemble_designer 鈫?processplatform_assemble_designer (6 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__create_flow() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_designer: /jaxrs/processplatform/assemble/designer/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__delete_flow() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_designer: /jaxrs/processplatform/assemble/designer/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__get_flow() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::mock_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_designer: /jaxrs/processplatform/assemble/designer/get/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__list_flows() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_designer: /jaxrs/processplatform/assemble/designer/list/{{category}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__preview_flow() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/preview/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_designer: /jaxrs/processplatform/assemble/designer/preview/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_designer__save_flow() {
        let router = oa4rust::processplatform_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/designer/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_designer: /jaxrs/processplatform/assemble/designer/save/{{id}} (POST)"
        );
    }

    // 鈹€鈹€ x_processplatform_assemble_surface 鈫?processplatform_assemble_surface (7 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__create_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_surface: /jaxrs/processplatform/assemble/surface/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__delete_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_surface: /jaxrs/processplatform/assemble/surface/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__get_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::mock_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_surface: /jaxrs/processplatform/assemble/surface/get/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__list_surfaces() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_surface: /jaxrs/processplatform/assemble/surface/list/{{category}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__preview_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/preview/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_surface: /jaxrs/processplatform/assemble/surface/preview/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__publish_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/publish/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_surface: /jaxrs/processplatform/assemble/surface/publish/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_assemble_surface__save_surface() {
        let router = oa4rust::processplatform_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/assemble/surface/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_assemble_surface: /jaxrs/processplatform/assemble/surface/save/{{id}} (POST)"
        );
    }

    // 鈹€鈹€ x_processplatform_core_entity 鈫?processplatform_core_entity (6 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__processplatform_core_entity__task_list() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/task/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_entity: /jaxrs/process/task/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_entity__task_get() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/task/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_entity: /jaxrs/process/task/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_entity__ticket_list() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/ticket/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_entity: /jaxrs/process/ticket/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_entity__work_list() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/work/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_entity: /jaxrs/process/work/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_entity__work_get() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/work/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_entity: /jaxrs/process/work/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_entity__workcompleted_list() {
        let router = oa4rust::processplatform_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/process/workcompleted/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_entity: /jaxrs/process/workcompleted/list (GET)"
        );
    }

    // 鈹€鈹€ x_processplatform_core_express 鈫?processplatform_core_express (6 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__processplatform_core_express__task_count_with_person() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/task/count/with/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_express: /jaxrs/processplatform/task/count/with/person/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_express__task_processing() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/task/processing/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_express: /jaxrs/processplatform/task/processing/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_express__work_count_with_person() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/work/count/with/person/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_express: /jaxrs/processplatform/work/count/with/person/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_express__work_processing() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/work/processing/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_express: /jaxrs/processplatform/work/processing/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_express__work_retract() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/work/retract/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_express: /jaxrs/processplatform/work/retract/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_core_express__work_terminate() {
        let router = oa4rust::processplatform_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/work/terminate/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_core_express: /jaxrs/processplatform/work/terminate/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_processplatform_service_processing 鈫?processplatform_service_processing (21 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__processplatform_service_processing__gateway_join() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::mock_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/gateway/test-id/test-id/join")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/gateway/{{work_id}}/{{activity_token}}/join (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__cancel_process_instance() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/cancel/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/processplatform/service/processing/cancel/{{executionId}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__create_process() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/processplatform/service/processing/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__execute_process() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/execute/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/processplatform/service/processing/execute/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__gateway_fork() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/gateway/fork/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/processplatform/service/processing/gateway/fork/{{gateway_instance_id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__get_process() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/processplatform/service/processing/get/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__get_process_instance() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/instance/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/processplatform/service/processing/instance/{{executionId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__list_processes() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/processplatform/service/processing/list/{{category}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__process_id_complex() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/process/test-id/complex")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/processplatform/service/processing/process/{{id}}/complex (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__start_timer() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/timer/start")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/processplatform/service/processing/timer/start (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__cancel_timer() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/timer/test-id/cancel")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/processplatform/service/processing/timer/{{job_id}}/cancel (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_list() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/processplatform/service/processing/work/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/processplatform/service/processing/work/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__task_claim() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/task/test-id/claim")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/task/{{id}}/claim (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__task_complete() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/task/test-id/complete")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/task/{{id}}/complete (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__task_reject() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/task/test-id/reject")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/task/{{id}}/reject (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__task_transfer() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/task/test-id/transfer/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/task/{{id}}/transfer/{{person}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_complete() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/work/test-id/complete")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/work/{{id}}/complete (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_id_processing() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/work/test-id/processing")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/work/{{id}}/processing (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_v2_id_retract() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/work/test-id/retract")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/work/{{id}}/retract (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_start() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/work/test-id/start")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/work/{{id}}/start (POST)"
        );
    }
    #[tokio::test]
    async fn parity__processplatform_service_processing__work_v2_id_terminate() {
        let router = oa4rust::processplatform_service_processing::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/work/test-id/terminate")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on processplatform_service_processing: /jaxrs/work/{{id}}/terminate (POST)"
        );
    }

    // 鈹€鈹€ x_program_center 鈫?program_center (7 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__program_center__applications() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program/applications")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center: /jaxrs/program/applications (GET)"
        );
    }
    #[tokio::test]
    async fn parity__program_center__current_style() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program/appstyle/current/style")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center: /jaxrs/program/appstyle/current/style (GET)"
        );
    }
    #[tokio::test]
    async fn parity__program_center__modules_all() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program/datastructure/modules/all")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center: /jaxrs/program/datastructure/modules/all (GET)"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center: /jaxrs/program_center/agent/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__program_center__agent_save() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center: /jaxrs/program_center/agent/save/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__program_center__application_create() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/application/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center: /jaxrs/program_center/application/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__program_center__application_save() {
        let router = oa4rust::program_center::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/application/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center: /jaxrs/program_center/application/save/{{id}} (POST)"
        );
    }

    // 鈹€鈹€ x_program_center_core_entity 鈫?program_center_core_entity (15 routes async) 鈹€鈹€
    #[tokio::test]
    async fn parity__program_center_core_entity__agent_create() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/agent (POST)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__agent_list() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/agent/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__agent_update() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/agent/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/agent/{{id}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__application_create() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/application")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/application (POST)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__application_list() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/application/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/application/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__application_update() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/application/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/application/{{id}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__invoke_create() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/invoke (POST)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__invoke_list() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/invoke/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__invoke_update() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/invoke/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/invoke/{{id}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__script_create() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/script (POST)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__script_list() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/script/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__script_update() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/script/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/script/{{id}} (PUT)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__structure_create() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/structure")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/structure (POST)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__structure_list() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/structure/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/structure/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__program_center_core_entity__structure_update() {
        let router = oa4rust::program_center_core_entity::program_center_core_entity_router(shared::testing::test_pool()).await;
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/program_center/structure/test-id")
                    .method(axum::http::Method::PUT)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_center_core_entity: /jaxrs/program_center/structure/{{id}} (PUT)"
        );
    }

    // 鈹€鈹€ x_program_init 鈫?program_init (3 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__program_init__check() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/secret/check")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_init: /jaxrs/secret/check (GET)"
        );
    }
    #[tokio::test]
    async fn parity__program_init__set() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/secret/set")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_init: /jaxrs/secret/set (POST)"
        );
    }
    #[tokio::test]
    async fn parity__program_init__set_cancel() {
        let router = oa4rust::program_init::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/secret/set/cancel")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on program_init: /jaxrs/secret/set/cancel (GET)"
        );
    }

    // 鈹€鈹€ x_query_assemble_designer 鈫?query_assemble_designer (5 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__query_assemble_designer__create_designer() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_designer: /jaxrs/query/assemble/designer/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__delete_designer() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_designer: /jaxrs/query/assemble/designer/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__get_designer() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_designer: /jaxrs/query/assemble/designer/get/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__list_designers() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_designer: /jaxrs/query/assemble/designer/list/{{category}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_designer__save_designer() {
        let router = oa4rust::query_assemble_designer::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/designer/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_designer: /jaxrs/query/assemble/designer/save/{{id}} (POST)"
        );
    }

    // 鈹€鈹€ x_query_assemble_surface 鈫?query_assemble_surface (9 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__query_assemble_surface__importmodel_id_execute() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/importmodel/id/test-id/execute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_surface: /jaxrs/importmodel/id/{{id}}/execute (POST)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__create_surface() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_surface: /jaxrs/query/assemble/surface/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__delete_surface() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/delete/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_surface: /jaxrs/query/assemble/surface/delete/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__get_surface() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/get/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_surface: /jaxrs/query/assemble/surface/get/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__list_surfaces() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/list/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_surface: /jaxrs/query/assemble/surface/list/{{category}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__preview_surface() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/preview/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_surface: /jaxrs/query/assemble/surface/preview/{{id}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__save_surface() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/assemble/surface/save/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_surface: /jaxrs/query/assemble/surface/save/{{id}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__view_flag_flag_query_queryFlag_execute() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/flag/test-id/application/flag/test-id/execute")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_surface: /jaxrs/queryview/flag/{{view}}/application/flag/{{app}}/execute (GET)"
        );
    }
    #[tokio::test]
    async fn parity__query_assemble_surface__view_flag_flag_query_queryFlag_execute_v2_page_page_size_size() {
        let router = oa4rust::query_assemble_surface::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/queryview/flag/test-id/application/flag/test-id/execute/page/test-id/size/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_assemble_surface: /jaxrs/queryview/flag/{{view}}/application/flag/{{app}}/execute/page/{{page}}/size/{{size}} (GET)"
        );
    }

    // 鈹€鈹€ x_query_core_entity 鈫?query_core_entity (5 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__query_core_entity__import_list() {
        let router = oa4rust::query_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/import/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_core_entity: /jaxrs/query/import/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__query_core_entity__item_list() {
        let router = oa4rust::query_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/item/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_core_entity: /jaxrs/query/item/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__query_core_entity__view_create() {
        let router = oa4rust::query_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/view/create")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_core_entity: /jaxrs/query/view/create (POST)"
        );
    }
    #[tokio::test]
    async fn parity__query_core_entity__view_list() {
        let router = oa4rust::query_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/view/list")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_core_entity: /jaxrs/query/view/list (GET)"
        );
    }
    #[tokio::test]
    async fn parity__query_core_entity__view_get() {
        let router = oa4rust::query_core_entity::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/view/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_core_entity: /jaxrs/query/view/{{id}} (GET)"
        );
    }

    // 鈹€鈹€ x_query_core_express 鈫?query_core_express (4 routes) 鈹€鈹€
    #[tokio::test]
    async fn parity__query_core_express__get_cache_status() {
        let router = oa4rust::query_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/core/express/cache/status/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_core_express: /jaxrs/query/core/express/cache/status/{{queryId}} (GET)"
        );
    }
    #[tokio::test]
    async fn parity__query_core_express__cache_query_result() {
        let router = oa4rust::query_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/core/express/cache/test-id")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_core_express: /jaxrs/query/core/express/cache/{{queryId}} (POST)"
        );
    }
    #[tokio::test]
    async fn parity__query_core_express__execute_query() {
        let router = oa4rust::query_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/core/express/execute")
                    .method(axum::http::Method::POST)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_core_express: /jaxrs/query/core/express/execute (POST)"
        );
    }
    #[tokio::test]
    async fn parity__query_core_express__get_query_history() {
        let router = oa4rust::query_core_express::router(shared::testing::test_pool());
        let response = router
            .oneshot(
                axum::http::Request::builder()
                    .uri("/jaxrs/query/core/express/history/test-id")
                    .method(axum::http::Method::GET)
                    .body(axum::body::Body::empty())
                    .expect("build request"),
            )
            .await
            .expect("oneshot dispatch");
        assert_ne!(
            response.status(),
            axum::http::StatusCode::NOT_FOUND,
            "parity: route missing on query_core_express: /jaxrs/query/core/express/history/{{limit}} (GET)"
        );
    }

// 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
// Parity count sanity check
// 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
#[test]
fn parity_generated_test_count() {
    assert!(824 > 0, "no parity tests generated 鈥?check the Python script output");
}



