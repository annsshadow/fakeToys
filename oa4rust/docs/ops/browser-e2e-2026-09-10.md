# Browser (IAB) auth E2E — 2026-09-10
Live in-app browser run against frontend :5173 (vite proxy) + Rust :3000.
- login: POST /jaxrs/authentication/login -> 200, Set-Cookie oa4rust_session (no token in JSON). PASS
- current-user: GET /jaxrs/authentication/who with cookie -> tokenType=user, unique=it-login. PASS
- reload restore (matrix #9): hard reload on /app/dashboard stays authenticated, who still user. PASS
- logout: POST /jaxrs/authentication/logout -> 200; who then anonymous (session cleared + cookie cleared). PASS
NOTE: Set-Cookie is a forbidden response header, not readable via JS; cookie clearing proven by who->anonymous.
OPEN DEFECT (pre-existing, not auth): /app/* routes render an empty #o2-app-root in IAB
(login page renders fine); the router guard does not fire a redirect there. Separate rendering issue, tracked separately.
OAuth/OIDC real-provider flow: BLOCKED (no live provider credentials locally).
