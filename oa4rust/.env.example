# Database
DATABASE_URL=postgres://o2server:password@localhost:5432/oa4rust

# Redis（Phase B-U-B2: 默认 session 存储）
# 默认连接本地 Redis，不可达时自动降级为内存+DB 模式。
REDIS_URL=redis://127.0.0.1:6379

# Environment
RUST_ENV=development
