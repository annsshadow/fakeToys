# 2026-09-22

## augmentor 工作区清理（评估提交价值 → 全部不提交 → 处置）
- 工作区 3 处改动全部判定为运行时副产物，无提交价值：
  - `M config.yaml`：`config.py:423` 的 `yaml.safe_dump` 运行时重写所致——丢全部中文注释、清 `api_key: ${...}` 占位符 → `git checkout --` 恢复；
  - `?? augmentor/data/`（17K versions 快照）、`?? augmentor/up.json`（临时训练样本）→ 加入 .gitignore。
- 提交记录（分支 feat/three-ends-f-contract-zero）：
  - `bf56d92f` chore(augmentor): 忽略运行时副产物 data/ 与 up.json
  - `4ef9caaf` docs: 新建技术债台账，登记 TD-1
- 新建 `docs/tech-debt.md`（fakeToys 首个技术债台账）：TD-1 = config.py 保存配置时重写 config.yaml 丢注释/清占位符，仅登记不修复，候选方案含 ruamel.yaml 保留注释、可变配置与手工配置分离。
