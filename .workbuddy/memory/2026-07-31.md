# 2026-07-31

## 安全审计：已暂存内容（ai/ + storage/）

- 审计 13 个已暂存文件，发现 1 项高危 + 1 项中风险。
- **高危（已修复）**：`ai/enhanceTXT.py` 曾明文硬编码百度云 AK/SK（具体值已从本记录及代码移除）。已改为从环境变量 `BAIDU_API_KEY`/`BAIDU_SECRET_KEY` 读取并加缺失校验，执行 `git restore --staged` 取消该文件暂存，明文值已从工作区文件移除。
- **待办（用户操作）**：需在百度云控制台轮换作废原 AK/SK，仅改代码不足以防泄露（值可能已被复制/克隆）。
- **中风险（运维注意）**：`storage/install_storage_service.sh` 以 root 运行，修改 iptables/firewall-cmd、向 `/etc/crontab` 追加任务；MinIO 启动未设 `MINIO_ROOT_USER/PASSWORD`（默认 minioadmin），且脚本开放 9000 端口——公网暴露有未授权访问风险。部署时需显式设强密码并限制来源 IP。
- **无风险确认**：对 `ai/*.json` 扫描身份证/手机号/邮箱三种 PII 模式，均为 0 匹配；训练数据为租房业务 FAQ，无真实个人隐私。`check_storage_service.sh`、`README.md` 干净。
