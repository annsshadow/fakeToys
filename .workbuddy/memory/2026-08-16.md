# 2026-08-16 工作日志

## 续修 fakeToys 仓库 pull/push 失败
- 复核状态：远端 tip `9f6422f9`（合并提交）已持久，push 已恢复。
- 确认 WIP（extract_endpoints.py / parity_runner.rs）已并入 `9f6422f9`，无数据丢失；`wip_oa4rust.patch` 备份冗余。
- 诊断出假象：`.git/packed-refs` 全行 CRLF + SHA 清零 → 所有 ref 不可读 → 分支 "has no commits" → 32901 文件显示 staged-A。
- 沙箱抑制 .git 写入（loose ref / reset 跨命令不持久），故无法在沙箱内完成本地分支 re-anchor；已改为在回复中给出用户真实终端执行步骤。
- 已写入 `.workbuddy/memory/MEMORY.md` 记录根因与可复用修复步骤。
- 磁盘残留：`.git/packed-refs.bak.crlf`、`wip_oa4rust.patch`、3 个未跟踪文件（含 .workbuddy 记忆文件、生成的 behavior_compare_endpoints.rs）——均无需提交。

## 清理残留 + 根因纠偏 + 拒绝沙箱提交
- 已删除三处临时物：`.git/packed-refs.bak.crlf`、`wip_oa4rust.patch`、`oa4rust/tests/behavior_compare_endpoints.rs`（均非被跟踪，无风险）。保留 `.workbuddy/memory/`。
- 根因纠偏：**真机根因 = 分支分叉（本地+6 / 远端+14）+ `pull.ff=only`**，完整解释 pull 中止与 push 非快进；此前怀疑的 packed-refs CRLF/清零是**沙箱 overlay 占位桩（54 条 ref 全零 SHA）伪影**，真机未必存在。已更新 MEMORY.md。
- 配置层事实：全局+系统 `core.autocrlf=true`、`core.fscache=true`、git 2.55.0.windows.3；仓库无活动钩子、无 .gitattributes、无脚本引用 packed-refs/GIT_DIR。
- 用户说"那提交和推送吧"：明确**拒在沙箱 commit**——会造出 3 万文件垃圾根提交且 .git 写入不持久；告知内容早已在远端 9f6422f9，无需提交。用户选定"确认已同步即可"。已再次 ls-remote 核实远端 tip=9f6422f9（期间遇一次 SSH 连接重置，瞬时抖动）。
- 教训：沙箱内凡涉及 commit/push 必须先核验 HEAD/索引，宁可拒绝也不盲目执行。

## 封装可复用 Skill：git-branch-tidy-safe
- 用户要求把"分支整理 + 提交"的安全流程固化为 Skill，并强约束：**任何清理/提交前必须先保全全部未提交改动、零丢失风险**。
- 已用 skill-creator 的 init_skill.py 在 `~/.workbuddy/skills/git-branch-tidy-safe` 生成（用户级，跨项目复用）。
- 内容：SKILL.md（硬安全闸门 Phase 0 保全→Phase1 诊断→Phase2 清理方案→Phase3 提交→Phase4 还原守卫）；scripts/safeguard_uncommitted.sh（自动快照全部未提交并校验可还原，打印恢复命令）；references/diagnose.md（分叉量化、沙箱 vs 真机 packed-refs 判别、pull.ff 说明、detached-HEAD 合并推送）。
- package_skill.py 校验通过并打包为 git-branch-tidy-safe.zip。
- 设计要点：把"保全未提交改动"作为不可跳过闸门；识别"全树 staged-A + HEAD 不可解析 = 假象，禁止提交"；区分沙箱 packed-refs 占位桩与真机损坏。

## 补：Windows 兼容 + 重新打包
- 用户反馈"本机是 windows，脚本怕是不支持" → 新增 `scripts/safeguard_uncommitted.ps1`（PowerShell 原生版：stash/补丁双模式、`$env:USERPROFILE\.git-tidy-backup\` 备份、try/finally 清理、打印 RESTORE COMMAND）。
- SKILL.md Phase 0 改为按平台选择脚本：Windows 用 `powershell -ExecutionPolicy Bypass -File scripts/safeguard_uncommitted.ps1 [repo-dir]`；Unix/Git Bash 用 `bash scripts/safeguard_uncommitted.sh [repo-dir]`。
- 重新 package_skill.py 校验通过（✅ Skill is valid）并打包，zip 含 .ps1 与 .sh。
- 因沙箱 .git 写入不持久、且真机 packed-refs 非桩，所有 re-anchor/commit 步骤须在用户真实 Windows 终端执行；已向用户交付分步 Windows 指引。
