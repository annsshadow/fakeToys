# 2026-08-14 oa4rust 全量替换进度

## 上下文
延续 oa4rust 对 oa/o2server 的替换。上一会话遗留：127 个 list GET 端点 HTTP500，且诊断注入（tracing）未能编译（部分 crate 无 tracing 依赖）。

## 关键事件
1. **诊断注入失败 + 源码损坏事故**：为看 500 真因，注入 `tracing::error!` 到 122 文件；但 `process_bam`/`general` 等 crate 无 tracing 依赖 → 编译失败。随后写的 `clean_inject.py` 把含内联 tracing 的**整行删除**，破坏了源码（app.rs 等）。
   - 恢复：`git stash` 备份已跟踪改动（迁移 SQL 026-033 为未跟踪，自动保留）→ `git checkout -- crates/` 回到干净 HEAD 基线。备份在 stash `diag-injection-backup`。
   - 教训：注入式诊断很脆；优先用"读代码 + 机械化修复"，不要依赖编译期注入。

2. **系统性根因与修复（真实修复，已落到源码/迁移）**：
   - `X.` schema 前缀 bug：39 处 handler 写 `X.AI_MODEL` 等，但只有 `public` schema，表实为 `x_ai_model`。`scripts/fix_x_schema.py` 把 `X.<UPPER>` → `x_<lower>`（9 文件/39 处）。
   - 缺失表：13 个（`x_ai_clue`…`x_pp_c_task`）+ 9 个（`x_general_assemble_excel_result`…`x_program_deploy_server`）。分别由 `migrations/034`、`036` 建表（含引用列 + 标准审计列）。
   - 缺失列：32 个（含 `x_ai_model` 的 `xname/xtype/xmodel/xenable/xcompletionurl/xapikey/xasdefault/xdesc` 等），`migrations/035` 补齐。
   - `LIMIT $n`→`LIMIT $n::bigint` / `OFFSET` 同理：`scripts/fix_limit_params.py`（SQL 端强转 BIGINT，化解 i32 绑定类型问题）。
   - `organization_assemble_control` 13 处页大小 `let count: i32` → `i64`。
   - 复检：`gen_missing_columns.py --dry` 报告 **0 缺失列**；库表 292→314。

## 进行中
- 干净构建（无注入）已触发，需 kill 旧锁二进制的进程后重build。
- 计划：重启 → 重跑 `scripts/probe_endpoints.py` 度量剩余 500；残余按"读 handler 代码"定位（不靠注入）。

## 待办（全量替换长期）
- 残余 500 诊断闭环；把 parity 审计/探测脚本纳入 CI。
- 消除 201 处 `Value::Null` 静默空标记；真实业务实现（CMS/processplatform/workflow）。
- LDAP/WeCom/DingTalk 鉴权；多 DB 适配；分布式/缓存/搜索；灰度切换。
- 可选：把 probe/parity 工具包沉淀为 skill。
