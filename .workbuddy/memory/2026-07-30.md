# 2026-07-30 工作记录

## fakeToys 仓库 git 问题排查与修复

### 1. 远程地址拼写错误(已修复)
- 原 `origin` 为 `git@github.com:annssadow/fakeToys.git`(少一个 h),导致 pull/push 报 "Repository not found"。
- SSH 鉴权本身正常(账户 `annsshadow`)。真实仓库是 `git@github.com:annsshadow/fakeToys.git`。
- 已用 `git remote set-url origin git@github.com:annsshadow/fakeToys.git` 修正。

### 2. main 与远程分叉(已对齐)
- 根因:本地 `main`(194add26) 与远程 `main`(358e93e) **无共同祖先(两条不相关历史)**,但两份 **tip 的工作区内容完全一致**(同一份代码各自独立提交)。
- `git merge-base` 返回空;`git diff --stat` 空。
- 处理:未做 merge(避免重复提交噪音),改用 `git reset --hard 358e93eb64e955719b2a488aa009645050569ae2` 让本地 main 对齐远程 main。丢弃的 5 个本地提交内容已被远程覆盖,且 reflog 可恢复。
- 已设 `git config pull.ff only`(本仓库),避免日后 pull 再报 "specify how to reconcile"。

### 3. 环境特性(重要):远程跟踪引用无法在本工具沙箱内持久化
- 现象:本 Bash 工具里 `git fetch`/`update-ref` 报 "new branch"/成功,但 `git rev-parse origin/main` 仍 unknown、`git branch -r` 为空;`refs/remotes/origin/` 目录会被 fetch 清掉。底层 shell 直接 `mkdir`+`echo` 写引用却能被 git 读到。
- 用户自己的 PowerShell 终端里 git 正常(之前的报错都是真实 git 信息,非沙箱造成)。
- 可行变通:用 `git ls-remote origin` 取权威 SHA → `git reset --hard <sha>` 对齐,而非依赖 `origin/*` 引用。

### 4. 各分支同步状态
- main: 本地=远程=358e93e(已同步)。
- feat/watering-poc: 本地=远程=194add2(已同步);注意它比 main 落后。
- feat/systems-documentation: 本地 194add2,远程 358e93e(落后,用户未要求同步)。
- `watering/`、`csv2sql/` 等被 .gitignore 忽略,未纳入版本控制。
