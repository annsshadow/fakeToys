# 2026-08-20 工作日志

## oa4rust 分批提交（沙箱 refs 不持久，交付真机脚本）

- 任务：分批提交工作区修改。待提交清单（相对 HEAD=160f71ff 的真实差异）：批次 1 = 57 个 oa4rust 文件（47 个 tests_generated.rs 扩充 + 5 个 lib.rs 挂载 `#[cfg(test)] mod tests_generated;` + preview/realtime/search/signature/sms 5 个新 tests_generated.rs）；批次 2 = docs/brainstorms/2026-08-20-oa4rust-remaining-gap-closure-requirements.md + docs/plans/2026-08-20-001-feat-oa4rust-remaining-gap-closure-plan.md。
- **沙箱 git 写入不持久的新证据**：`git update-ref refs/heads/feat/oa4rust-completion-phase0 fb171717` 报告 exit=0 且（升级授权后）ref 文件短暂可见，但**跨命令立即丢失**（`refs/heads/feat/` 目录整体消失）；`.git/` 根下普通文件写入持久（探针验证），对象库 objects 写入持久（fb171717 提交对象跨命令可解析），唯独 **refs/ 目录写入不持久**。→ 沙箱内无法完成可信提交，commit 后 ref 更新必然丢失，且可能产生"空仓库假象"（HEAD 不可解析 + 32857 个 staged-A 污染）。
- **批次 2 的坑**：HEAD 不可解析期间执行 commit 会把整个 index（32857 条目）提交成**无父根提交** c70f426d——已弃用（dangling 无引用，gc 清理）。正确做法：先恢复 ref 再 reset --mixed HEAD 清理 index，再按批次 add+commit。
- 交付：`D:\WORKSPACE\fakeToys\commit_batch_oa4rust.ps1`（UTF-8 BOM，PowerShell 5.1 兼容），真机运行即完成两批提交；逻辑含 HEAD 可解析性检查（不可解析则提示先跑 recover_repo_sync.ps1）、mixed reset 清 index 污染、无差异批次自动跳过。沙箱中生成的 fb171717（批次1提交对象）仅供参考，真机按自身 HEAD 重新提交。
- 经验：**git 写操作前先确认运行环境**（沙箱 or 真机）——本会话最初 git log 正常（160f71ff 可解析）造成"环境健康"假象，commit 后才暴露 refs 漂移；判断依据：`git update-ref` 后立即 `cat refs/...` 是否落盘。
