[pytest]
testpaths = tests
addopts =
    -ra
    --strict-markers
    --cov=augmentor
    --cov=api
    --cov=cli
    --cov-report=term-missing:skip-covered
    --cov-report=xml:coverage.xml
    --cov-fail-under=80
